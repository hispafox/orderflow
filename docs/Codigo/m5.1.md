# M5.1 — JWT, OAuth 2.0 y ASP.NET Core Identity

## Branch

```bash
git checkout m4.4
git checkout -b m5.1
```

## Objetivo

Añadir autenticación y autorización a OrderFlow con ASP.NET Core Identity embebido en Gateway.API. Sin procesos externos, sin Java, sin Docker — solo .NET. Al terminar, ninguna petición sin token llega a los servicios internos, `john@techshop.es` solo ve sus pedidos, y `admin@techshop.es` puede verlos todos.

## Qué se construye en este módulo

```
src/Gateway.API/
  ├── Data/
  │   ├── GatewayIdentityDbContext.cs      ← nuevo
  │   ├── ApplicationUser.cs               ← nuevo
  │   └── SeedData.cs                      ← nuevo
  ├── Services/
  │   └── JwtService.cs                    ← nuevo
  ├── Areas/Identity/Pages/Account/
  │   ├── Login.cshtml                     ← scaffolded (generado)
  │   └── Login.cshtml.cs                  ← modificar: generar JWT
  ├── Program.cs                           ← actualizar: Identity + JWT + Auth en YARP
  ├── appsettings.json                     ← añadir Jwt section
  └── appsettings.Development.json         ← añadir IdentityDb connection string

src/Orders.API/
  ├── API/Controllers/OrdersController.cs  ← añadir [Authorize] + userId del claim
  ├── Application/Queries/ListOrdersQuery.cs  ← añadir UserId + IsAdmin
  ├── Application/Queries/ListOrdersHandler.cs ← filtrar por userId si no admin
  └── Program.cs                           ← añadir AddAuthentication+AddJwtBearer

infrastructure/OrderFlow.AppHost/
  └── Program.cs                           ← añadir identityDb

tests/Orders.API.Tests/Integration/
  ├── Shared/JwtTestHelper.cs              ← nuevo
  └── Auth/AuthenticationTests.cs          ← nuevo
```

---

## Paso 1 — Instalar paquetes en Gateway.API

```bash
dotnet add src/Gateway.API/Gateway.API.csproj \
    package Microsoft.AspNetCore.Identity.EntityFrameworkCore

dotnet add src/Gateway.API/Gateway.API.csproj \
    package Microsoft.EntityFrameworkCore.SqlServer

dotnet add src/Gateway.API/Gateway.API.csproj \
    package Microsoft.EntityFrameworkCore.Design

dotnet add src/Gateway.API/Gateway.API.csproj \
    package Microsoft.AspNetCore.Authentication.JwtBearer

dotnet add src/Gateway.API/Gateway.API.csproj \
    package System.IdentityModel.Tokens.Jwt
```

---

## Paso 2 — ApplicationUser.cs (fichero completo)

Crear `src/Gateway.API/Data/ApplicationUser.cs`:

```csharp
using Microsoft.AspNetCore.Identity;

namespace Gateway.API.Data;

/// <summary>
/// Usuario de OrderFlow. Extiende IdentityUser añadiendo campos de negocio.
/// Se persiste en IdentityDb (LocalDB). En producción se migra a Azure AD.
/// </summary>
public class ApplicationUser : IdentityUser
{
    public string?   FullName  { get; set; }
    public DateTime  CreatedAt { get; set; } = DateTime.UtcNow;
}
```

---

## Paso 3 — GatewayIdentityDbContext.cs (fichero completo)

Crear `src/Gateway.API/Data/GatewayIdentityDbContext.cs`:

```csharp
using Microsoft.AspNetCore.Identity.EntityFrameworkCore;
using Microsoft.EntityFrameworkCore;

namespace Gateway.API.Data;

/// <summary>
/// DbContext para ASP.NET Core Identity.
/// Gestiona: AspNetUsers, AspNetRoles, AspNetUserRoles,
///           AspNetUserClaims, AspNetUserTokens (refresh tokens).
/// Schema: dbo (default de Identity).
/// </summary>
public class GatewayIdentityDbContext : IdentityDbContext<ApplicationUser>
{
    public GatewayIdentityDbContext(
        DbContextOptions<GatewayIdentityDbContext> options)
        : base(options) { }
}
```

---

## Paso 4 — JwtService.cs (fichero completo)

Crear `src/Gateway.API/Services/JwtService.cs`:

```csharp
using System.IdentityModel.Tokens.Jwt;
using System.Security.Claims;
using System.Text;
using Gateway.API.Data;
using Microsoft.AspNetCore.Identity;
using Microsoft.IdentityModel.Tokens;

namespace Gateway.API.Services;

/// <summary>
/// Genera JWT firmados con HS256 cuando el usuario hace login correctamente.
/// Claims incluidos: sub (userId), email, name, role(s), jti, iss, aud, exp, iat.
/// La SigningKey vive en User Secrets — nunca en appsettings.json ni en el repo.
/// </summary>
public class JwtService
{
    private readonly IConfiguration                  _config;
    private readonly UserManager<ApplicationUser>    _userManager;
    private readonly ILogger<JwtService>             _logger;

    public JwtService(
        IConfiguration               config,
        UserManager<ApplicationUser> userManager,
        ILogger<JwtService>          logger)
    {
        _config      = config;
        _userManager = userManager;
        _logger      = logger;
    }

    /// <summary>
    /// Genera un JWT firmado para el usuario dado.
    /// Incluye roles como claims de tipo ClaimTypes.Role.
    /// </summary>
    public async Task<string> GenerateTokenAsync(ApplicationUser user)
    {
        var roles = await _userManager.GetRolesAsync(user);

        var claims = new List<Claim>
        {
            new(JwtRegisteredClaimNames.Sub,   user.Id),
            new(JwtRegisteredClaimNames.Email, user.Email!),
            new(JwtRegisteredClaimNames.Jti,   Guid.NewGuid().ToString()),
            new("name",                         user.FullName ?? user.Email!),
        };

        foreach (var role in roles)
            claims.Add(new Claim(ClaimTypes.Role, role));

        var key = new SymmetricSecurityKey(
            Encoding.UTF8.GetBytes(_config["Jwt:SigningKey"]!));

        var expiryMinutes = int.Parse(_config["Jwt:ExpiryMinutes"] ?? "60");

        var token = new JwtSecurityToken(
            issuer:             _config["Jwt:Issuer"],
            audience:           _config["Jwt:Audience"],
            claims:             claims,
            notBefore:          DateTime.UtcNow,
            expires:            DateTime.UtcNow.AddMinutes(expiryMinutes),
            signingCredentials: new SigningCredentials(key, SecurityAlgorithms.HmacSha256));

        var tokenString = new JwtSecurityTokenHandler().WriteToken(token);

        _logger.LogInformation(
            "JWT generated for user {UserId} with roles [{Roles}], expires in {Minutes} min",
            user.Id, string.Join(", ", roles), expiryMinutes);

        return tokenString;
    }
}
```

---

## Paso 5 — SeedData.cs (fichero completo)

Crear `src/Gateway.API/Data/SeedData.cs`:

```csharp
using Microsoft.AspNetCore.Identity;

namespace Gateway.API.Data;

/// <summary>
/// Crea roles y usuarios de prueba al arrancar en desarrollo.
/// Idempotente: verifica si ya existen antes de crear.
/// Usuarios creados:
///   admin@techshop.es / Admin1234! → rol admin
///   john@techshop.es  / Customer1234! → rol customer
/// </summary>
public static class SeedData
{
    public static async Task InitializeAsync(IServiceProvider services)
    {
        var userManager = services.GetRequiredService<UserManager<ApplicationUser>>();
        var roleManager = services.GetRequiredService<RoleManager<IdentityRole>>();
        var logger      = services.GetRequiredService<ILogger<GatewayIdentityDbContext>>();

        // ─── Roles ────────────────────────────────────────────────────────────
        foreach (var role in new[] { "admin", "customer" })
        {
            if (!await roleManager.RoleExistsAsync(role))
            {
                await roleManager.CreateAsync(new IdentityRole(role));
                logger.LogInformation("Role '{Role}' created", role);
            }
        }

        // ─── Admin ────────────────────────────────────────────────────────────
        if (await userManager.FindByEmailAsync("admin@techshop.es") is null)
        {
            var admin = new ApplicationUser
            {
                UserName = "admin@techshop.es",
                Email    = "admin@techshop.es",
                FullName = "Admin TechShop"
            };
            var result = await userManager.CreateAsync(admin, "Admin1234!");
            if (result.Succeeded)
            {
                await userManager.AddToRoleAsync(admin, "admin");
                logger.LogInformation("Admin user created: admin@techshop.es");
            }
            else
            {
                logger.LogError("Failed to create admin: {Errors}",
                    string.Join(", ", result.Errors.Select(e => e.Description)));
            }
        }

        // ─── Customer de prueba ───────────────────────────────────────────────
        if (await userManager.FindByEmailAsync("john@techshop.es") is null)
        {
            var john = new ApplicationUser
            {
                UserName = "john@techshop.es",
                Email    = "john@techshop.es",
                FullName = "John Doe"
            };
            var result = await userManager.CreateAsync(john, "Customer1234!");
            if (result.Succeeded)
            {
                await userManager.AddToRoleAsync(john, "customer");
                logger.LogInformation("Customer user created: john@techshop.es");
            }
        }
    }
}
```

---

## Paso 6 — Endpoint de login con token

En lugar del scaffold completo (requiere `dotnet-aspnet-codegenerator`), crear el endpoint de login como Minimal API directamente en Program.cs. Más simple y sin dependencia de la herramienta de scaffolding:

Crear `src/Gateway.API/Endpoints/AccountEndpoints.cs`:

```csharp
using System.ComponentModel.DataAnnotations;
using Gateway.API.Data;
using Gateway.API.Services;
using Microsoft.AspNetCore.Identity;

namespace Gateway.API.Endpoints;

/// <summary>
/// Endpoints de autenticación del Gateway.
/// POST /account/login → valida credenciales → devuelve JWT
/// </summary>
public static class AccountEndpoints
{
    public static WebApplication MapAccountEndpoints(this WebApplication app)
    {
        var group = app.MapGroup("/account")
            .WithTags("Auth")
            .AllowAnonymous();

        /// <summary>Login: valida email+password, devuelve JWT.</summary>
        group.MapPost("/login", async (
            LoginRequest              request,
            SignInManager<ApplicationUser> signInManager,
            UserManager<ApplicationUser>   userManager,
            JwtService                     jwtService) =>
        {
            var user = await userManager.FindByEmailAsync(request.Email);
            if (user is null)
                return Results.Problem(
                    title:      "Invalid credentials",
                    detail:     "Email or password is incorrect",
                    statusCode: StatusCodes.Status401Unauthorized);

            var result = await signInManager.CheckPasswordSignInAsync(
                user, request.Password, lockoutOnFailure: true);

            if (!result.Succeeded)
            {
                var detail = result.IsLockedOut
                    ? "Account is locked out"
                    : "Email or password is incorrect";
                return Results.Problem(
                    title:      "Invalid credentials",
                    detail:     detail,
                    statusCode: StatusCodes.Status401Unauthorized);
            }

            var token = await jwtService.GenerateTokenAsync(user);

            return Results.Ok(new
            {
                access_token = token,
                token_type   = "Bearer",
                expires_in   = 3600,
                user_id      = user.Id,
                email        = user.Email,
                full_name    = user.FullName
            });
        });

        return app;
    }
}

public record LoginRequest(
    [Required, EmailAddress] string Email,
    [Required]               string Password);
```

---

## Paso 7 — Actualizar appsettings de Gateway.API

Actualizar `src/Gateway.API/appsettings.json` — añadir sección Jwt:

```json
{
  "Serilog": {
    "MinimumLevel": {
      "Default": "Information",
      "Override": {
        "Microsoft": "Warning",
        "Yarp": "Warning"
      }
    }
  },
  "Jwt": {
    "Issuer":        "orderflow-gateway",
    "Audience":      "orderflow",
    "ExpiryMinutes": "60"
  },
  "ReverseProxy": {
    "...": "misma configuración de M4.4"
  }
}
```

Actualizar `src/Gateway.API/appsettings.Development.json`:

```json
{
  "Serilog": {
    "MinimumLevel": { "Default": "Debug" }
  },
  "ConnectionStrings": {
    "IdentityDb": "Server=(localdb)\\MSSQLLocalDB;Database=IdentityDb;Trusted_Connection=true;TrustServerCertificate=true"
  }
}
```

Configurar User Secrets **en Gateway.API** (la clave no va al repo):

```bash
cd src/Gateway.API
dotnet user-secrets init
dotnet user-secrets set "Jwt:SigningKey" "orderflow-dev-signing-key-min-32-chars!!"
```

---

## Paso 8 — Program.cs del Gateway actualizado (fichero completo)

Reemplazar `src/Gateway.API/Program.cs`:

```csharp
using System.Text;
using System.Threading.RateLimiting;
using Gateway.API.Data;
using Gateway.API.Endpoints;
using Gateway.API.Services;
using Microsoft.AspNetCore.Authentication.JwtBearer;
using Microsoft.AspNetCore.Identity;
using Microsoft.EntityFrameworkCore;
using Microsoft.IdentityModel.Tokens;
using Serilog;
using Serilog.Events;

var builder = WebApplication.CreateBuilder(args);

// ─── ServiceDefaults ─────────────────────────────────────────────────────────
builder.AddServiceDefaults();

// ─── Serilog ─────────────────────────────────────────────────────────────────
builder.Host.UseSerilog((ctx, svc, cfg) => cfg
    .ReadFrom.Configuration(ctx.Configuration)
    .ReadFrom.Services(svc)
    .Enrich.FromLogContext()
    .Enrich.WithProperty("Service", "gateway-api")
    .Enrich.WithMachineName());

// ─── Identity DbContext ───────────────────────────────────────────────────────
builder.Services.AddDbContext<GatewayIdentityDbContext>(options =>
    options.UseSqlServer(
        builder.Configuration.GetConnectionString("IdentityDb"),
        sql => sql.MigrationsHistoryTable("__EFMigrationsHistory", "identity")));

// ─── ASP.NET Core Identity ────────────────────────────────────────────────────
builder.Services
    .AddIdentity<ApplicationUser, IdentityRole>(options =>
    {
        options.Password.RequiredLength         = 8;
        options.Password.RequireNonAlphanumeric = false;
        options.SignIn.RequireConfirmedEmail     = false; // sin confirmación en desarrollo
        options.Lockout.MaxFailedAccessAttempts = 5;
        options.Lockout.DefaultLockoutTimeSpan  = TimeSpan.FromMinutes(5);
    })
    .AddEntityFrameworkStores<GatewayIdentityDbContext>()
    .AddDefaultTokenProviders();

// ─── JWT Authentication ───────────────────────────────────────────────────────
var jwtSettings  = builder.Configuration.GetSection("Jwt");
var signingKey   = new SymmetricSecurityKey(
    Encoding.UTF8.GetBytes(jwtSettings["SigningKey"]!));

builder.Services
    .AddAuthentication(JwtBearerDefaults.AuthenticationScheme)
    .AddJwtBearer(options =>
    {
        options.TokenValidationParameters = new TokenValidationParameters
        {
            ValidateIssuer           = true,
            ValidIssuer              = jwtSettings["Issuer"],
            ValidateAudience         = true,
            ValidAudience            = jwtSettings["Audience"],
            ValidateLifetime         = true,
            IssuerSigningKey         = signingKey,
            ClockSkew                = TimeSpan.FromSeconds(30)
        };

        options.Events = new JwtBearerEvents
        {
            OnAuthenticationFailed = ctx =>
            {
                Log.Warning(
                    "JWT validation failed: {Error} for {Path}",
                    ctx.Exception.Message,
                    ctx.HttpContext.Request.Path);
                return Task.CompletedTask;
            },
            OnTokenValidated = ctx =>
            {
                var userId = ctx.Principal?
                    .FindFirst(System.Security.Claims.ClaimTypes.NameIdentifier)?.Value;
                Log.Debug("JWT valid for user {UserId}", userId);
                return Task.CompletedTask;
            }
        };
    });

// ─── Authorization Policies ───────────────────────────────────────────────────
builder.Services.AddAuthorization(options =>
{
    options.AddPolicy("CustomerOrAdmin", policy =>
        policy.RequireRole("customer", "admin"));

    options.AddPolicy("AdminOnly", policy =>
        policy.RequireRole("admin"));
});

// ─── JwtService ───────────────────────────────────────────────────────────────
builder.Services.AddScoped<JwtService>();

// ─── YARP ─────────────────────────────────────────────────────────────────────
builder.Services
    .AddReverseProxy()
    .LoadFromConfig(builder.Configuration.GetSection("ReverseProxy"));

// ─── Rate Limiting (mismo que M4.4) ──────────────────────────────────────────
builder.Services.AddRateLimiter(options =>
{
    options.AddSlidingWindowLimiter("PerIp", config =>
    {
        config.Window              = TimeSpan.FromMinutes(1);
        config.PermitLimit         = 100;
        config.SegmentsPerWindow   = 4;
        config.QueueLimit          = 20;
        config.QueueProcessingOrder = QueueProcessingOrder.OldestFirst;
    });
    options.AddFixedWindowLimiter("CreateOrder", config =>
    {
        config.Window      = TimeSpan.FromMinutes(1);
        config.PermitLimit = 10;
        config.QueueLimit  = 0;
    });
    options.RejectionStatusCode = StatusCodes.Status429TooManyRequests;
    options.OnRejected = async (context, ct) =>
    {
        context.HttpContext.Response.Headers.RetryAfter = "60";
        await context.HttpContext.Response.WriteAsJsonAsync(new
        {
            title  = "Too Many Requests",
            status = 429,
            detail = "Rate limit exceeded. Please try again in 60 seconds."
        }, ct);
    };
});

// ─── CORS ─────────────────────────────────────────────────────────────────────
builder.Services.AddCors(options =>
{
    options.AddPolicy("TechShopFrontend", policy =>
    {
        policy.WithOrigins(
                "https://techshop.es",
                builder.Environment.IsDevelopment() ? "http://localhost:3000" : string.Empty)
              .AllowAnyMethod()
              .AllowAnyHeader()
              .WithExposedHeaders("X-Correlation-Id", "Location", "Retry-After");
    });
});

builder.Services.AddHealthChecks();

// ─── Build ────────────────────────────────────────────────────────────────────
var app = builder.Build();

// ─── Seed de usuarios de prueba ───────────────────────────────────────────────
if (app.Environment.IsDevelopment())
{
    using var scope = app.Services.CreateScope();
    await SeedData.InitializeAsync(scope.ServiceProvider);
}

// ─── Pipeline (orden crítico) ─────────────────────────────────────────────────
app.UseCors("TechShopFrontend");

// CorrelationId
app.Use(async (context, next) =>
{
    var correlationId = context.Request.Headers["X-Correlation-Id"].FirstOrDefault()
        ?? System.Diagnostics.Activity.Current?.TraceId.ToString()
        ?? Guid.NewGuid().ToString("N");
    context.Items["CorrelationId"] = correlationId;
    context.Response.Headers["X-Correlation-Id"] = correlationId;
    await next();
});

app.UseSerilogRequestLogging(opts =>
{
    opts.GetLevel = (ctx, _, _) =>
        ctx.Request.Path.StartsWithSegments("/health")
            ? LogEventLevel.Verbose
            : LogEventLevel.Information;
    opts.EnrichDiagnosticContext = (diag, ctx) =>
        diag.Set("CorrelationId", ctx.Items["CorrelationId"]?.ToString());
});

// Auth — antes de RateLimiter y YARP
app.UseAuthentication();
app.UseAuthorization();

app.UseRateLimiter();

// Endpoints de autenticación (anónimos)
app.MapAccountEndpoints();

// YARP (protegido por las policies de cada ruta)
app.MapReverseProxy();

app.MapDefaultEndpoints();

app.Run();
```

---

## Paso 9 — Actualizar appsettings.json de YARP con policies de autorización

Actualizar la sección `ReverseProxy` en `src/Gateway.API/appsettings.json`:

```json
{
  "ReverseProxy": {
    "Routes": {
      "orders-create-route": {
        "ClusterId": "orders-cluster",
        "Match": { "Path": "/api/orders", "Methods": [ "POST" ] },
        "AuthorizationPolicy": "CustomerOrAdmin",
        "RateLimiterPolicy": "CreateOrder",
        "Transforms": [
          { "RequestHeadersCopy": "true" },
          { "RequestHeader": "X-Gateway-Source", "Set": "orderflow-gateway" }
        ]
      },
      "orders-read-route": {
        "ClusterId": "orders-cluster",
        "Match": { "Path": "/api/orders/{**catch-all}", "Methods": [ "GET", "POST" ] },
        "AuthorizationPolicy": "CustomerOrAdmin",
        "RateLimiterPolicy": "PerIp",
        "Transforms": [
          { "RequestHeadersCopy": "true" },
          { "RequestHeader": "X-Gateway-Source", "Set": "orderflow-gateway" }
        ]
      },
      "products-route": {
        "ClusterId": "products-cluster",
        "Match": { "Path": "/api/products/{**catch-all}" },
        "RateLimiterPolicy": "PerIp",
        "Transforms": [ { "RequestHeadersCopy": "true" } ]
      },
      "payments-route": {
        "ClusterId": "payments-cluster",
        "Match": { "Path": "/api/payments/{**catch-all}" },
        "AuthorizationPolicy": "AdminOnly",
        "RateLimiterPolicy": "PerIp",
        "Transforms": [ { "RequestHeadersCopy": "true" } ]
      },
      "notifications-route": {
        "ClusterId": "notifications-cluster",
        "Match": { "Path": "/api/notifications/{**catch-all}" },
        "AuthorizationPolicy": "AdminOnly"
      }
    }
  }
}
```

---

## Paso 10 — Migration de Identity

```bash
dotnet ef migrations add InitialIdentity \
    --project src/Gateway.API \
    --context GatewayIdentityDbContext \
    --output-dir Data/Migrations

dotnet ef database update \
    --project src/Gateway.API \
    --context GatewayIdentityDbContext
```

---

## Paso 11 — Añadir JWT validation a Orders.API

### User Secrets en Orders.API

```bash
cd src/Orders.API
dotnet user-secrets init
dotnet user-secrets set "Jwt:SigningKey" "orderflow-dev-signing-key-min-32-chars!!"
```

La clave debe ser **idéntica** a la del Gateway.

Añadir sección Jwt a `src/Orders.API/appsettings.json`:

```json
{
  "Jwt": {
    "Issuer":   "orderflow-gateway",
    "Audience": "orderflow"
  }
}
```

### Actualizar Program.cs de Orders.API — añadir autenticación

Añadir al bloque de servicios de `src/Orders.API/Program.cs`:

```csharp
using Microsoft.AspNetCore.Authentication.JwtBearer;
using Microsoft.IdentityModel.Tokens;
using System.Text;

// ─── JWT Authentication ───────────────────────────────────────────────────────
var jwtKey = builder.Configuration["Jwt:SigningKey"]!;

builder.Services
    .AddAuthentication(JwtBearerDefaults.AuthenticationScheme)
    .AddJwtBearer(options =>
    {
        options.TokenValidationParameters = new TokenValidationParameters
        {
            ValidateIssuer           = true,
            ValidIssuer              = builder.Configuration["Jwt:Issuer"],
            ValidateAudience         = true,
            ValidAudience            = builder.Configuration["Jwt:Audience"],
            ValidateLifetime         = true,
            IssuerSigningKey         = new SymmetricSecurityKey(
                Encoding.UTF8.GetBytes(jwtKey)),
            ClockSkew                = TimeSpan.FromSeconds(30)
        };
    });

builder.Services.AddAuthorization(options =>
{
    options.AddPolicy("CustomerOrAdmin", policy =>
        policy.RequireRole("customer", "admin"));

    options.AddPolicy("AdminOnly", policy =>
        policy.RequireRole("admin"));
});
```

Añadir al pipeline en `Program.cs` de Orders.API (antes de `MapControllers`):

```csharp
app.UseAuthentication();
app.UseAuthorization();
```

---

## Paso 12 — Actualizar OrdersController con autorización y ownership

Actualizar `src/Orders.API/API/Controllers/OrdersController.cs`:

```csharp
using System.Security.Claims;
using Microsoft.AspNetCore.Authorization;

[ApiController]
[Route("api/[controller]")]
[Authorize(Policy = "CustomerOrAdmin")]   // ← JWT obligatorio en todos los endpoints
public class OrdersController : ControllerBase
{
    private readonly IMediator _mediator;

    public OrdersController(IMediator mediator) => _mediator = mediator;

    // Extrae el userId del JWT — nunca del request body ni de la URL
    private string GetUserId() =>
        User.FindFirstValue(ClaimTypes.NameIdentifier)
        ?? User.FindFirstValue("sub")
        ?? throw new UnauthorizedAccessException("User ID not found in token");

    private bool IsAdmin() => User.IsInRole("admin");

    [HttpGet]
    public async Task<ActionResult<PagedResult<OrderSummaryDto>>> GetAll(
        [FromQuery] string? status,
        [FromQuery] int page     = 1,
        [FromQuery] int pageSize = 20,
        CancellationToken ct     = default)
    {
        var userId = GetUserId();
        // Customers solo ven sus pedidos. Admins ven todos.
        return Ok(await _mediator.Send(
            new ListOrdersQuery(status, page, pageSize, UserId: userId, IsAdmin: IsAdmin()), ct));
    }

    [HttpGet("{id:guid}")]
    public async Task<ActionResult<OrderDto>> GetById(Guid id, CancellationToken ct = default)
    {
        var order = await _mediator.Send(new GetOrderByIdQuery(id), ct);
        if (order is null) return NotFound();

        // Ownership check: customers solo ven sus propios pedidos
        if (!IsAdmin() && order.CustomerId.ToString() != GetUserId())
            return Forbid();

        return Ok(order);
    }

    [HttpPost]
    public async Task<ActionResult<OrderDto>> Create(
        [FromBody] CreateOrderRequest request, CancellationToken ct = default)
    {
        // El customerId viene del JWT — no del body del request
        var command = request.ToCommand() with { CustomerId = Guid.Parse(GetUserId()) };
        var result  = await _mediator.Send(command, ct);
        return CreatedAtAction(nameof(GetById), new { id = result.Id }, result);
    }

    [HttpPost("{id:guid}/confirm")]
    [Authorize(Policy = "AdminOnly")]  // Solo admins confirman pedidos
    public async Task<IActionResult> Confirm(Guid id, CancellationToken ct = default)
    {
        await _mediator.Send(new ConfirmOrderCommand(id), ct);
        return NoContent();
    }

    [HttpPost("{id:guid}/cancel")]
    public async Task<IActionResult> Cancel(
        Guid id,
        [FromBody] CancelOrderRequest request,
        CancellationToken ct = default)
    {
        var order = await _mediator.Send(new GetOrderByIdQuery(id), ct);
        if (order is null) return NotFound();

        // Customers solo cancelan sus propios pedidos. Admins cancelan cualquiera.
        if (!IsAdmin() && order.CustomerId.ToString() != GetUserId())
            return Forbid();

        await _mediator.Send(new CancelOrderCommand(id, request.Reason), ct);
        return NoContent();
    }
}
```

---

## Paso 13 — Actualizar ListOrdersQuery con UserId e IsAdmin

Actualizar `src/Orders.API/Application/Queries/ListOrdersQuery.cs`:

```csharp
using MediatR;
using Orders.API.API.DTOs;
using Orders.API.API.DTOs.Responses;

namespace Orders.API.Application.Queries;

/// <summary>
/// Query para listar pedidos.
/// Si IsAdmin = false → solo devuelve pedidos del UserId (ownership).
/// Si IsAdmin = true → devuelve todos los pedidos.
/// </summary>
public record ListOrdersQuery(
    string? Status,
    int     Page,
    int     PageSize,
    Guid?   CustomerId = null,
    string? UserId     = null,
    bool    IsAdmin    = false)
    : IRequest<PagedResult<OrderSummaryDto>>;
```

Actualizar `src/Orders.API/Application/Queries/ListOrdersHandler.cs` — añadir filtro de ownership:

```csharp
public async Task<PagedResult<OrderSummaryDto>> Handle(
    ListOrdersQuery   query,
    CancellationToken ct)
{
    // Ownership: si no es admin, filtrar solo sus pedidos
    var customerIdFilter = query.IsAdmin
        ? query.CustomerId
        : (query.UserId is not null ? Guid.Parse(query.UserId) : query.CustomerId);

    var (orders, total) = await _repository.ListAsync(
        status:     query.Status,
        customerId: customerIdFilter,
        page:       query.Page,
        pageSize:   query.PageSize,
        ct:         ct);

    return new PagedResult<OrderSummaryDto>
    {
        Items      = orders.Select(o => o.ToSummaryDto()).ToList(),
        TotalCount = total,
        Page       = query.Page,
        PageSize   = query.PageSize
    };
}
```

---

## Paso 14 — Actualizar AppHost

Actualizar `infrastructure/OrderFlow.AppHost/appsettings.json`:

```json
{
  "ConnectionStrings": {
    "sqlserver": "Server=(localdb)\\MSSQLLocalDB;Trusted_Connection=true;TrustServerCertificate=true",
    "messaging": "amqp://guest:guest@localhost:5672",
    "identityDb": "Server=(localdb)\\MSSQLLocalDB;Database=IdentityDb;Trusted_Connection=true;TrustServerCertificate=true"
  }
}
```

Actualizar `infrastructure/OrderFlow.AppHost/Program.cs`:

```csharp
var identityDb = sqlServer.AddDatabase("IdentityDb");   // ← nuevo

var gateway = builder.AddProject<Projects.Gateway_API>("gateway-api")
    .WithReference(orders)
    .WithReference(products)
    .WithReference(payments)
    .WithReference(notifications)
    .WithReference(identityDb);                         // ← nuevo
```

---

## Paso 15 — Tests de autenticación

### JwtTestHelper.cs (fichero completo)

Crear `tests/Orders.API.Tests/Shared/JwtTestHelper.cs`:

```csharp
using System.IdentityModel.Tokens.Jwt;
using System.Security.Claims;
using System.Text;
using Microsoft.IdentityModel.Tokens;

namespace Orders.API.Tests.Shared;

/// <summary>
/// Genera JWT de prueba para los integration tests de Orders.API.
/// La clave y configuración deben coincidir con las del Gateway.
/// </summary>
public static class JwtTestHelper
{
    private const string DefaultSigningKey = "orderflow-dev-signing-key-min-32-chars!!";
    private const string DefaultIssuer     = "orderflow-gateway";
    private const string DefaultAudience   = "orderflow";

    public static string GenerateToken(
        string?  userId     = null,
        string[]? roles     = null,
        string   signingKey = DefaultSigningKey,
        string   issuer     = DefaultIssuer,
        string   audience   = DefaultAudience,
        int      expiryMinutes = 60)
    {
        var actualUserId = userId ?? Guid.NewGuid().ToString();
        var actualRoles  = roles ?? ["customer"];

        var claims = new List<Claim>
        {
            new(JwtRegisteredClaimNames.Sub,   actualUserId),
            new(JwtRegisteredClaimNames.Email, "test@techshop.es"),
            new(JwtRegisteredClaimNames.Jti,   Guid.NewGuid().ToString()),
            new("name",                         "Test User"),
        };

        foreach (var role in actualRoles)
            claims.Add(new Claim(ClaimTypes.Role, role));

        var key   = new SymmetricSecurityKey(Encoding.UTF8.GetBytes(signingKey));
        var token = new JwtSecurityToken(
            issuer:             issuer,
            audience:           audience,
            claims:             claims,
            notBefore:          DateTime.UtcNow,
            expires:            DateTime.UtcNow.AddMinutes(expiryMinutes),
            signingCredentials: new SigningCredentials(key, SecurityAlgorithms.HmacSha256));

        return new JwtSecurityTokenHandler().WriteToken(token);
    }

    public static string CustomerToken(string? userId = null)
        => GenerateToken(userId, ["customer"]);

    public static string AdminToken(string? userId = null)
        => GenerateToken(userId, ["admin"]);

    public static string ExpiredToken()
        => GenerateToken(expiryMinutes: -1);
}
```

### AuthenticationTests.cs (fichero completo)

Crear `tests/Orders.API.Tests/Integration/Auth/AuthenticationTests.cs`:

```csharp
using System.Net;
using System.Net.Http.Headers;
using FluentAssertions;
using Orders.API.Tests.Shared;
using Xunit;

namespace Orders.API.Tests.Integration.Auth;

[Trait("Category", "Integration")]
public class AuthenticationTests : IClassFixture<OrdersApiFactory>
{
    private readonly HttpClient _client;

    public AuthenticationTests(OrdersApiFactory factory)
        => _client = factory.CreateClient();

    // ─── Sin token ────────────────────────────────────────────────────────────

    [Fact]
    public async Task GetOrders_WithoutToken_Returns401()
    {
        var response = await _client.GetAsync("/api/orders");
        response.StatusCode.Should().Be(HttpStatusCode.Unauthorized);
    }

    [Fact]
    public async Task CreateOrder_WithoutToken_Returns401()
    {
        var response = await _client.PostAsJsonAsync("/api/orders", new { });
        response.StatusCode.Should().Be(HttpStatusCode.Unauthorized);
    }

    // ─── Token expirado ───────────────────────────────────────────────────────

    [Fact]
    public async Task GetOrders_WithExpiredToken_Returns401()
    {
        _client.DefaultRequestHeaders.Authorization =
            new AuthenticationHeaderValue("Bearer", JwtTestHelper.ExpiredToken());

        var response = await _client.GetAsync("/api/orders");
        response.StatusCode.Should().Be(HttpStatusCode.Unauthorized);
    }

    // ─── Token de customer ────────────────────────────────────────────────────

    [Fact]
    public async Task GetOrders_WithCustomerToken_Returns200()
    {
        _client.DefaultRequestHeaders.Authorization =
            new AuthenticationHeaderValue("Bearer", JwtTestHelper.CustomerToken());

        var response = await _client.GetAsync("/api/orders");
        response.StatusCode.Should().Be(HttpStatusCode.OK);
    }

    // ─── Restricciones de rol ─────────────────────────────────────────────────

    [Fact]
    public async Task ConfirmOrder_WithCustomerToken_Returns403()
    {
        _client.DefaultRequestHeaders.Authorization =
            new AuthenticationHeaderValue("Bearer", JwtTestHelper.CustomerToken());

        var response = await _client.PostAsync(
            $"/api/orders/{Guid.NewGuid()}/confirm", null);

        // 403 Forbidden — customer no puede confirmar pedidos
        response.StatusCode.Should().Be(HttpStatusCode.Forbidden);
    }

    [Fact]
    public async Task GetOrders_WithAdminToken_Returns200()
    {
        _client.DefaultRequestHeaders.Authorization =
            new AuthenticationHeaderValue("Bearer", JwtTestHelper.AdminToken());

        var response = await _client.GetAsync("/api/orders");
        response.StatusCode.Should().Be(HttpStatusCode.OK);
    }
}
```

### Actualizar OrdersApiFactory para soportar JWT

Añadir configuración de JWT en el factory de tests:

```csharp
// En OrdersApiFactory.cs — dentro de ConfigureWebHost
builder.ConfigureTestServices(services =>
{
    // ... configuración existente (InMemory DB, etc.) ...

    // JWT para tests — misma configuración que el servicio real
    services.AddAuthentication(JwtBearerDefaults.AuthenticationScheme)
        .AddJwtBearer(options =>
        {
            options.TokenValidationParameters = new TokenValidationParameters
            {
                ValidateIssuer           = true,
                ValidIssuer              = "orderflow-gateway",
                ValidateAudience         = true,
                ValidAudience            = "orderflow",
                ValidateLifetime         = true,
                IssuerSigningKey         = new SymmetricSecurityKey(
                    System.Text.Encoding.UTF8.GetBytes(
                        "orderflow-dev-signing-key-min-32-chars!!")),
                ClockSkew                = TimeSpan.Zero
            };
        });
});
```

---

## Paso 16 — Compilar, migrar y verificar

```bash
# Compilar
dotnet build OrderFlow.sln

# Tests unitarios (no necesitan auth)
dotnet test --filter "Category=Unit"

# Tests de autenticación
dotnet test --filter "Category=Integration" --verbosity normal
```

### Verificar el flujo con el fichero .http

Actualizar `src/Gateway.API/Gateway.API.http`:

```http
@api = https://localhost:8080

### ─── 1. LOGIN ─────────────────────────────────────────────────────────────────
POST {{api}}/account/login
Content-Type: application/json

{
  "email":    "john@techshop.es",
  "password": "Customer1234!"
}

### → Copiar el access_token de la respuesta en @token

@token = PEGAR_TOKEN_AQUI

### ─── 2. PEDIDO CON TOKEN ────────────────────────────────────────────────────
POST {{api}}/api/orders
Authorization: Bearer {{token}}
Content-Type: application/json

{
  "items": [{ "productId": "{{productId}}", "quantity": 1 }],
  "shippingAddress": { "street": "Gran Vía 28", "city": "Madrid",
    "zipCode": "28013", "country": "ES" }
}
### → 201 Created — el customerId se toma del JWT, no del body

### ─── 3. SIN TOKEN — debe devolver 401 ──────────────────────────────────────
GET {{api}}/api/orders
### → 401 Unauthorized

### ─── 4. LOGIN ADMIN ─────────────────────────────────────────────────────────
POST {{api}}/account/login
Content-Type: application/json

{ "email": "admin@techshop.es", "password": "Admin1234!" }

### ─── 5. CONFIRMAR PEDIDO (solo admin) ──────────────────────────────────────
### Con token customer → 403 Forbidden
### Con token admin    → 204 No Content
POST {{api}}/api/orders/{{orderId}}/confirm
Authorization: Bearer {{token}}

### ─── 6. VERIFICAR JWT EN jwt.io ────────────────────────────────────────────
### Pegar el token en https://jwt.io
### Payload esperado:
### { "sub": "...", "email": "john@techshop.es",
###   "role": "customer", "iss": "orderflow-gateway",
###   "aud": "orderflow", "exp": ... }
```

---

## Paso 17 — Commit y push

```bash
git add .
git commit -m "m5.1: JWT + OAuth 2.0 + ASP.NET Core Identity en Gateway.API

- ASP.NET Core Identity embebido en Gateway.API — sin procesos externos
  GatewayIdentityDbContext + ApplicationUser (FullName, CreatedAt)
  IdentityDb → LocalDB, schema identity
  Migration InitialIdentity + seed: admin@techshop.es + john@techshop.es
  Roles: admin, customer
- JwtService: genera JWT firmados con HS256
  Claims: sub, email, name, roles, jti, exp, iat
  SigningKey en User Secrets (nunca en appsettings.json o repo)
  ExpiryMinutes: 60 (configurable en appsettings.json)
- AccountEndpoints: POST /account/login → valida credenciales → devuelve JWT
  Lockout: 5 intentos fallidos → bloqueo 5 minutos
- Gateway Program.cs actualizado:
  AddIdentity + AddJwtBearer + AddAuthorization (CustomerOrAdmin, AdminOnly)
  Pipeline: CORS → CorrelationId → Serilog → Auth → RateLimiter → YARP
  SeedData.InitializeAsync al arrancar en Development
- YARP appsettings: AuthorizationPolicy por ruta
  orders-create-route → CustomerOrAdmin (POST /api/orders)
  orders-read-route   → CustomerOrAdmin (GET /api/orders/...)
  payments-route      → AdminOnly
  notifications-route → AdminOnly
  products-route      → público
- Orders.API:
  AddJwtBearer con la misma SigningKey (User Secrets)
  [Authorize(Policy='CustomerOrAdmin')] en todo el controller
  [Authorize(Policy='AdminOnly')] en POST /confirm
  Ownership check: GetById y Cancel verifican customerId == sub del JWT
  customerId en Create viene del JWT (sub), no del body del request
  ListOrdersQuery: añadir UserId + IsAdmin para filtrado de ownership
- AppHost: añadir identityDb connection string + referencia en gateway
- Tests:
  JwtTestHelper: genera tokens customer, admin y expirado
  AuthenticationTests (4 tests): 401 sin token, 401 token expirado,
    200 con customer token, 403 customer en endpoint admin"

git push origin m5.1
```

---

## Checklist final

- [ ] `dotnet build OrderFlow.sln` → sin errores
- [ ] Migration de Identity aplicada (tablas `AspNet*` en `IdentityDb`)
- [ ] Seed ejecutado: `admin@techshop.es` y `john@techshop.es` en BD
- [ ] `POST /account/login` (john) → `{ "access_token": "eyJ..." }`
- [ ] `GET /api/orders` sin token → 401
- [ ] `GET /api/orders` con token customer → 200 (solo sus pedidos)
- [ ] `POST /api/orders/{id}/confirm` con token customer → 403
- [ ] `POST /api/orders/{id}/confirm` con token admin → 204
- [ ] Token verificable en https://jwt.io con payload correcto
- [ ] `dotnet test --filter "Category=Integration"` → 4 tests auth pasan
- [ ] Commit en rama `m5.1` y push completados

---

## El modelo de seguridad completo

```
Internet
  │ HTTPS
  ▼
Gateway.API (:8080)
  ├── POST /account/login
  │     ASP.NET Core Identity valida email+password en IdentityDb
  │     JwtService genera JWT firmado (HS256, 60 min)
  │     ← { access_token, token_type, expires_in }
  │
  ├── POST /api/orders → AuthorizationPolicy: CustomerOrAdmin
  │     Gateway valida JWT (firma + exp + iss + aud)
  │     YARP hace forward con Authorization header intacto
  │     Orders.API extrae sub del JWT → CustomerId del pedido
  │
  ├── GET /api/orders → AuthorizationPolicy: CustomerOrAdmin
  │     Orders.API filtra: customer → solo suyos, admin → todos
  │
  └── GET /api/products → sin AuthorizationPolicy (público)

IdentityDb (LocalDB)
  └── AspNetUsers, AspNetRoles, AspNetUserRoles, AspNetUserTokens
```

## User Secrets — la regla de oro

```
Servicio        User Secret
──────────────  ──────────────────────────────────────────────
Gateway.API     Jwt:SigningKey = "orderflow-dev-signing-key-..."
Orders.API      Jwt:SigningKey = "orderflow-dev-signing-key-..."  ← IDÉNTICA
Products.API    (no valida JWT en M5.1 — público)
```

En producción (M5.2): la clave se mueve a Azure Key Vault y todos los servicios la leen desde allí con Managed Identity.

## Lo que viene en M5.2

`docs/m5.2.md` → HTTPS, mTLS entre servicios, y `Jwt:SigningKey` a Azure Key Vault. Ningún secreto en el código ni en variables de entorno planas.
