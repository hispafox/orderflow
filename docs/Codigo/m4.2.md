# M4.2 — Comunicación Asíncrona: Mensajería y Eventos

## Branch

```bash
git checkout m4.1
git checkout -b m4.2
```

## Prerrequisito

RabbitMQ debe estar instalado y corriendo nativamente en Windows. Verificar:

```bash
rabbitmqctl status
# → Status of node rabbit@localhost ... running

# Panel web de gestión
# http://localhost:15672  (guest/guest)
```

Si no está arrancado: `rabbitmq-service start` o desde Servicios de Windows.

## Objetivo

Añadir la dimensión asíncrona a OrderFlow. Orders.API publica el Integration Event `OrderCreated` a RabbitMQ. Notifications.API (nuevo microservicio) lo consume y simula el envío de email. `FakeEventPublisher` se elimina — MassTransit es el mecanismo real desde ahora.

## Qué se construye en este módulo

```
shared/OrderFlow.Contracts/              ← proyecto nuevo (classlib)
  └── Events/
      ├── Orders/   OrderCreated.cs, OrderConfirmed.cs, OrderCancelled.cs
      ├── Products/ StockReserved.cs, StockInsufficient.cs, StockReleased.cs
      └── Payments/ PaymentProcessed.cs, PaymentFailed.cs

src/Notifications.API/                   ← microservicio nuevo (web)
  ├── Consumers/    OrderCreatedConsumer.cs
  ├── Domain/       Notification.cs
  ├── Infrastructure/ NotificationDbContext.cs
  ├── Services/     IEmailService.cs, FakeEmailService.cs
  └── Program.cs

src/Orders.API/                          ← cambios en existente
  ├── Application/Commands/
  │   ├── CreateOrderCommand.cs          ← añadir CustomerEmail
  │   └── CreateOrderHandler.cs         ← reemplazar IEventPublisher → IPublishEndpoint
  ├── Infrastructure/Events/
  │   └── FakeEventPublisher.cs         ← eliminar
  └── Program.cs                        ← añadir MassTransit

infrastructure/OrderFlow.AppHost/        ← cambios en existente
  ├── Program.cs                        ← añadir NotificationsDb + notifications-api
  └── appsettings.json                  ← añadir "messaging" connection string

tests/Orders.API.Tests/Unit/Application/
  └── CreateOrderHandlerPublishTests.cs  ← verificar publicación (nuevo)

tests/Notifications.API.Tests/           ← proyecto nuevo
  └── Consumers/OrderCreatedConsumerTests.cs
```

---

## Paso 1 — Crear OrderFlow.Contracts

```bash
# Crear el proyecto compartido en shared/
mkdir -p shared
dotnet new classlib -n OrderFlow.Contracts -f net10.0 -o shared/OrderFlow.Contracts

# Añadir a la solución
dotnet sln add shared/OrderFlow.Contracts/OrderFlow.Contracts.csproj
```

Borrar el fichero `Class1.cs` generado por defecto.

### OrderCreated.cs (fichero completo)

Crear `shared/OrderFlow.Contracts/Events/Orders/OrderCreated.cs`:

```csharp
namespace OrderFlow.Contracts.Events.Orders;

/// <summary>
/// Integration Event publicado por Orders.API cuando se crea un pedido.
/// Consumers en M4.2: Notifications.API (email de confirmación)
/// Consumers en M4.3: Products.API (reserva de stock via Saga)
/// </summary>
public record OrderCreated
{
    public Guid     OrderId       { get; init; }
    public Guid     CustomerId    { get; init; }
    public string   CustomerEmail { get; init; } = string.Empty;
    public decimal  Total         { get; init; }
    public string   Currency      { get; init; } = "EUR";
    public DateTime CreatedAt     { get; init; }
    public IList<OrderCreatedItem>    Items           { get; init; } = [];
    public OrderCreatedAddress        ShippingAddress { get; init; } = null!;
}

public record OrderCreatedItem(
    Guid    ProductId,
    string  ProductName,
    int     Quantity,
    decimal UnitPrice);

public record OrderCreatedAddress(
    string Street,
    string City,
    string ZipCode,
    string Country);
```

### OrderConfirmed.cs y OrderCancelled.cs

Crear `shared/OrderFlow.Contracts/Events/Orders/OrderConfirmed.cs`:

```csharp
namespace OrderFlow.Contracts.Events.Orders;

public record OrderConfirmed
{
    public Guid     OrderId     { get; init; }
    public Guid     CustomerId  { get; init; }
    public string   CustomerEmail { get; init; } = string.Empty;
    public DateTime ConfirmedAt { get; init; }
}
```

Crear `shared/OrderFlow.Contracts/Events/Orders/OrderCancelled.cs`:

```csharp
namespace OrderFlow.Contracts.Events.Orders;

public record OrderCancelled
{
    public Guid     OrderId       { get; init; }
    public Guid     CustomerId    { get; init; }
    public string   CustomerEmail { get; init; } = string.Empty;
    public string   Reason        { get; init; } = string.Empty;
    public DateTime CancelledAt   { get; init; }
}
```

### Events de Products y Payments (para M4.3 — esqueletos ahora)

Crear `shared/OrderFlow.Contracts/Events/Products/StockReserved.cs`:

```csharp
namespace OrderFlow.Contracts.Events.Products;

public record StockReserved
{
    public Guid OrderId   { get; init; }
    public Guid ProductId { get; init; }
    public int  Quantity  { get; init; }
}

public record StockInsufficient
{
    public Guid   OrderId   { get; init; }
    public Guid   ProductId { get; init; }
    public int    Requested { get; init; }
    public int    Available { get; init; }
}

public record StockReleased
{
    public Guid OrderId   { get; init; }
    public Guid ProductId { get; init; }
    public int  Quantity  { get; init; }
}
```

Crear `shared/OrderFlow.Contracts/Events/Payments/PaymentProcessed.cs`:

```csharp
namespace OrderFlow.Contracts.Events.Payments;

public record PaymentProcessed
{
    public Guid     OrderId   { get; init; }
    public Guid     PaymentId { get; init; }
    public decimal  Amount    { get; init; }
    public string   Currency  { get; init; } = "EUR";
    public DateTime ProcessedAt { get; init; }
}

public record PaymentFailed
{
    public Guid   OrderId { get; init; }
    public string Reason  { get; init; } = string.Empty;
}
```

---

## Paso 2 — Instalar MassTransit en Orders.API

```bash
dotnet add src/Orders.API/Orders.API.csproj package MassTransit
dotnet add src/Orders.API/Orders.API.csproj package MassTransit.RabbitMQ
dotnet add src/Orders.API/Orders.API.csproj reference shared/OrderFlow.Contracts/OrderFlow.Contracts.csproj
```

---

## Paso 3 — Actualizar CreateOrderCommand con CustomerEmail

Actualizar `src/Orders.API/Application/Commands/CreateOrderCommand.cs`:

```csharp
using MediatR;
using Orders.API.API.DTOs.Responses;
using Orders.API.Application.Behaviors;

namespace Orders.API.Application.Commands;

public record CreateOrderCommand(
    Guid                              CustomerId,
    string                            CustomerEmail,      // ← NUEVO en M4.2
    IReadOnlyList<CreateOrderItemDto> Items,
    CreateOrderAddressDto             ShippingAddress)
    : IRequest<OrderDto>, ITransactional;

public record CreateOrderItemDto(
    Guid    ProductId,
    string  ProductName,
    int     Quantity,
    decimal UnitPrice,
    string  Currency);

public record CreateOrderAddressDto(
    string Street,
    string City,
    string ZipCode,
    string Country);
```

Actualizar el validator `CreateOrderCommandValidator.cs` — añadir regla para `CustomerEmail`:

```csharp
    RuleFor(x => x.CustomerEmail)
        .NotEmpty().WithMessage("CustomerEmail is required")
        .EmailAddress().WithMessage("CustomerEmail must be a valid email address");
```

Actualizar el mapping `OrderMappings.cs` — añadir `CustomerEmail` en `ToCommand()`:

```csharp
public static CreateOrderCommand ToCommand(this CreateOrderRequest request) =>
    new(
        request.CustomerId,
        request.CustomerEmail,        // ← añadir
        request.Items.Select(i =>
            new CreateOrderItemDto(i.ProductId, i.ProductName,
                i.Quantity, i.UnitPrice, i.Currency)).ToList(),
        new CreateOrderAddressDto(
            request.ShippingAddress.Street, request.ShippingAddress.City,
            request.ShippingAddress.ZipCode, request.ShippingAddress.Country));
```

Actualizar el DTO de request `CreateOrderRequest.cs` — añadir `CustomerEmail`:

```csharp
public record CreateOrderRequest
{
    [Required]
    public Guid CustomerId { get; init; }

    [Required, EmailAddress]
    public string CustomerEmail { get; init; } = string.Empty;  // ← añadir

    [Required, MinLength(1)]
    public IReadOnlyList<CreateOrderItemRequestDto> Items { get; init; } = [];

    [Required]
    public ShippingAddressRequestDto ShippingAddress { get; init; } = null!;
}
```

---

## Paso 4 — Actualizar CreateOrderHandler — reemplazar IEventPublisher por IPublishEndpoint

Reemplazar `src/Orders.API/Application/Commands/CreateOrderHandler.cs`:

```csharp
using MassTransit;
using MediatR;
using OrderFlow.Contracts.Events.Orders;
using Orders.API.API.DTOs.Responses;
using Orders.API.API.Mappings;
using Orders.API.Domain.Entities;
using Orders.API.Domain.Exceptions;
using Orders.API.Domain.Interfaces;
using Orders.API.Domain.ValueObjects;
using Orders.API.Infrastructure.Http;

namespace Orders.API.Application.Commands;

/// <summary>
/// Handler que crea un pedido y publica el Integration Event OrderCreated a RabbitMQ.
/// IEventPublisher (stub) reemplazado por IPublishEndpoint (MassTransit real).
/// </summary>
public class CreateOrderHandler : IRequestHandler<CreateOrderCommand, OrderDto>
{
    private readonly IOrderRepository  _repository;
    private readonly ProductsClient    _productsClient;
    private readonly IPublishEndpoint  _publishEndpoint;
    private readonly ILogger<CreateOrderHandler> _logger;

    public CreateOrderHandler(
        IOrderRepository            repository,
        ProductsClient              productsClient,
        IPublishEndpoint            publishEndpoint,
        ILogger<CreateOrderHandler> logger)
    {
        _repository      = repository;
        _productsClient  = productsClient;
        _publishEndpoint = publishEndpoint;
        _logger          = logger;
    }

    public async Task<OrderDto> Handle(
        CreateOrderCommand command,
        CancellationToken  ct)
    {
        _logger.LogInformation(
            "Creating order for customer {CustomerId} ({Email})",
            command.CustomerId, command.CustomerEmail);

        // 1. Verificar precios y stock en Products.API
        var validatedItems = new List<(Guid, string, int, Money)>();
        foreach (var item in command.Items)
        {
            var product = await _productsClient.GetProductAsync(item.ProductId, ct);

            if (product is null)
                throw new DomainException(
                    $"Product {item.ProductId} is not available or could not be verified");

            if (!product.IsActive)
                throw new DomainException($"Product '{product.Name}' is no longer active");

            if (product.Stock < item.Quantity)
                throw new DomainException(
                    $"Insufficient stock for '{product.Name}'. " +
                    $"Available: {product.Stock}, Requested: {item.Quantity}");

            validatedItems.Add((
                item.ProductId,
                product.Name,
                item.Quantity,
                new Money(product.Price, product.Currency)));
        }

        // 2. Crear el aggregate
        var address = new Address(
            command.ShippingAddress.Street, command.ShippingAddress.City,
            command.ShippingAddress.ZipCode, command.ShippingAddress.Country);

        var order = Order.Create(command.CustomerId, validatedItems, address);

        // 3. Persistir (el TransactionBehavior abre la transacción)
        await _repository.SaveAsync(order, ct);

        // 4. Publicar Integration Event a RabbitMQ vía MassTransit
        var orderCreated = new OrderCreated
        {
            OrderId       = order.Id,
            CustomerId    = order.CustomerId,
            CustomerEmail = command.CustomerEmail,
            Total         = order.Total.Amount,
            Currency      = order.Total.Currency,
            CreatedAt     = order.CreatedAt,
            Items = order.Lines.Select(l => new OrderCreatedItem(
                l.ProductId, l.ProductName,
                l.Quantity, l.UnitPrice.Amount)).ToList(),
            ShippingAddress = new OrderCreatedAddress(
                order.ShippingAddress.Street, order.ShippingAddress.City,
                order.ShippingAddress.ZipCode, order.ShippingAddress.Country)
        };

        await _publishEndpoint.Publish(orderCreated, ct);

        _logger.LogInformation(
            "Order {OrderId} created and OrderCreated event published",
            order.Id);

        order.ClearDomainEvents();
        return order.ToDto();
    }
}
```

---

## Paso 5 — Eliminar FakeEventPublisher e IEventPublisher de Orders.API

```bash
# Eliminar los ficheros que ya no son necesarios
# src/Orders.API/Infrastructure/Events/FakeEventPublisher.cs
# src/Orders.API/Application/Interfaces/IEventPublisher.cs
```

Eliminar también el registro en `ServiceCollectionExtensions.cs`:

```csharp
// Eliminar estas líneas:
// services.AddScoped<IEventPublisher, FakeEventPublisher>();
```

---

## Paso 6 — Configurar MassTransit en Orders.API Program.cs

Añadir al `src/Orders.API/Program.cs` (después del bloque de FluentValidation/MediatR):

```csharp
using MassTransit;

// ─── MassTransit + RabbitMQ ───────────────────────────────────────────────────
builder.Services.AddMassTransit(x =>
{
    // Orders.API solo PUBLICA — sin consumers propios en M4.2
    // Los consumers de Orders llegan en M4.3 (Saga)
    x.UsingRabbitMq((context, cfg) =>
    {
        cfg.Host(
            builder.Configuration.GetConnectionString("messaging")!,
            h =>
            {
                h.Username("guest");
                h.Password("guest");
            });

        // Reintentos exponenciales para todos los producers
        cfg.UseMessageRetry(r => r.Exponential(
            retryLimit:    5,
            minInterval:   TimeSpan.FromSeconds(1),
            maxInterval:   TimeSpan.FromSeconds(30),
            intervalDelta: TimeSpan.FromSeconds(2)));

        cfg.ConfigureEndpoints(context);
    });
});
```

Añadir métricas de MassTransit en el bloque de OpenTelemetry:

```csharp
builder.Services.AddOpenTelemetry()
    .WithMetrics(m => m
        .AddMeter("MassTransit")              // ← añadir
        .AddHttpClientInstrumentation()
        .AddRuntimeInstrumentation());
```

---

## Paso 7 — Crear Notifications.API

```bash
dotnet new web -n Notifications.API -f net10.0 -o src/Notifications.API
dotnet sln add src/Notifications.API/Notifications.API.csproj

# Paquetes
dotnet add src/Notifications.API/Notifications.API.csproj package MassTransit
dotnet add src/Notifications.API/Notifications.API.csproj package MassTransit.RabbitMQ
dotnet add src/Notifications.API/Notifications.API.csproj package Microsoft.EntityFrameworkCore.SqlServer
dotnet add src/Notifications.API/Notifications.API.csproj package Microsoft.EntityFrameworkCore.Design
dotnet add src/Notifications.API/Notifications.API.csproj package Serilog.AspNetCore
dotnet add src/Notifications.API/Notifications.API.csproj package AspNetCore.HealthChecks.Rabbitmq

# Referencia a contratos compartidos
dotnet add src/Notifications.API/Notifications.API.csproj reference shared/OrderFlow.Contracts/OrderFlow.Contracts.csproj

# Referencia a ServiceDefaults
dotnet add src/Notifications.API/Notifications.API.csproj reference infrastructure/OrderFlow.ServiceDefaults/OrderFlow.ServiceDefaults.csproj
```

### Notification.cs — la entidad de idempotencia

Crear `src/Notifications.API/Domain/Notification.cs`:

```csharp
namespace Notifications.API.Domain;

/// <summary>
/// Registro de cada notificación enviada.
/// El índice único en OrderId + Type garantiza idempotencia:
/// si el mismo mensaje llega dos veces, lo detectamos.
/// </summary>
public class Notification
{
    public Guid     Id             { get; private set; } = Guid.NewGuid();
    public Guid     OrderId        { get; private set; }
    public string   Type           { get; private set; } = string.Empty;
    public string   RecipientEmail { get; private set; } = string.Empty;
    public DateTime ProcessedAt    { get; private set; } = DateTime.UtcNow;
    public bool     Success        { get; private set; }

    // Para EF Core
    private Notification() { }

    public static Notification Create(
        Guid   orderId,
        string type,
        string email,
        bool   success) => new()
    {
        OrderId        = orderId,
        Type           = type,
        RecipientEmail = email,
        Success        = success
    };
}
```

### NotificationDbContext.cs (fichero completo)

Crear `src/Notifications.API/Infrastructure/NotificationDbContext.cs`:

```csharp
using Microsoft.EntityFrameworkCore;
using Notifications.API.Domain;

namespace Notifications.API.Infrastructure;

public class NotificationDbContext : DbContext
{
    public NotificationDbContext(DbContextOptions<NotificationDbContext> options)
        : base(options) { }

    public DbSet<Notification> Notifications => Set<Notification>();

    protected override void OnModelCreating(ModelBuilder modelBuilder)
    {
        modelBuilder.HasDefaultSchema("notifications");

        modelBuilder.Entity<Notification>(entity =>
        {
            entity.ToTable("Notifications");
            entity.HasKey(n => n.Id);

            entity.HasIndex(n => new { n.OrderId, n.Type })
                  .IsUnique()
                  .HasDatabaseName("IX_Notifications_OrderId_Type");

            entity.HasIndex(n => n.ProcessedAt)
                  .HasDatabaseName("IX_Notifications_ProcessedAt");

            entity.Property(n => n.Type).HasMaxLength(100);
            entity.Property(n => n.RecipientEmail).HasMaxLength(320);
        });
    }
}
```

### IEmailService.cs (fichero completo)

Crear `src/Notifications.API/Services/IEmailService.cs`:

```csharp
using OrderFlow.Contracts.Events.Orders;

namespace Notifications.API.Services;

public interface IEmailService
{
    Task SendOrderConfirmationAsync(
        string               email,
        Guid                 orderId,
        decimal              total,
        string               currency,
        IList<OrderCreatedItem> items,
        CancellationToken    ct = default);
}
```

### FakeEmailService.cs (fichero completo)

Crear `src/Notifications.API/Services/FakeEmailService.cs`:

```csharp
using OrderFlow.Contracts.Events.Orders;

namespace Notifications.API.Services;

/// <summary>
/// Implementación fake del email service para el curso.
/// Loggea el email que se enviaría en producción.
/// En producción: reemplazar por SendGrid, Amazon SES o SMTP.
/// La interfaz IEmailService no cambia.
/// </summary>
public class FakeEmailService : IEmailService
{
    private readonly ILogger<FakeEmailService> _logger;

    public FakeEmailService(ILogger<FakeEmailService> logger)
        => _logger = logger;

    public Task SendOrderConfirmationAsync(
        string               email,
        Guid                 orderId,
        decimal              total,
        string               currency,
        IList<OrderCreatedItem> items,
        CancellationToken    ct = default)
    {
        _logger.LogInformation(
            "[EMAIL SIMULADO] Para: {Email} | Pedido: {OrderId} | " +
            "Total: {Total} {Currency} | Artículos: {ItemCount}",
            email, orderId, total, currency, items.Count);

        foreach (var item in items)
            _logger.LogInformation(
                "[EMAIL] - {Qty}x {Name} a {Price} {Currency}",
                item.Quantity, item.ProductName, item.UnitPrice, currency);

        return Task.CompletedTask;
    }
}
```

### OrderCreatedConsumer.cs (fichero completo)

Crear `src/Notifications.API/Consumers/OrderCreatedConsumer.cs`:

```csharp
using MassTransit;
using Microsoft.EntityFrameworkCore;
using Notifications.API.Domain;
using Notifications.API.Infrastructure;
using Notifications.API.Services;
using OrderFlow.Contracts.Events.Orders;

namespace Notifications.API.Consumers;

/// <summary>
/// Consumer de OrderCreated. Envía email de confirmación al cliente.
/// Idempotencia básica: verifica si ya procesamos este OrderId + Type.
/// Si el mismo mensaje llega dos veces (at-least-once), el segundo se ignora.
/// </summary>
public class OrderCreatedConsumer : IConsumer<OrderCreated>
{
    private readonly IEmailService      _emailService;
    private readonly NotificationDbContext _dbContext;
    private readonly ILogger<OrderCreatedConsumer> _logger;

    public OrderCreatedConsumer(
        IEmailService      emailService,
        NotificationDbContext dbContext,
        ILogger<OrderCreatedConsumer> logger)
    {
        _emailService = emailService;
        _dbContext    = dbContext;
        _logger       = logger;
    }

    public async Task Consume(ConsumeContext<OrderCreated> context)
    {
        var message = context.Message;

        _logger.LogInformation(
            "Processing OrderCreated for order {OrderId}. " +
            "Customer: {Email}. Total: {Total} {Currency}",
            message.OrderId, message.CustomerEmail,
            message.Total, message.Currency);

        // Idempotencia: ¿ya procesamos esta notificación?
        var alreadyProcessed = await _dbContext.Notifications.AnyAsync(
            n => n.OrderId == message.OrderId && n.Type == "OrderConfirmation",
            context.CancellationToken);

        if (alreadyProcessed)
        {
            _logger.LogWarning(
                "Duplicate OrderCreated for {OrderId} — skipping",
                message.OrderId);
            return; // ACK sin reprocesar
        }

        // Enviar notificación
        await _emailService.SendOrderConfirmationAsync(
            message.CustomerEmail,
            message.OrderId,
            message.Total,
            message.Currency,
            message.Items,
            context.CancellationToken);

        // Registrar como procesado (garantía de idempotencia)
        _dbContext.Notifications.Add(Notification.Create(
            message.OrderId,
            "OrderConfirmation",
            message.CustomerEmail,
            success: true));

        await _dbContext.SaveChangesAsync(context.CancellationToken);

        _logger.LogInformation(
            "Order confirmation notification sent and recorded for order {OrderId}",
            message.OrderId);
    }
}
```

### Program.cs de Notifications.API (fichero completo)

Crear `src/Notifications.API/Program.cs`:

```csharp
using MassTransit;
using Microsoft.AspNetCore.Diagnostics.HealthChecks;
using Microsoft.EntityFrameworkCore;
using Notifications.API.Consumers;
using Notifications.API.Infrastructure;
using Notifications.API.Services;
using Serilog;

var builder = WebApplication.CreateBuilder(args);

// ─── ServiceDefaults (OpenTelemetry, health checks base) ─────────────────────
builder.AddServiceDefaults();

// ─── Serilog ─────────────────────────────────────────────────────────────────
builder.Host.UseSerilog((ctx, lc) => lc
    .ReadFrom.Configuration(ctx.Configuration)
    .Enrich.FromLogContext()
    .Enrich.WithMachineName());

// ─── NotificationDbContext ────────────────────────────────────────────────────
builder.Services.AddDbContext<NotificationDbContext>(options =>
    options.UseSqlServer(
        builder.Configuration.GetConnectionString("NotificationsDb"),
        sql => sql.MigrationsHistoryTable("__EFMigrationsHistory", "notifications")));

// ─── Email service ────────────────────────────────────────────────────────────
builder.Services.AddScoped<IEmailService, FakeEmailService>();

// ─── MassTransit + RabbitMQ ───────────────────────────────────────────────────
builder.Services.AddMassTransit(x =>
{
    // Registrar todos los consumers del assembly automáticamente
    x.AddConsumers(typeof(Program).Assembly);

    x.UsingRabbitMq((context, cfg) =>
    {
        cfg.Host(
            builder.Configuration.GetConnectionString("messaging")!,
            h =>
            {
                h.Username("guest");
                h.Password("guest");
            });

        // Reintentos exponenciales para fallos transitorios (SMTP caído, etc.)
        cfg.UseMessageRetry(r => r.Exponential(
            retryLimit:    5,
            minInterval:   TimeSpan.FromSeconds(1),
            maxInterval:   TimeSpan.FromSeconds(30),
            intervalDelta: TimeSpan.FromSeconds(2)));

        // MassTransit configura automáticamente queues y bindings para cada consumer
        cfg.ConfigureEndpoints(context);
    });
});

// ─── OpenTelemetry con métricas de MassTransit ───────────────────────────────
builder.Services.AddOpenTelemetry()
    .WithMetrics(m => m.AddMeter("MassTransit"));

// ─── Health checks ────────────────────────────────────────────────────────────
builder.Services.AddHealthChecks()
    .AddDbContextCheck<NotificationDbContext>(
        name: "notifications-db",
        failureStatus: Microsoft.Extensions.Diagnostics.HealthChecks.HealthStatus.Degraded)
    .AddRabbitMQ(
        rabbitConnectionString: builder.Configuration.GetConnectionString("messaging")!,
        name: "rabbitmq",
        failureStatus: Microsoft.Extensions.Diagnostics.HealthChecks.HealthStatus.Degraded);

// ─── Build ────────────────────────────────────────────────────────────────────
var app = builder.Build();

app.UseSerilogRequestLogging();
app.MapDefaultEndpoints();

// Notifications.API es un worker — no expone endpoints REST propios
// Los health checks son suficientes para Aspire
app.Run();
```

### appsettings.json de Notifications.API

Crear `src/Notifications.API/appsettings.json`:

```json
{
  "Serilog": {
    "MinimumLevel": {
      "Default": "Information",
      "Override": {
        "Microsoft": "Warning",
        "Microsoft.EntityFrameworkCore.Database.Command": "Warning"
      }
    }
  }
}
```

Crear `src/Notifications.API/appsettings.Development.json`:

```json
{
  "Serilog": {
    "MinimumLevel": {
      "Default": "Debug",
      "Override": {
        "Microsoft.EntityFrameworkCore.Database.Command": "Information"
      }
    }
  },
  "ConnectionStrings": {
    "NotificationsDb": "Server=(localdb)\\MSSQLLocalDB;Database=NotificationsDb;Trusted_Connection=true;TrustServerCertificate=true",
    "messaging": "amqp://guest:guest@localhost:5672"
  }
}
```

---

## Paso 8 — Migration de Notifications.API

```bash
dotnet ef migrations add InitialNotificationSchema \
    --project src/Notifications.API \
    --startup-project src/Notifications.API \
    --context NotificationDbContext

dotnet ef database update \
    --project src/Notifications.API \
    --context NotificationDbContext
```

---

## Paso 9 — Actualizar el AppHost

Actualizar `infrastructure/OrderFlow.AppHost/appsettings.json` — añadir la connection string de mensajería:

```json
{
  "ConnectionStrings": {
    "sqlserver": "Server=(localdb)\\MSSQLLocalDB;Trusted_Connection=true;TrustServerCertificate=true",
    "messaging": "amqp://guest:guest@localhost:5672"
  }
}
```

Actualizar `infrastructure/OrderFlow.AppHost/Program.cs`:

```csharp
var builder = DistributedApplication.CreateBuilder(args);

var sqlServer = builder.AddConnectionString("sqlserver");
var messaging = builder.AddConnectionString("messaging");   // ← RabbitMQ nativo

var ordersDb        = sqlServer.AddDatabase("OrdersDb");
var productsDb      = sqlServer.AddDatabase("ProductsDb");
var notificationsDb = sqlServer.AddDatabase("NotificationsDb");  // ← NUEVO

var products = builder.AddProject<Projects.Products_API>("products-api")
    .WithReference(productsDb)
    .WithReference(messaging);

var notifications = builder                                       // ← NUEVO
    .AddProject<Projects.Notifications_API>("notifications-api")
    .WithReference(notificationsDb)
    .WithReference(messaging);

var orders = builder.AddProject<Projects.Orders_API>("orders-api")
    .WithReference(ordersDb)
    .WithReference(messaging)
    .WithReference(products);

builder.Build().Run();
```

---

## Paso 10 — Tests de mensajería

Crear el proyecto de tests:

```bash
dotnet new xunit -n Notifications.API.Tests -f net10.0 -o tests/Notifications.API.Tests
dotnet sln add tests/Notifications.API.Tests/Notifications.API.Tests.csproj

dotnet add tests/Notifications.API.Tests/Notifications.API.Tests.csproj package MassTransit.Testing
dotnet add tests/Notifications.API.Tests/Notifications.API.Tests.csproj package NSubstitute
dotnet add tests/Notifications.API.Tests/Notifications.API.Tests.csproj package FluentAssertions
dotnet add tests/Notifications.API.Tests/Notifications.API.Tests.csproj package Microsoft.EntityFrameworkCore.InMemory
dotnet add tests/Notifications.API.Tests/Notifications.API.Tests.csproj reference shared/OrderFlow.Contracts/OrderFlow.Contracts.csproj
dotnet add tests/Notifications.API.Tests/Notifications.API.Tests.csproj reference src/Notifications.API/Notifications.API.csproj
```

### OrderCreatedConsumerTests.cs (fichero completo)

Crear `tests/Notifications.API.Tests/Consumers/OrderCreatedConsumerTests.cs`:

```csharp
using FluentAssertions;
using MassTransit;
using MassTransit.Testing;
using Microsoft.EntityFrameworkCore;
using Microsoft.Extensions.DependencyInjection;
using Microsoft.Extensions.Logging.Abstractions;
using Notifications.API.Consumers;
using Notifications.API.Infrastructure;
using Notifications.API.Services;
using NSubstitute;
using OrderFlow.Contracts.Events.Orders;
using Xunit;

namespace Notifications.API.Tests.Consumers;

[Trait("Category", "Unit")]
public class OrderCreatedConsumerTests
{
    private static OrderCreated BuildMessage(Guid? orderId = null) => new()
    {
        OrderId       = orderId ?? Guid.NewGuid(),
        CustomerId    = Guid.NewGuid(),
        CustomerEmail = "john@techshop.es",
        Total         = 3999.99m,
        Currency      = "EUR",
        CreatedAt     = DateTime.UtcNow,
        Items         = [new(Guid.NewGuid(), "MacBook Pro", 1, 3999.99m)],
        ShippingAddress = new("Gran Vía 28", "Madrid", "28013", "ES")
    };

    private static NotificationDbContext BuildInMemoryDb()
    {
        var options = new DbContextOptionsBuilder<NotificationDbContext>()
            .UseInMemoryDatabase($"TestNotificationsDb_{Guid.NewGuid()}")
            .Options;
        return new NotificationDbContext(options);
    }

    [Fact]
    public async Task Consume_ValidOrderCreated_SendsConfirmationEmail()
    {
        // Arrange
        var emailService = Substitute.For<IEmailService>();
        var dbContext    = BuildInMemoryDb();
        var consumer     = new OrderCreatedConsumer(
            emailService, dbContext, NullLogger<OrderCreatedConsumer>.Instance);

        await using var harness = new InMemoryTestHarness();
        var consumerHarness = harness.Consumer(() => consumer);

        await harness.Start();
        var message = BuildMessage();

        // Act
        await harness.InputQueueSendEndpoint.Send(message);

        // Assert — consumer procesó el mensaje
        (await harness.Consumed.Any<OrderCreated>()).Should().BeTrue();
        await emailService.Received(1).SendOrderConfirmationAsync(
            Arg.Is<string>(e => e == "john@techshop.es"),
            Arg.Is<Guid>(id => id == message.OrderId),
            Arg.Any<decimal>(),
            Arg.Any<string>(),
            Arg.Any<IList<OrderCreatedItem>>(),
            Arg.Any<CancellationToken>());

        // Verificar que se guardó en la BD
        var notification = await dbContext.Notifications
            .FirstOrDefaultAsync(n => n.OrderId == message.OrderId);
        notification.Should().NotBeNull();
        notification!.Type.Should().Be("OrderConfirmation");

        await harness.Stop();
    }

    [Fact]
    public async Task Consume_DuplicateOrderCreated_SkipsProcessing()
    {
        // Arrange — pre-registrar la notificación como ya procesada
        var emailService = Substitute.For<IEmailService>();
        var dbContext    = BuildInMemoryDb();
        var orderId      = Guid.NewGuid();

        dbContext.Notifications.Add(
            Notifications.API.Domain.Notification.Create(
                orderId, "OrderConfirmation", "john@techshop.es", true));
        await dbContext.SaveChangesAsync();

        var consumer = new OrderCreatedConsumer(
            emailService, dbContext, NullLogger<OrderCreatedConsumer>.Instance);

        await using var harness = new InMemoryTestHarness();
        harness.Consumer(() => consumer);
        await harness.Start();

        // Act — enviar el mismo OrderId que ya está registrado
        await harness.InputQueueSendEndpoint.Send(BuildMessage(orderId));

        (await harness.Consumed.Any<OrderCreated>()).Should().BeTrue();

        // Assert — email NO enviado (idempotencia)
        await emailService.DidNotReceive().SendOrderConfirmationAsync(
            Arg.Any<string>(), Arg.Any<Guid>(), Arg.Any<decimal>(),
            Arg.Any<string>(), Arg.Any<IList<OrderCreatedItem>>(),
            Arg.Any<CancellationToken>());

        await harness.Stop();
    }
}
```

### CreateOrderHandlerPublishTests.cs (fichero completo)

Añadir a `tests/Orders.API.Tests/Unit/Application/CreateOrderHandlerPublishTests.cs`:

```csharp
using FluentAssertions;
using MassTransit;
using Microsoft.Extensions.Logging.Abstractions;
using NSubstitute;
using OrderFlow.Contracts.Events.Orders;
using Orders.API.Application.Commands;
using Orders.API.Domain.Interfaces;
using Orders.API.Infrastructure.Http;
using Xunit;

namespace Orders.API.Tests.Unit.Application;

[Trait("Category", "Unit")]
public class CreateOrderHandlerPublishTests
{
    private readonly IOrderRepository _repository   = Substitute.For<IOrderRepository>();
    private readonly IPublishEndpoint _publishEndpoint = Substitute.For<IPublishEndpoint>();

    private ProductsClient BuildProductsClient(ProductDetailDto? product)
    {
        var client = Substitute.For<ProductsClient>(
            Arg.Any<System.Net.Http.HttpClient>(),
            Arg.Any<Microsoft.Extensions.Caching.Memory.IMemoryCache>(),
            Arg.Any<Microsoft.Extensions.Logging.ILogger<ProductsClient>>());
        client.GetProductAsync(Arg.Any<Guid>(), Arg.Any<CancellationToken>())
              .Returns(product);
        return client;
    }

    [Fact]
    public async Task Handle_ValidCommand_PublishesOrderCreatedEvent()
    {
        // Arrange
        var product = new ProductDetailDto(
            Guid.NewGuid(), "MacBook Pro", 3999.99m, "EUR", 25, true, Guid.Empty);
        var handler = new CreateOrderHandler(
            _repository, BuildProductsClient(product),
            _publishEndpoint, NullLogger<CreateOrderHandler>.Instance);

        var command = new CreateOrderCommand(
            CustomerId:     Guid.NewGuid(),
            CustomerEmail:  "test@example.com",
            Items:          [new(product.Id, "MacBook Pro", 1, 3999.99m, "EUR")],
            ShippingAddress: new("Gran Vía 28", "Madrid", "28013", "ES"));

        // Act
        var result = await handler.Handle(command, CancellationToken.None);

        // Assert — resultado correcto
        result.Status.Should().Be("Pending");

        // Assert — evento publicado con datos correctos
        await _publishEndpoint.Received(1).Publish(
            Arg.Is<OrderCreated>(e =>
                e.CustomerEmail == "test@example.com" &&
                e.Total         == 3999.99m           &&
                e.Currency      == "EUR"              &&
                e.Items.Count   == 1),
            Arg.Any<CancellationToken>());
    }

    [Fact]
    public async Task Handle_ProductNotAvailable_DoesNotPublishEvent()
    {
        // Arrange — ProductsClient devuelve null (producto no disponible)
        var handler = new CreateOrderHandler(
            _repository, BuildProductsClient(null),
            _publishEndpoint, NullLogger<CreateOrderHandler>.Instance);

        var command = new CreateOrderCommand(
            Guid.NewGuid(), "test@example.com",
            [new(Guid.NewGuid(), "MacBook", 1, 3999.99m, "EUR")],
            new("c", "m", "28013", "ES"));

        // Act
        var act = () => handler.Handle(command, CancellationToken.None);

        // Assert — excepción, sin publicación
        await act.Should().ThrowAsync<Domain.Exceptions.DomainException>();
        await _publishEndpoint.DidNotReceive().Publish(
            Arg.Any<OrderCreated>(), Arg.Any<CancellationToken>());
    }
}
```

---

## Paso 11 — Actualizar el .http con customerEmail

Actualizar `src/Orders.API/Orders.API.http` — añadir `customerEmail` al body del POST:

```http
### [1] Crear pedido (ahora con customerEmail)
POST {{ordersUrl}}/api/orders
Content-Type: application/json

{
  "customerId":    "3fa85f64-5717-4562-b3fc-2c963f66afa6",
  "customerEmail": "john@techshop.es",
  "items": [{
    "productId":   "{{productId}}",
    "productName": "MacBook Pro 16\" M4",
    "quantity": 1,
    "unitPrice":   3999.99,
    "currency":    "EUR"
  }],
  "shippingAddress": {
    "street":  "Gran Vía 28",
    "city":    "Madrid",
    "zipCode": "28013",
    "country": "ES"
  }
}
```

---

## Paso 12 — Verificar compilación y tests

```bash
dotnet build OrderFlow.sln
dotnet test --filter "Category=Unit" --verbosity normal
```

### Verificar el flujo en Aspire

```bash
dotnet run --project infrastructure/OrderFlow.AppHost
```

El dashboard debe mostrar 3 servicios corriendo:

```
● orders-api          Running  https://localhost:7153
● products-api        Running  https://localhost:7200
● notifications-api   Running  (worker, sin puerto HTTP propio)
```

Crear un pedido. En los logs de `notifications-api` debe aparecer:

```
[INF] Processing OrderCreated for order 7b3a2c1d...
      Customer: john@techshop.es. Total: 3999.99 EUR
[INF] [EMAIL SIMULADO] Para: john@techshop.es | Pedido: 7b3a2c1d...
[INF] Order confirmation notification sent and recorded for order 7b3a2c1d...
```

**Panel de RabbitMQ** (`http://localhost:15672`):
- Pestaña **Exchanges**: debe aparecer `OrderFlow.Contracts.Events.Orders:OrderCreated`
- Pestaña **Queues**: debe aparecer `notifications-api-order-created`

---

## Paso 13 — Commit y push

```bash
git add .
git commit -m "m4.2: Mensajería asíncrona con RabbitMQ + MassTransit + Notifications.API

- OrderFlow.Contracts: proyecto classlib con Integration Events
  OrderCreated (+ Items + Address), OrderConfirmed, OrderCancelled
  Esqueletos para M4.3: StockReserved, StockInsufficient, StockReleased, PaymentProcessed, PaymentFailed
- Orders.API:
  CreateOrderCommand + CreateOrderRequest: añadir CustomerEmail
  CreateOrderHandler: IEventPublisher → IPublishEndpoint (MassTransit real)
  FakeEventPublisher + IEventPublisher eliminados
  MassTransit configurado (solo publisher, sin consumers en M4.2)
- Notifications.API: nuevo microservicio worker
  Notification entity con idempotencia por índice único (OrderId + Type)
  NotificationDbContext con schema notifications
  IEmailService + FakeEmailService (loggea email simulado)
  OrderCreatedConsumer: at-least-once + idempotencia básica
  MassTransit con reintentos exponenciales (5 intentos, 1s→30s)
  AddConsumers(Assembly) + ConfigureEndpoints automático
- AppHost: añadir NotificationsDb + notifications-api + messaging connection string
  messaging = amqp://guest:guest@localhost:5672 (RabbitMQ nativo, sin Docker)
- Tests:
  OrderCreatedConsumerTests: InMemoryTestHarness (sin RabbitMQ real)
    - consume_valid: verifica email enviado y registrado en BD
    - consume_duplicate: verifica idempotencia (segundo mensaje ignorado)
  CreateOrderHandlerPublishTests: NSubstitute IPublishEndpoint
    - verifica que se publica OrderCreated con datos correctos
    - verifica que no se publica si el producto no está disponible"

git push origin m4.2
```

---

## Checklist final

- [ ] `dotnet build OrderFlow.sln` → sin errores
- [ ] `dotnet test --filter "Category=Unit"` → todos pasan
- [ ] F5 en AppHost → 3 servicios corriendo (orders, products, notifications)
- [ ] `POST /api/orders` → log `[EMAIL SIMULADO]` en notifications-api
- [ ] Exchange `OrderCreated` y queue `notifications-api-order-created` visibles en http://localhost:15672
- [ ] Traza distribuida en Aspire muestra span de Notifications consumer
- [ ] Detener notifications-api → crear pedido → reiniciar → procesa el mensaje pendiente
- [ ] Commit en rama `m4.2` y push completados

---

## El flujo completo de mensajería

```
Cliente → POST /api/orders
              ↓
         OrdersController → CreateOrderCommand → MediatR Pipeline
              ↓
         CreateOrderHandler
              ├── ProductsClient.GetProductAsync()  → products-api (HTTP síncrono)
              ├── Order.Create() + _repository.SaveAsync()  → SQL INSERT
              └── _publishEndpoint.Publish(OrderCreated)
                      ↓
                 RabbitMQ Exchange: OrderFlow.Contracts.Events.Orders:OrderCreated
                      ↓
                 Queue: notifications-api-order-created
                      ↓                              ↓
              ← 201 Created al cliente    OrderCreatedConsumer.Consume()
                 (sin esperar)                 └── idempotencia check
                                                └── FakeEmailService.Send()
                                                └── NotificationDbContext.SaveChanges()
                                            [INF] [EMAIL SIMULADO] Para: john@techshop.es
```

Orders responde `201 Created` **sin esperar** que Notifications procese el evento.

## Lo que viene en M4.3

`docs/m4.3.md` → Payments.API + Saga Pattern. La Saga coordina el flujo transaccional distribuido: `OrderCreated → ReserveStock → ProcessPayment → OrderConfirmed`. Con compensaciones automáticas si cualquier paso falla.
