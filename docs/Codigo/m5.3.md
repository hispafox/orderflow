# M5.3 — Seguridad Avanzada: OWASP, Rate Limiting y Audit Logging

## Branch

```bash
git checkout m5.2
git checkout -b m5.3
```

## Objetivo

Añadir la última capa de seguridad a OrderFlow: cabeceras HTTP en el gateway, rate limiting diferenciado por usuario autenticado vs anónimo, audit logging persistido e inmutable, validaciones de seguridad en FluentValidation y detección de ataques en el pipeline de autenticación. Al terminar, OrderFlow supera el checklist OWASP Top 10 completo.

## Qué se construye en este módulo

```
src/Gateway.API/
  └── Program.cs                               ← cabeceras HTTP + SmartRateLimit

src/Orders.API/
  ├── Application/Behaviors/AuditBehavior.cs   ← nuevo: pipeline behavior de auditoría
  ├── Application/Interfaces/IAuditLogger.cs   ← nuevo
  ├── Application/Interfaces/IAuditable.cs     ← nuevo: marker interface
  ├── Infrastructure/Audit/
  │   ├── AuditLogEntry.cs                     ← nuevo: entidad de audit
  │   └── DatabaseAuditLogger.cs               ← nuevo: implementación
  ├── Domain/Entities/AuditEvent.cs            ← nuevo: record de auditoría
  ├── Infrastructure/Persistence/OrderDbContext.cs  ← añadir AuditLogs
  ├── Application/Commands/
  │   └── CreateOrderCommand.cs               ← implementar IAuditable
  └── Program.cs                               ← registrar IAuditLogger + AuditBehavior

src/Orders.API/ (validaciones de seguridad)
  └── Application/Commands/
      └── CreateOrderCommandValidator.cs      ← añadir validaciones de seguridad

Directory.Build.props                          ← nuevo: NuGetAudit=true

tests/Orders.API.Tests/
  └── Integration/Security/SecurityTests.cs   ← nuevo: tests de seguridad
```

---

## Paso 1 — Cabeceras HTTP de seguridad en Gateway.API

Añadir middleware de cabeceras de seguridad en `src/Gateway.API/Program.cs`, justo antes de `app.UseAuthentication()`:

```csharp
// ─── Cabeceras de seguridad HTTP ─────────────────────────────────────────────
// Se aplican a TODAS las respuestas del gateway — incluyendo los forwards de YARP
app.Use(async (context, next) =>
{
    var headers = context.Response.Headers;

    // HSTS: fuerza HTTPS durante 1 año (incluyendo subdominios)
    // Solo en producción — en dev rompe el flujo de certificados autofirmados
    if (!context.RequestServices
            .GetRequiredService<IWebHostEnvironment>()
            .IsDevelopment())
    {
        headers.Append("Strict-Transport-Security",
            "max-age=31536000; includeSubDomains");
    }

    // Prevenir clickjacking (iframe embebido)
    headers.Append("X-Frame-Options", "DENY");

    // Prevenir MIME sniffing (el navegador respeta el Content-Type declarado)
    headers.Append("X-Content-Type-Options", "nosniff");

    // Política de referrer: solo el origen, no la URL completa
    headers.Append("Referrer-Policy", "strict-origin-when-cross-origin");

    // CSP estricto para API pura (solo JSON, sin HTML)
    // Si hay Scalar UI, ver sección de CSP condicional más abajo
    headers.Append("Content-Security-Policy",
        "default-src 'none'; frame-ancestors 'none'");

    // Deshabilitar features del navegador no necesarias
    headers.Append("Permissions-Policy",
        "camera=(), microphone=(), geolocation=(), interest-cohort=()");

    // Eliminar headers que revelan tecnología usada
    headers.Remove("Server");
    headers.Remove("X-Powered-By");
    headers.Remove("X-AspNet-Version");
    headers.Remove("X-AspNetMvc-Version");

    await next();
});
```

### CSP condicional para Scalar UI (solo Development)

Si el gateway expone Scalar UI en desarrollo, añadir CSP menos restrictivo por ruta:

```csharp
if (app.Environment.IsDevelopment())
{
    app.Use(async (ctx, next) =>
    {
        if (ctx.Request.Path.StartsWithSegments("/openapi") ||
            ctx.Request.Path.StartsWithSegments("/scalar"))
        {
            // Scalar necesita inline scripts — CSP más permisivo
            ctx.Response.Headers["Content-Security-Policy"] =
                "default-src 'self'; " +
                "script-src 'self' 'unsafe-inline'; " +
                "style-src 'self' 'unsafe-inline'; " +
                "img-src 'self' data:; " +
                "connect-src 'self'; " +
                "frame-ancestors 'none'";
        }
        await next();
    });
    app.MapOpenApi();
}
```

### Configurar TLS mínimo en Kestrel

Añadir en `src/Gateway.API/Program.cs` antes de `builder.Build()`:

```csharp
using System.Security.Authentication;

builder.WebHost.ConfigureKestrel(options =>
{
    options.ConfigureHttpsDefaults(httpsOptions =>
    {
        // TLS 1.2 mínimo, preferir 1.3
        httpsOptions.SslProtocols = SslProtocols.Tls12 | SslProtocols.Tls13;
    });

    // Limitar el tamaño del request body: 1 MB máximo
    // Protege contra ataques de payload grande (OOM)
    options.Limits.MaxRequestBodySize = 1 * 1024 * 1024; // 1 MB
});
```

---

## Paso 2 — Rate Limiting inteligente: por usuario autenticado o por IP

Reemplazar el bloque de `AddRateLimiter` en `src/Gateway.API/Program.cs`:

```csharp
using System.Security.Claims;
using System.Threading.RateLimiting;

builder.Services.AddRateLimiter(options =>
{
    // ─── Rate limiter inteligente ─────────────────────────────────────────────
    // Usuarios autenticados: limitar por userId (60/min)
    // Usuarios anónimos:    limitar por IP (30/min — más restrictivo)
    options.AddPolicy("SmartRateLimit", httpContext =>
    {
        var userId = httpContext.User?.FindFirstValue("sub");

        if (!string.IsNullOrEmpty(userId))
        {
            // Autenticado: partition key = userId del JWT
            return RateLimitPartition.GetSlidingWindowLimiter(
                partitionKey: $"user:{userId}",
                factory: _ => new SlidingWindowRateLimiterOptions
                {
                    Window            = TimeSpan.FromMinutes(1),
                    PermitLimit       = 60,
                    SegmentsPerWindow = 4,
                    QueueLimit        = 10,
                    QueueProcessingOrder = QueueProcessingOrder.OldestFirst
                });
        }

        // Anónimo: partition key = IP
        var clientIp = httpContext.Connection.RemoteIpAddress?.ToString()
                       ?? "unknown";
        return RateLimitPartition.GetSlidingWindowLimiter(
            partitionKey: $"ip:{clientIp}",
            factory: _ => new SlidingWindowRateLimiterOptions
            {
                Window            = TimeSpan.FromMinutes(1),
                PermitLimit       = 30,   // Más restrictivo para anónimos
                SegmentsPerWindow = 4,
                QueueLimit        = 5,
                QueueProcessingOrder = QueueProcessingOrder.OldestFirst
            });
    });

    // ─── Creación de pedidos — estricto ──────────────────────────────────────
    options.AddPolicy("CreateOrder", httpContext =>
    {
        var userId = httpContext.User?.FindFirstValue("sub")
                     ?? httpContext.Connection.RemoteIpAddress?.ToString()
                     ?? "unknown";
        return RateLimitPartition.GetFixedWindowLimiter(
            partitionKey: $"create-order:{userId}",
            factory: _ => new FixedWindowRateLimiterOptions
            {
                Window      = TimeSpan.FromMinutes(1),
                PermitLimit = 10,
                QueueLimit  = 0   // Sin cola — rechaza inmediatamente
            });
    });

    // ─── Respuesta 429 ────────────────────────────────────────────────────────
    options.RejectionStatusCode = StatusCodes.Status429TooManyRequests;
    options.OnRejected = async (context, ct) =>
    {
        var userId = context.HttpContext.User?.FindFirstValue("sub") ?? "anonymous";
        var path   = context.HttpContext.Request.Path;
        var logger = context.HttpContext.RequestServices
            .GetRequiredService<ILogger<Program>>();

        logger.LogWarning(
            "[SECURITY] Rate limit exceeded for {UserId} on {Path}",
            userId, path);

        context.HttpContext.Response.Headers.RetryAfter = "60";
        context.HttpContext.Response.ContentType = "application/json";
        await context.HttpContext.Response.WriteAsJsonAsync(new
        {
            title    = "Too Many Requests",
            status   = 429,
            detail   = "Rate limit exceeded. Please try again in 60 seconds.",
            retryAfter = 60
        }, ct);
    };
});
```

### Actualizar appsettings.json de YARP para usar SmartRateLimit

Actualizar las rutas en `src/Gateway.API/appsettings.json`:

```json
{
  "ReverseProxy": {
    "Routes": {
      "orders-create-route": {
        "ClusterId": "orders-cluster",
        "Match": { "Path": "/api/orders", "Methods": [ "POST" ] },
        "AuthorizationPolicy": "CustomerOrAdmin",
        "RateLimiterPolicy": "CreateOrder",
        "Transforms": [
          { "RequestHeadersCopy": "true" },
          { "RequestHeader": "X-Gateway-Source", "Set": "orderflow-gateway" }
        ]
      },
      "orders-read-route": {
        "ClusterId": "orders-cluster",
        "Match": { "Path": "/api/orders/{**catch-all}", "Methods": [ "GET", "POST" ] },
        "AuthorizationPolicy": "CustomerOrAdmin",
        "RateLimiterPolicy": "SmartRateLimit",
        "Transforms": [
          { "RequestHeadersCopy": "true" }
        ]
      },
      "products-route": {
        "ClusterId": "products-cluster",
        "Match": { "Path": "/api/products/{**catch-all}" },
        "RateLimiterPolicy": "SmartRateLimit",
        "Transforms": [ { "RequestHeadersCopy": "true" } ]
      }
    }
  }
}
```

---

## Paso 3 — Detección de brute force en autenticación

Añadir detección de intentos fallidos repetidos en el bloque `AddJwtBearer` de `src/Gateway.API/Program.cs`:

```csharp
// Añadir el servicio de contador de fallos (singleton para que persista entre requests)
builder.Services.AddSingleton<IAuthFailureCounter, MemoryAuthFailureCounter>();

// En el bloque AddJwtBearer:
options.Events = new JwtBearerEvents
{
    OnAuthenticationFailed = async ctx =>
    {
        var logger = ctx.HttpContext.RequestServices
            .GetRequiredService<ILogger<Program>>();
        var ip = ctx.HttpContext.Connection.RemoteIpAddress?.ToString() ?? "unknown";

        logger.LogWarning(
            "[SECURITY] Authentication failed from {IpAddress}. " +
            "Error: {Error}. Path: {Path}",
            ip, ctx.Exception.Message, ctx.HttpContext.Request.Path);

        var counter  = ctx.HttpContext.RequestServices
            .GetRequiredService<IAuthFailureCounter>();
        var failures = await counter.IncrementAsync(ip);

        if (failures > 10)
        {
            logger.LogError(
                "[SECURITY] Possible brute force from {IpAddress}. " +
                "{Failures} failures in the last minute",
                ip, failures);
            // En producción: añadir a blocklist temporal en Redis
            // y disparar alerta en Application Insights
        }
    },
    OnTokenValidated = ctx =>
    {
        var userId = ctx.Principal?.FindFirstValue(
            System.Security.Claims.ClaimTypes.NameIdentifier);
        Log.Debug("JWT valid for user {UserId}", userId);
        return Task.CompletedTask;
    }
};
```

### IAuthFailureCounter + MemoryAuthFailureCounter

Crear `src/Gateway.API/Services/IAuthFailureCounter.cs`:

```csharp
namespace Gateway.API.Services;

public interface IAuthFailureCounter
{
    Task<int> IncrementAsync(string key, CancellationToken ct = default);
}
```

Crear `src/Gateway.API/Services/MemoryAuthFailureCounter.cs`:

```csharp
using Microsoft.Extensions.Caching.Memory;

namespace Gateway.API.Services;

/// <summary>
/// Contador de fallos de autenticación en memoria.
/// Ventana deslizante de 1 minuto por IP.
/// En producción con múltiples instancias: reemplazar por Redis.
/// </summary>
public class MemoryAuthFailureCounter : IAuthFailureCounter
{
    private readonly IMemoryCache _cache;

    public MemoryAuthFailureCounter(IMemoryCache cache) => _cache = cache;

    public Task<int> IncrementAsync(string key, CancellationToken ct = default)
    {
        var cacheKey = $"auth-fail:{key}";
        var count    = _cache.GetOrCreate(cacheKey, entry =>
        {
            entry.AbsoluteExpirationRelativeToNow = TimeSpan.FromMinutes(1);
            return 0;
        });
        count++;
        _cache.Set(cacheKey, count, TimeSpan.FromMinutes(1));
        return Task.FromResult(count);
    }
}
```

Registrar en Program.cs:

```csharp
builder.Services.AddMemoryCache();
builder.Services.AddSingleton<IAuthFailureCounter, MemoryAuthFailureCounter>();
```

---

## Paso 4 — IAuditLogger, AuditEvent y AuditLogEntry en Orders.API

### IAuditLogger.cs (fichero completo)

Crear `src/Orders.API/Application/Interfaces/IAuditLogger.cs`:

```csharp
using Orders.API.Infrastructure.Audit;

namespace Orders.API.Application.Interfaces;

public interface IAuditLogger
{
    Task LogAsync(AuditEvent auditEvent, CancellationToken ct = default);
}
```

### AuditEvent.cs (fichero completo)

Crear `src/Orders.API/Infrastructure/Audit/AuditEvent.cs`:

```csharp
namespace Orders.API.Infrastructure.Audit;

/// <summary>
/// Record inmutable que describe un evento de auditoría.
/// Separado del logging de aplicación (Serilog):
///   - El audit log es para compliance y respuesta a incidentes
///   - El logging de app es para debugging y operaciones
/// </summary>
public record AuditEvent
{
    public string  Action       { get; init; } = string.Empty;
    public string  UserId       { get; init; } = string.Empty;
    public string  UserEmail    { get; init; } = string.Empty;
    public string  ResourceType { get; init; } = string.Empty;
    public string? ResourceId   { get; init; }
    public string? IpAddress    { get; init; }
    public Dictionary<string, object>? Details { get; init; }
    public DateTime Timestamp   { get; init; } = DateTime.UtcNow;
    public bool    Success      { get; init; } = true;
    public string? FailureReason { get; init; }
}
```

### AuditLogEntry.cs (entidad EF Core)

Crear `src/Orders.API/Infrastructure/Audit/AuditLogEntry.cs`:

```csharp
namespace Orders.API.Infrastructure.Audit;

/// <summary>
/// Fila de la tabla orders.AuditLogs.
/// Sin cascade delete — el audit log es inmutable por diseño.
/// Índices estratégicos: UserId (GDPR acceso), Timestamp (informes), ResourceType+Id (investigación).
/// </summary>
public class AuditLogEntry
{
    public long     Id           { get; set; }
    public string   Action       { get; set; } = string.Empty;
    public string   UserId       { get; set; } = string.Empty;
    public string   UserEmail    { get; set; } = string.Empty;
    public string   ResourceType { get; set; } = string.Empty;
    public string?  ResourceId   { get; set; }
    public string?  IpAddress    { get; set; }
    public string?  Details      { get; set; }  // JSON serializado
    public DateTime Timestamp    { get; set; } = DateTime.UtcNow;
    public bool     Success      { get; set; }
    public string?  FailureReason { get; set; }
}
```

### DatabaseAuditLogger.cs (fichero completo)

Crear `src/Orders.API/Infrastructure/Audit/DatabaseAuditLogger.cs`:

```csharp
using System.Text.Json;
using Microsoft.Extensions.Logging;
using Orders.API.Application.Interfaces;
using Orders.API.Infrastructure.Persistence;

namespace Orders.API.Infrastructure.Audit;

/// <summary>
/// Persiste audit events en orders.AuditLogs y los emite como logs estructurados
/// para Application Insights (con el prefijo [AUDIT] para facilitar el filtrado).
/// </summary>
public class DatabaseAuditLogger : IAuditLogger
{
    private readonly OrderDbContext         _dbContext;
    private readonly ILogger<DatabaseAuditLogger> _logger;

    public DatabaseAuditLogger(
        OrderDbContext              dbContext,
        ILogger<DatabaseAuditLogger> logger)
    {
        _dbContext = dbContext;
        _logger    = logger;
    }

    public async Task LogAsync(AuditEvent evt, CancellationToken ct = default)
    {
        // 1. Persistir en BD (inmutable, para auditoría formal)
        var entry = new AuditLogEntry
        {
            Action       = evt.Action,
            UserId       = evt.UserId,
            UserEmail    = evt.UserEmail,
            ResourceType = evt.ResourceType,
            ResourceId   = evt.ResourceId,
            IpAddress    = evt.IpAddress,
            Details      = evt.Details is not null
                ? JsonSerializer.Serialize(evt.Details)
                : null,
            Timestamp    = evt.Timestamp,
            Success      = evt.Success,
            FailureReason = evt.FailureReason
        };

        _dbContext.AuditLogs.Add(entry);
        await _dbContext.SaveChangesAsync(ct);

        // 2. Log estructurado para Application Insights / Aspire Dashboard
        _logger.LogInformation(
            "[AUDIT] {Action} by {UserEmail} ({UserId}) on {ResourceType} " +
            "{ResourceId} from {IpAddress}. Success: {Success}",
            evt.Action, evt.UserEmail, evt.UserId,
            evt.ResourceType, evt.ResourceId ?? "N/A",
            evt.IpAddress ?? "unknown", evt.Success);
    }
}
```

---

## Paso 5 — Añadir AuditLogs a OrderDbContext

Actualizar `src/Orders.API/Infrastructure/Persistence/OrderDbContext.cs`:

```csharp
using Orders.API.Infrastructure.Audit;

// Dentro de la clase OrderDbContext:
public DbSet<AuditLogEntry> AuditLogs => Set<AuditLogEntry>();

// Dentro de OnModelCreating — añadir al final:
modelBuilder.Entity<AuditLogEntry>(entity =>
{
    entity.ToTable("AuditLogs", "orders");
    entity.HasKey(e => e.Id);

    // Índices para las queries más frecuentes:
    entity.HasIndex(e => e.UserId)
          .HasDatabaseName("IX_AuditLogs_UserId");
    entity.HasIndex(e => e.Timestamp)
          .HasDatabaseName("IX_AuditLogs_Timestamp");
    entity.HasIndex(e => new { e.ResourceType, e.ResourceId })
          .HasDatabaseName("IX_AuditLogs_Resource");

    entity.Property(e => e.Action).HasMaxLength(100);
    entity.Property(e => e.UserId).HasMaxLength(100);
    entity.Property(e => e.UserEmail).HasMaxLength(320);
    entity.Property(e => e.ResourceType).HasMaxLength(100);
    entity.Property(e => e.ResourceId).HasMaxLength(100);
    entity.Property(e => e.IpAddress).HasMaxLength(45);
    entity.Property(e => e.FailureReason).HasMaxLength(500);

    // Sin cascade delete: el audit log no se borra nunca
    // (para el derecho al olvido GDPR: anonimizar, no borrar)
});
```

Crear la migration:

```bash
dotnet ef migrations add AddAuditLogs \
    --project src/Orders.API \
    --context OrderDbContext

dotnet ef database update \
    --project src/Orders.API \
    --context OrderDbContext
```

---

## Paso 6 — IAuditable y AuditBehavior (pipeline MediatR)

### IAuditable.cs (marker interface)

Crear `src/Orders.API/Application/Interfaces/IAuditable.cs`:

```csharp
namespace Orders.API.Application.Interfaces;

/// <summary>
/// Marker interface para Commands que deben generar audit log.
/// Si un Command implementa IAuditable, el AuditBehavior lo intercepta
/// y genera automáticamente el registro de auditoría.
/// </summary>
public interface IAuditable
{
    Guid    ActorId       { get; }
    string  ActorEmail    { get; }
    string  ResourceType  { get; }
    Guid?   ResourceId    { get; }
}
```

### AuditBehavior.cs (fichero completo)

Crear `src/Orders.API/Application/Behaviors/AuditBehavior.cs`:

```csharp
using MediatR;
using Microsoft.AspNetCore.Http;
using Orders.API.Application.Interfaces;
using Orders.API.Infrastructure.Audit;

namespace Orders.API.Application.Behaviors;

/// <summary>
/// Pipeline behavior que intercepta todos los Commands que implementan IAuditable
/// y genera automáticamente el registro de auditoría.
/// Registra tanto el éxito como el fallo — ambos son relevantes para compliance.
/// </summary>
public class AuditBehavior<TRequest, TResponse>
    : IPipelineBehavior<TRequest, TResponse>
    where TRequest : IRequest<TResponse>
{
    private readonly IAuditLogger        _auditLogger;
    private readonly IHttpContextAccessor _httpContextAccessor;
    private readonly ILogger<AuditBehavior<TRequest, TResponse>> _logger;

    public AuditBehavior(
        IAuditLogger         auditLogger,
        IHttpContextAccessor httpContextAccessor,
        ILogger<AuditBehavior<TRequest, TResponse>> logger)
    {
        _auditLogger         = auditLogger;
        _httpContextAccessor = httpContextAccessor;
        _logger              = logger;
    }

    public async Task<TResponse> Handle(
        TRequest                         request,
        RequestHandlerDelegate<TResponse> next,
        CancellationToken                ct)
    {
        // Solo procesar requests auditables
        if (request is not IAuditable auditable)
            return await next();

        var ip = _httpContextAccessor.HttpContext?
            .Connection.RemoteIpAddress?.ToString();

        try
        {
            var response = await next();

            // Éxito — registrar la acción completada
            await _auditLogger.LogAsync(new AuditEvent
            {
                Action       = typeof(TRequest).Name,
                UserId       = auditable.ActorId.ToString(),
                UserEmail    = auditable.ActorEmail,
                ResourceType = auditable.ResourceType,
                ResourceId   = auditable.ResourceId?.ToString(),
                IpAddress    = ip,
                Success      = true
            }, ct);

            return response;
        }
        catch (Exception ex)
        {
            // Fallo — también se registra (para detección de intentos)
            await _auditLogger.LogAsync(new AuditEvent
            {
                Action        = $"{typeof(TRequest).Name}Failed",
                UserId        = auditable.ActorId.ToString(),
                UserEmail     = auditable.ActorEmail,
                ResourceType  = auditable.ResourceType,
                ResourceId    = auditable.ResourceId?.ToString(),
                IpAddress     = ip,
                Success       = false,
                FailureReason = ex.Message
            }, ct);

            throw;
        }
    }
}
```

---

## Paso 7 — Implementar IAuditable en CreateOrderCommand

Actualizar `src/Orders.API/Application/Commands/CreateOrderCommand.cs`:

```csharp
using MediatR;
using Orders.API.API.DTOs.Responses;
using Orders.API.Application.Behaviors;
using Orders.API.Application.Interfaces;

namespace Orders.API.Application.Commands;

public record CreateOrderCommand(
    Guid                              CustomerId,
    string                            CustomerEmail,
    IReadOnlyList<CreateOrderItemDto> Items,
    CreateOrderAddressDto             ShippingAddress)
    : IRequest<OrderDto>, ITransactional, IAuditable
{
    // IAuditable — datos del actor para el audit log
    public Guid   ActorId      => CustomerId;
    public string ActorEmail   => CustomerEmail;
    public string ResourceType => "Order";
    public Guid?  ResourceId   => null; // Se rellena cuando el pedido existe
}
```

---

## Paso 8 — Registrar IAuditLogger y AuditBehavior en Orders.API

Añadir al bloque de servicios en `src/Orders.API/Program.cs`:

```csharp
using Orders.API.Application.Behaviors;
using Orders.API.Application.Interfaces;
using Orders.API.Infrastructure.Audit;

// ─── Audit Logger ─────────────────────────────────────────────────────────────
builder.Services.AddScoped<IAuditLogger, DatabaseAuditLogger>();
builder.Services.AddHttpContextAccessor();

// ─── MediatR Pipeline — añadir AuditBehavior ─────────────────────────────────
// Orden del pipeline: Logging → Validation → Audit → Transaction → Handler
builder.Services.AddMediatR(cfg =>
{
    cfg.RegisterServicesFromAssembly(typeof(Program).Assembly);
    cfg.AddBehavior(typeof(IPipelineBehavior<,>),
        typeof(LoggingBehavior<,>));
    cfg.AddBehavior(typeof(IPipelineBehavior<,>),
        typeof(ValidationBehavior<,>));
    cfg.AddBehavior(typeof(IPipelineBehavior<,>),
        typeof(AuditBehavior<,>));          // ← nuevo
    cfg.AddBehavior(typeof(IPipelineBehavior<,>),
        typeof(TransactionBehavior<,>));
});
```

---

## Paso 9 — Actualizar OrdersController: prevenir enumeración de recursos

Actualizar `src/Orders.API/API/Controllers/OrdersController.cs` — el `GetById` debe devolver **404 siempre** cuando el recurso no pertenece al usuario (no 403, que revela que el recurso existe):

```csharp
[HttpGet("{id:guid}")]
public async Task<ActionResult<OrderDto>> GetById(
    Guid id, CancellationToken ct = default)
{
    var order = await _mediator.Send(new GetOrderByIdQuery(id), ct);

    if (order is null) return NotFound();

    var userId  = GetUserId();
    var isAdmin = IsAdmin();

    // Para customers: devolver 404 tanto si no existe como si no es suyo
    // (no revelar que el pedido existe pero pertenece a otro)
    // Para admins: 403 explícito (tienen contexto del sistema)
    if (!isAdmin && order.CustomerId.ToString() != userId)
        return NotFound();  // ← 404, no 403 — previene enumeración

    return Ok(order);
}
```

---

## Paso 10 — Validaciones de seguridad en FluentValidation

Actualizar `src/Orders.API/Application/Commands/CreateOrderCommandValidator.cs`:

```csharp
using FluentValidation;

namespace Orders.API.Application.Commands;

public class CreateOrderCommandValidator : AbstractValidator<CreateOrderCommand>
{
    public CreateOrderCommandValidator()
    {
        RuleFor(x => x.CustomerId)
            .NotEmpty().WithMessage("CustomerId is required");

        RuleFor(x => x.CustomerEmail)
            .NotEmpty()
            .EmailAddress()
            .MaximumLength(256)
            // Prevenir inyección de headers en el email
            .Matches(@"^[^<>\r\n\t;]+$")
            .WithMessage("Invalid email format or characters");

        RuleFor(x => x.Items)
            .NotEmpty().WithMessage("Order must have at least one item")
            // Prevenir payloads absurdamente grandes
            .Must(items => items.Count <= 50)
            .WithMessage("Maximum 50 items per order");

        RuleForEach(x => x.Items).ChildRules(item =>
        {
            item.RuleFor(i => i.ProductId)
                .NotEmpty();

            // Prevenir XSS si el nombre se muestra en HTML
            item.RuleFor(i => i.ProductName)
                .NotEmpty()
                .MaximumLength(200)
                .Matches(@"^[^<>""'&]+$")
                .WithMessage("ProductName contains invalid characters");

            // Prevenir cantidades absurdas (buffer de negocio + seguridad)
            item.RuleFor(i => i.Quantity)
                .GreaterThan(0).WithMessage("Quantity must be at least 1")
                .LessThanOrEqualTo(999).WithMessage("Maximum quantity is 999");

            // Prevenir precios negativos o inyección de precio=0
            item.RuleFor(i => i.UnitPrice)
                .GreaterThan(0.01m).WithMessage("UnitPrice must be greater than 0")
                .LessThanOrEqualTo(99_999.99m)
                .WithMessage("UnitPrice exceeds maximum allowed");
        });

        RuleFor(x => x.ShippingAddress.Street)
            .NotEmpty()
            .MaximumLength(200)
            .Matches(@"^[\w\s\.,\-À-ÿ]+$")
            .WithMessage("Street contains invalid characters");

        RuleFor(x => x.ShippingAddress.City)
            .NotEmpty()
            .MaximumLength(100)
            .Matches(@"^[\w\s\-À-ÿ]+$")
            .WithMessage("City contains invalid characters");

        RuleFor(x => x.ShippingAddress.ZipCode)
            .NotEmpty()
            .MaximumLength(20)
            .Matches(@"^[\w\s\-]+$")
            .WithMessage("ZipCode contains invalid characters");

        RuleFor(x => x.ShippingAddress.Country)
            .NotEmpty()
            .MaximumLength(2)
            .Matches(@"^[A-Z]{2}$")
            .WithMessage("Country must be a 2-letter ISO code (e.g. ES, DE)");
    }
}
```

---

## Paso 11 — Dependency Scanning: NuGetAudit

Crear `Directory.Build.props` en la raíz del repositorio:

```xml
<Project>
  <PropertyGroup>
    <!-- Falla el build si hay vulnerabilidades High o Critical en dependencias NuGet -->
    <NuGetAudit>true</NuGetAudit>
    <NuGetAuditLevel>high</NuGetAuditLevel>
    <!-- Tratar warnings de auditoria como errores en CI -->
    <NuGetAuditMode>all</NuGetAuditMode>
  </PropertyGroup>
</Project>
```

Verificar manualmente:

```bash
# Listar dependencias con vulnerabilidades conocidas
dotnet list package --vulnerable --include-transitive

# En el pipeline de CI — debe fallar si hay vulnerabilidades High/Critical
dotnet build OrderFlow.sln
```

---

## Paso 12 — Security Tests

Añadir paquete en el proyecto de tests:

```bash
dotnet add tests/Orders.API.Tests/Orders.API.Tests.csproj \
    package FluentAssertions
```

### SecurityTests.cs (fichero completo)

Crear `tests/Orders.API.Tests/Integration/Security/SecurityTests.cs`:

```csharp
using System.Net;
using System.Net.Http.Headers;
using System.Net.Http.Json;
using FluentAssertions;
using Orders.API.Tests.Shared;
using Xunit;

namespace Orders.API.Tests.Integration.Security;

/// <summary>
/// Tests que verifican los controles de seguridad de Orders.API.
/// Ejecutar con: dotnet test --filter "Category=Security"
/// </summary>
[Trait("Category", "Security")]
public class SecurityTests : IClassFixture<OrdersApiFactory>
{
    private readonly HttpClient _client;

    private static readonly object ValidOrderRequest = new
    {
        items = new[] { new { productId = Guid.NewGuid(), quantity = 1 } },
        shippingAddress = new
        {
            street  = "Gran Vía 28", city    = "Madrid",
            zipCode = "28013",       country = "ES"
        }
    };

    public SecurityTests(OrdersApiFactory factory)
        => _client = factory.CreateClient();

    // ─── A01 Broken Access Control ────────────────────────────────────────────

    [Fact]
    public async Task GetOrder_WithoutToken_Returns401()
    {
        _client.DefaultRequestHeaders.Authorization = null;
        var response = await _client.GetAsync("/api/orders");
        response.StatusCode.Should().Be(HttpStatusCode.Unauthorized);
    }

    [Fact]
    public async Task GetOrder_AnotherUsersOrder_Returns404_NotForbidden()
    {
        // John crea un pedido
        var johnId    = Guid.NewGuid().ToString();
        var johnToken = JwtTestHelper.CustomerToken(johnId);
        _client.DefaultRequestHeaders.Authorization =
            new AuthenticationHeaderValue("Bearer", johnToken);
        var createResp = await _client.PostAsJsonAsync("/api/orders", ValidOrderRequest);

        // Si la creación fue exitosa
        if (createResp.IsSuccessStatusCode)
        {
            var created = await createResp.Content
                .ReadFromJsonAsync<dynamic>();
            var orderId = created?.GetProperty("id").GetString();

            // Jane intenta ver el pedido de John
            var janeToken = JwtTestHelper.CustomerToken(Guid.NewGuid().ToString());
            _client.DefaultRequestHeaders.Authorization =
                new AuthenticationHeaderValue("Bearer", janeToken);

            var getResp = await _client.GetAsync($"/api/orders/{orderId}");

            // DEBE ser 404, no 403 — no revelar que el pedido existe
            getResp.StatusCode.Should().Be(HttpStatusCode.NotFound,
                "customers should get 404 (not 403) to prevent resource enumeration");
        }
    }

    [Fact]
    public async Task AdminConfirm_WithCustomerToken_Returns403()
    {
        var token = JwtTestHelper.CustomerToken();
        _client.DefaultRequestHeaders.Authorization =
            new AuthenticationHeaderValue("Bearer", token);

        var response = await _client.PostAsync(
            $"/api/orders/{Guid.NewGuid()}/confirm", null);

        // Customer no puede confirmar pedidos — debe recibir 403
        response.StatusCode.Should().Be(HttpStatusCode.Forbidden);
    }

    // ─── A07 Authentication Failures ─────────────────────────────────────────

    [Fact]
    public async Task ExpiredToken_Returns401()
    {
        var expired = JwtTestHelper.ExpiredToken();
        _client.DefaultRequestHeaders.Authorization =
            new AuthenticationHeaderValue("Bearer", expired);

        var response = await _client.GetAsync("/api/orders");
        response.StatusCode.Should().Be(HttpStatusCode.Unauthorized);
    }

    [Fact]
    public async Task TamperedToken_Returns401()
    {
        var validToken  = JwtTestHelper.CustomerToken();
        var tamperedToken = validToken[..^5] + "XXXXX";  // Modificar la firma
        _client.DefaultRequestHeaders.Authorization =
            new AuthenticationHeaderValue("Bearer", tamperedToken);

        var response = await _client.GetAsync("/api/orders");
        response.StatusCode.Should().Be(HttpStatusCode.Unauthorized);
    }

    // ─── A03 Injection prevention ─────────────────────────────────────────────

    [Fact]
    public async Task SqlInjectionInEmail_Returns422()
    {
        var token = JwtTestHelper.CustomerToken();
        _client.DefaultRequestHeaders.Authorization =
            new AuthenticationHeaderValue("Bearer", token);

        var maliciousRequest = new
        {
            customerEmail = "'; DROP TABLE Orders; --",
            items = new[] { new { productId = Guid.NewGuid(), quantity = 1 } },
            shippingAddress = new
            {
                street = "Test", city = "Madrid", zipCode = "28013", country = "ES"
            }
        };

        var response = await _client.PostAsJsonAsync("/api/orders", maliciousRequest);

        // FluentValidation debe rechazar con 422 (Unprocessable Entity)
        response.StatusCode.Should().Be(HttpStatusCode.UnprocessableEntity,
            "SQL injection attempts in email should be rejected by FluentValidation");
    }

    [Fact]
    public async Task XssInProductName_Returns422()
    {
        var token = JwtTestHelper.CustomerToken();
        _client.DefaultRequestHeaders.Authorization =
            new AuthenticationHeaderValue("Bearer", token);

        var xssRequest = new
        {
            customerEmail = "test@test.com",
            items = new[]
            {
                new { productId = Guid.NewGuid(), productName = "<script>alert('xss')</script>",
                      quantity = 1, unitPrice = 99.99m, currency = "EUR" }
            },
            shippingAddress = new
            {
                street = "Test", city = "Madrid", zipCode = "28013", country = "ES"
            }
        };

        var response = await _client.PostAsJsonAsync("/api/orders", xssRequest);
        response.StatusCode.Should().Be(HttpStatusCode.UnprocessableEntity,
            "XSS in productName should be rejected by FluentValidation");
    }

    // ─── A05 Security Misconfiguration ────────────────────────────────────────

    [Fact]
    public async Task Response_HasSecurityHeaders()
    {
        var response = await _client.GetAsync("/health");

        // Verificar que la respuesta tiene las cabeceras básicas de seguridad
        // (en el factory de tests puede que no estén configuradas — test informativo)
        response.Headers.Should().NotContainKey("Server",
            "Server header should be removed to avoid technology disclosure");
    }
}
```

---

## Paso 13 — Verificación: OWASP Top 10 de OrderFlow

Ejecutar el checklist de seguridad tras implementar M5.3:

```bash
# Tests de seguridad
dotnet test --filter "Category=Security" --verbosity normal

# Tests de integración incluyendo autenticación
dotnet test --filter "Category=Integration" --verbosity normal

# Build con NuGetAudit activo
dotnet build OrderFlow.sln
# → Si aparece algún WARNING de vulnerabilidad → resolver antes de continuar

# Dependencias vulnerables
dotnet list package --vulnerable --include-transitive
# → Debe devolver vacío

# Verificar cabeceras con curl (Aspire corriendo)
curl -I https://localhost:8080/health
# Esperar: x-frame-options, x-content-type-options, referrer-policy
# No esperar: Server, X-Powered-By

# Verificar que el Rate Limiting funciona
# Ejecutar 11 veces seguidas el mismo request → la 11ª debe devolver 429
for i in {1..11}; do
    curl -s -o /dev/null -w "%{http_code}\n" \
        -X POST https://localhost:8080/api/orders \
        -H "Content-Type: application/json" \
        -H "Authorization: Bearer TOKEN_AQUI" \
        -d '{}'
done
# → Las primeras 10: 422 (validación). La 11ª: 429 Too Many Requests
```

---

## Paso 14 — Commit y push

```bash
git add .
git commit -m "m5.3: Seguridad avanzada — OWASP, Rate Limiting y Audit Logging

- Cabeceras HTTP de seguridad en Gateway.API (middleware en pipeline):
  HSTS (max-age=31536000, includeSubDomains — solo producción)
  X-Frame-Options: DENY
  X-Content-Type-Options: nosniff
  Referrer-Policy: strict-origin-when-cross-origin
  Content-Security-Policy: default-src 'none' (API pura)
  Permissions-Policy: camera, microphone, geolocation deshabilitados
  Eliminación de Server, X-Powered-By, X-AspNet-Version
  CSP condicional menos restrictivo para Scalar UI (solo Development)
- TLS mínimo: Tls12 | Tls13 en Kestrel
- MaxRequestBodySize: 1 MB (protección contra payload grande / OOM)
- Rate Limiting inteligente (SmartRateLimit):
  Autenticados: sliding window 60/min por userId del JWT
  Anónimos: sliding window 30/min por IP (más restrictivo)
  CreateOrder: fixed window 10/min por userId (estricto, sin cola)
  Respuesta 429 JSON con Retry-After: 60
- Detección de brute force: IAuthFailureCounter + MemoryAuthFailureCounter
  JwtBearerEvents.OnAuthenticationFailed: log [SECURITY] + contador por IP
  Umbral 10 fallos/minuto → log [SECURITY] Possible brute force
- Audit Logging:
  IAuditLogger / AuditEvent / AuditLogEntry / DatabaseAuditLogger
  Persiste en orders.AuditLogs (inmutable, 3 índices: UserId, Timestamp, Resource)
  Emite [AUDIT] logs estructurados para Application Insights
  IAuditable marker interface para Commands auditables
  AuditBehavior en pipeline MediatR (4ª posición: Log→Val→Audit→Tx→Handler)
  CreateOrderCommand implementa IAuditable
  Registra éxito Y fallo (ambos relevantes para compliance)
- OrdersController: GetById devuelve 404 (no 403) para recursos ajenos
  Previene enumeración de recursos (OWASP A01 + privacidad)
- FluentValidation: validaciones de seguridad adicionales
  CustomerEmail: MaxLength(256) + regex sin <>\r\n\t;
  ProductName: MaxLength(200) + regex sin HTML peligroso (<>\"'&)
  Quantity: rango 1-999; UnitPrice: rango 0.01-99999.99
  Country: regex ^[A-Z]{2}$ (código ISO de 2 letras)
  MaxItems: <=50 por pedido (previene payload abuse)
- Directory.Build.props: NuGetAudit=true (high), NuGetAuditMode=all
  Falla el build si hay dependencias con CVEs High/Critical
- Security Tests (Category=Security):
  GetOrder sin token → 401
  GetOrder de otro usuario → 404 (no 403)
  AdminConfirm con customer token → 403
  Token expirado → 401; Token manipulado → 401
  SQL Injection en email → 422; XSS en ProductName → 422"

git push origin m5.3
```

---

## Checklist final

- [ ] `dotnet build OrderFlow.sln` → sin errores ni warnings de vulnerabilidades
- [ ] `dotnet test --filter "Category=Security"` → todos los tests pasan
- [ ] `curl -I https://localhost:8080/health` → cabeceras de seguridad presentes
- [ ] Login + 11 POSTs seguidos → la 11ª devuelve 429 + Retry-After: 60
- [ ] `POST /api/orders` con `customerEmail: "'; DROP TABLE --"` → 422
- [ ] `GET /api/orders/{id_ajeno}` con token de customer → 404 (no 403)
- [ ] Tabla `orders.AuditLogs` tiene filas tras crear pedidos
- [ ] `dotnet list package --vulnerable` → vacío
- [ ] Commit en rama `m5.3` y push completados

---

## El Módulo 5 completo

```
M5.1 — JWT + OAuth 2.0
  ASP.NET Core Identity en Gateway. JWT HS256 dev / RS256 prod.
  Policies: CustomerOrAdmin, AdminOnly. userId del JWT, no del body.

M5.2 — HTTPS + Secretos
  User Secrets (dev) → Key Vault (prod). DefaultAzureCredential universal.
  Managed Identity: SQL + Service Bus sin passwords. SSL termination en Azure.

M5.3 — OWASP + Controles ← este módulo
  Cabeceras HTTP. SmartRateLimit (userId / IP). Audit Logging persistido.
  Prevención enumeración (404 vs 403). Validaciones de seguridad. NuGetAudit.
```

## OWASP Top 10 — estado final de OrderFlow

| Riesgo | Estado | Mitigación |
|---|---|---|
| A01 Broken Access Control | ✅ | `[Authorize]` + ownership check + 404 para recursos ajenos |
| A02 Cryptographic Failures | ✅ | HTTPS, JWT RS256, Key Vault, TDE en Azure SQL |
| A03 Injection | ✅ | EF Core LINQ (parámetros automáticos), FluentValidation regex |
| A04 Insecure Design | ✅ | Gateway único, Database per Service, JWT corta duración |
| A05 Security Misconfiguration | ✅ | Cabeceras HTTP, TLS 1.2+, MaxRequestBodySize |
| A06 Vulnerable Components | ✅ | NuGetAudit + Dependabot en CI |
| A07 Auth Failures | ✅ | JWT exp corto, brute force detection, rate limiting |
| A08 Data Integrity | ✅ | Integration Events con `record` (inmutables), hash verificado en CI |
| A09 Logging Failures | ✅ | Audit logging persistido, logs [SECURITY] estructurados |
| A10 SSRF | ✅ | No hay fetch de URLs externas en el código de producción |

## Lo que viene en M6.1

`docs/m6.1.md` → Database per Service en profundidad: múltiples DbContext, migrations sin downtime, estrategia Expand-Contract.
