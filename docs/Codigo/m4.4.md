# M4.4 — API Gateway con YARP

## Branch

```bash
git checkout m4.3
git checkout -b m4.4
```

## Objetivo

Añadir `Gateway.API` con YARP como punto de entrada único para los 4 microservicios de OrderFlow. Todo el tráfico externo pasa por el gateway (puerto 8080). Los servicios internos dejan de estar expuestos directamente al cliente. Rate limiting centralizado, transforms de headers y Aspire Service Discovery configurados.

## Qué se construye en este módulo

```
src/Gateway.API/                    ← proyecto nuevo (web)
  ├── Program.cs                    ← ReverseProxy + RateLimiter + pipeline
  ├── appsettings.json              ← configuración YARP (Routes + Clusters)
  └── appsettings.Development.json

infrastructure/OrderFlow.AppHost/
  └── Program.cs                    ← añadir gateway-api (actualizar)

tests/Gateway.API.Tests/            ← proyecto nuevo (opcional)
  └── GatewayRoutingTests.cs
```

---

## Paso 1 — Crear el proyecto Gateway.API

```bash
dotnet new web -n Gateway.API -f net10.0 -o src/Gateway.API
dotnet sln add src/Gateway.API/Gateway.API.csproj

# Solo un paquete necesario
dotnet add src/Gateway.API/Gateway.API.csproj package Yarp.ReverseProxy
dotnet add src/Gateway.API/Gateway.API.csproj package Serilog.AspNetCore

# ServiceDefaults para OpenTelemetry y health checks
dotnet add src/Gateway.API/Gateway.API.csproj reference \
    infrastructure/OrderFlow.ServiceDefaults/OrderFlow.ServiceDefaults.csproj
```

---

## Paso 2 — appsettings.json — configuración de YARP

Reemplazar el contenido de `src/Gateway.API/appsettings.json`:

```json
{
  "Serilog": {
    "MinimumLevel": {
      "Default": "Information",
      "Override": {
        "Microsoft": "Warning",
        "Yarp": "Warning"
      }
    }
  },
  "ReverseProxy": {
    "Routes": {
      "orders-create-route": {
        "ClusterId": "orders-cluster",
        "Match": {
          "Path": "/api/orders",
          "Methods": [ "POST" ]
        },
        "RateLimiterPolicy": "CreateOrder",
        "Transforms": [
          { "RequestHeadersCopy": "true" },
          { "RequestHeader": "X-Gateway-Source", "Set": "orderflow-gateway" }
        ]
      },
      "orders-read-route": {
        "ClusterId": "orders-cluster",
        "Match": {
          "Path": "/api/orders/{**catch-all}",
          "Methods": [ "GET", "POST" ]
        },
        "RateLimiterPolicy": "PerIp",
        "Transforms": [
          { "RequestHeadersCopy": "true" },
          { "RequestHeader": "X-Gateway-Source", "Set": "orderflow-gateway" }
        ]
      },
      "products-route": {
        "ClusterId": "products-cluster",
        "Match": {
          "Path": "/api/products/{**catch-all}"
        },
        "RateLimiterPolicy": "PerIp",
        "Transforms": [
          { "RequestHeadersCopy": "true" }
        ]
      },
      "payments-route": {
        "ClusterId": "payments-cluster",
        "Match": {
          "Path": "/api/payments/{**catch-all}"
        },
        "RateLimiterPolicy": "PerIp",
        "Transforms": [
          { "RequestHeadersCopy": "true" }
        ]
      },
      "notifications-route": {
        "ClusterId": "notifications-cluster",
        "Match": {
          "Path": "/api/notifications/{**catch-all}"
        },
        "RateLimiterPolicy": "PerIp"
      }
    },
    "Clusters": {
      "orders-cluster": {
        "HttpRequest": {
          "Timeout": "00:00:30",
          "Version": "2",
          "VersionPolicy": "RequestVersionOrLower"
        },
        "HealthCheck": {
          "Active": {
            "Enabled": true,
            "Interval": "00:00:30",
            "Timeout": "00:00:05",
            "Policy": "ConsecutiveFailures",
            "Path": "/health"
          }
        },
        "Destinations": {
          "primary": { "Address": "http://orders-api" }
        }
      },
      "products-cluster": {
        "HttpRequest": { "Timeout": "00:00:15" },
        "HealthCheck": {
          "Active": {
            "Enabled": true,
            "Interval": "00:00:30",
            "Timeout": "00:00:05",
            "Policy": "ConsecutiveFailures",
            "Path": "/health"
          }
        },
        "Destinations": {
          "primary": { "Address": "http://products-api" }
        }
      },
      "payments-cluster": {
        "HttpRequest": { "Timeout": "00:00:30" },
        "HealthCheck": {
          "Active": {
            "Enabled": true,
            "Interval": "00:00:30",
            "Timeout": "00:00:05",
            "Policy": "ConsecutiveFailures",
            "Path": "/health"
          }
        },
        "Destinations": {
          "primary": { "Address": "http://payments-api" }
        }
      },
      "notifications-cluster": {
        "HttpRequest": { "Timeout": "00:00:15" },
        "Destinations": {
          "primary": { "Address": "http://notifications-api" }
        }
      }
    }
  }
}
```

Crear `src/Gateway.API/appsettings.Development.json`:

```json
{
  "Serilog": {
    "MinimumLevel": {
      "Default": "Debug",
      "Override": { "Yarp": "Information" }
    }
  }
}
```

---

## Paso 3 — Program.cs del Gateway (fichero completo)

Crear `src/Gateway.API/Program.cs`:

```csharp
using System.Threading.RateLimiting;
using Serilog;
using Serilog.Events;

var builder = WebApplication.CreateBuilder(args);

// ─── ServiceDefaults (OpenTelemetry, health checks base) ─────────────────────
builder.AddServiceDefaults();

// ─── Serilog ─────────────────────────────────────────────────────────────────
builder.Host.UseSerilog((ctx, svc, cfg) => cfg
    .ReadFrom.Configuration(ctx.Configuration)
    .ReadFrom.Services(svc)
    .Enrich.FromLogContext()
    .Enrich.WithProperty("Service", "gateway-api")
    .Enrich.WithMachineName());

// ─── YARP ─────────────────────────────────────────────────────────────────────
builder.Services
    .AddReverseProxy()
    .LoadFromConfig(builder.Configuration.GetSection("ReverseProxy"));

// ─── Rate Limiting ────────────────────────────────────────────────────────────
builder.Services.AddRateLimiter(options =>
{
    // Política general: 100 peticiones por minuto por IP (ventana deslizante)
    options.AddSlidingWindowLimiter("PerIp", config =>
    {
        config.Window              = TimeSpan.FromMinutes(1);
        config.PermitLimit         = 100;
        config.SegmentsPerWindow   = 4;   // Ventana dividida en 4 segmentos de 15s
        config.QueueLimit          = 20;
        config.QueueProcessingOrder = QueueProcessingOrder.OldestFirst;
    });

    // Política estricta para creación de pedidos: 10 por minuto
    options.AddFixedWindowLimiter("CreateOrder", config =>
    {
        config.Window      = TimeSpan.FromMinutes(1);
        config.PermitLimit = 10;
        config.QueueLimit  = 0;  // Sin cola — rechazo inmediato
    });

    // Respuesta cuando se supera el límite
    options.RejectionStatusCode = StatusCodes.Status429TooManyRequests;
    options.OnRejected = async (context, ct) =>
    {
        context.HttpContext.Response.Headers.RetryAfter = "60";
        context.HttpContext.Response.ContentType = "application/json";
        await context.HttpContext.Response.WriteAsJsonAsync(new
        {
            title  = "Too Many Requests",
            status = 429,
            detail = "Rate limit exceeded. Please try again in 60 seconds.",
            type   = "https://orderflow.api/errors/rate-limit"
        }, ct);
    };
});

// ─── CORS ─────────────────────────────────────────────────────────────────────
builder.Services.AddCors(options =>
{
    options.AddPolicy("TechShopFrontend", policy =>
    {
        policy.WithOrigins(
                "https://techshop.es",
                "https://www.techshop.es",
                builder.Environment.IsDevelopment() ? "http://localhost:3000" : string.Empty)
              .AllowAnyMethod()
              .AllowAnyHeader()
              .WithExposedHeaders("X-Correlation-Id", "Location", "Retry-After");
    });
});

// ─── Health checks ────────────────────────────────────────────────────────────
builder.Services.AddHealthChecks();

// ─── Build ────────────────────────────────────────────────────────────────────
var app = builder.Build();

// Pipeline — el orden importa
// 1. CORS — antes del rate limiter para que los preflight OPTIONS pasen
app.UseCors("TechShopFrontend");

// 2. CorrelationId — antes del logging para que aparezca en los logs
// El CorrelationIdMiddleware ya existe en el proyecto (M2.3)
// Si no existe, añadir inline:
app.Use(async (context, next) =>
{
    var correlationId = context.Request.Headers["X-Correlation-Id"].FirstOrDefault()
        ?? System.Diagnostics.Activity.Current?.TraceId.ToString()
        ?? Guid.NewGuid().ToString("N");

    context.Items["CorrelationId"] = correlationId;
    context.Response.Headers["X-Correlation-Id"] = correlationId;
    await next();
});

// 3. Serilog request logging
app.UseSerilogRequestLogging(opts =>
{
    opts.GetLevel = (ctx, _, _) =>
        ctx.Request.Path.StartsWithSegments("/health")
            ? LogEventLevel.Verbose
            : LogEventLevel.Information;
    opts.EnrichDiagnosticContext = (diag, ctx) =>
    {
        diag.Set("CorrelationId", ctx.Items["CorrelationId"]?.ToString());
        diag.Set("ClientIp", ctx.Connection.RemoteIpAddress?.ToString());
    };
});

// 4. Rate Limiting — ANTES de MapReverseProxy
app.UseRateLimiter();

// 5. YARP — el proxy inverso
app.MapReverseProxy();

// 6. Health y alive del propio gateway
app.MapDefaultEndpoints();

app.Run();
```

---

## Paso 4 — Actualizar AppHost

Actualizar `infrastructure/OrderFlow.AppHost/Program.cs`:

```csharp
var builder = DistributedApplication.CreateBuilder(args);

var sqlServer = builder.AddConnectionString("sqlserver");
var messaging  = builder.AddConnectionString("messaging");

var ordersDb        = sqlServer.AddDatabase("OrdersDb");
var productsDb      = sqlServer.AddDatabase("ProductsDb");
var notificationsDb = sqlServer.AddDatabase("NotificationsDb");
var paymentsDb      = sqlServer.AddDatabase("PaymentsDb");

var products = builder.AddProject<Projects.Products_API>("products-api")
    .WithReference(productsDb)
    .WithReference(messaging);

var notifications = builder.AddProject<Projects.Notifications_API>("notifications-api")
    .WithReference(notificationsDb)
    .WithReference(messaging);

var payments = builder.AddProject<Projects.Payments_API>("payments-api")
    .WithReference(paymentsDb)
    .WithReference(messaging);

var orders = builder.AddProject<Projects.Orders_API>("orders-api")
    .WithReference(ordersDb)
    .WithReference(messaging)
    .WithReference(products);

// Gateway — punto de entrada único para todo el tráfico externo
var gateway = builder.AddProject<Projects.Gateway_API>("gateway-api")
    .WithReference(orders)
    .WithReference(products)
    .WithReference(payments)
    .WithReference(notifications);

builder.Build().Run();
```

---

## Paso 5 — El fichero .http unificado

A partir de M4.4, el developer trabaja con **una sola URL**. Actualizar `src/Gateway.API/Gateway.API.http`:

```http
# OrderFlow — Todo el tráfico pasa por el Gateway
# Antes: 4 puertos distintos. Ahora: solo :8080
@api         = https://localhost:8080
@productId   = REEMPLAZAR_CON_ID_REAL
@orderId     = REEMPLAZAR_CON_ID_REAL

### ─── HEALTH ──────────────────────────────────────────────────────────────────
GET {{api}}/health
### → {"status":"Healthy"} — health del propio gateway

### ─── PRODUCTOS ────────────────────────────────────────────────────────────────
GET {{api}}/api/products
### → YARP enruta a products-api:7200/api/products

GET {{api}}/api/products/{{productId}}

POST {{api}}/api/products
Content-Type: application/json
{
  "name": "MacBook Pro 16\" M4",
  "description": "El portátil más potente de Apple",
  "price": 3999.99,
  "currency": "EUR",
  "initialStock": 10,
  "categoryId": "c1b2a3d4-e5f6-0123-4567-89abcdef0123"
}

### ─── PEDIDOS ───────────────────────────────────────────────────────────────────
GET {{api}}/api/orders

GET {{api}}/api/orders/{{orderId}}

### Ver estado de la Saga
GET {{api}}/api/orders/{{orderId}}/saga-state

### Crear pedido (rate limit: 10/min — política CreateOrder)
POST {{api}}/api/orders
Content-Type: application/json
{
  "customerId":    "3fa85f64-5717-4562-b3fc-2c963f66afa6",
  "customerEmail": "john@techshop.es",
  "items": [{
    "productId":   "{{productId}}",
    "productName": "MacBook Pro 16\" M4",
    "quantity": 1,
    "unitPrice":   3999.99,
    "currency":    "EUR"
  }],
  "shippingAddress": {
    "street":  "Gran Vía 28",
    "city":    "Madrid",
    "zipCode": "28013",
    "country": "ES"
  }
}
### → YARP enruta a orders-api:7153/api/orders → 201 Created

POST {{api}}/api/orders/{{orderId}}/confirm
POST {{api}}/api/orders/{{orderId}}/cancel
Content-Type: application/json
{ "reason": "Customer changed their mind" }

### ─── TEST RATE LIMIT ─────────────────────────────────────────────────────────
### Ejecutar más de 10 veces seguidas → 429 Too Many Requests + Retry-After: 60
POST {{api}}/api/orders
Content-Type: application/json
{}
```

---

## Paso 6 — Verificar compilación y arranque

```bash
dotnet build OrderFlow.sln
dotnet run --project infrastructure/OrderFlow.AppHost
```

El dashboard de Aspire debe mostrar **5 servicios**:

```
● gateway-api         Running   https://localhost:8080  ← NUEVO
● orders-api          Running   https://localhost:7153
● products-api        Running   https://localhost:7200
● payments-api        Running   (worker)
● notifications-api   Running   (worker)
```

### Verificar el routing

```http
### Antes (M4.3) → ahora solo necesitas el gateway:
GET https://localhost:8080/api/products
# → YARP → products-api/api/products → 200 OK

POST https://localhost:8080/api/orders
# → YARP → orders-api/api/orders → 201 Created + Saga inicia
```

### Verificar el CorrelationId propagado

En los logs de Aspire (Structured), la misma petición debe verse en **gateway-api** y en **orders-api** con el mismo `CorrelationId`. YARP lo propaga gracias a `RequestHeadersCopy: true`.

### Verificar el rate limit

Ejecutar el POST de crear pedido 11 veces seguidas desde el fichero `.http`. La respuesta 11 debe ser:

```
HTTP/1.1 429 Too Many Requests
Retry-After: 60
Content-Type: application/json

{
  "title": "Too Many Requests",
  "status": 429,
  "detail": "Rate limit exceeded. Please try again in 60 seconds."
}
```

---

## Paso 7 — Commit y push

```bash
git add .
git commit -m "m4.4: API Gateway con YARP — punto de entrada único para OrderFlow

- Gateway.API: proyecto web mínimo con Yarp.ReverseProxy
- appsettings.json: Routes + Clusters para 4 servicios
  Routes separadas por método HTTP (POST crear vs GET/POST leer)
  Transforms: RequestHeadersCopy + X-Gateway-Source header
  Health checks activos (GET /health cada 30s) para orders y products
  Timeouts: 30s orders/payments, 15s products/notifications
- Rate Limiting centralizado (no en cada servicio):
  'PerIp': 100 peticiones/min ventana deslizante → lecturas y reads
  'CreateOrder': 10 peticiones/min ventana fija → POST /api/orders
  Respuesta 429 con Retry-After: 60 y ProblemDetails JSON
- CORS: orígenes permitidos techshop.es + localhost:3000 (dev)
  ExposedHeaders: X-Correlation-Id, Location, Retry-After
- CorrelationId propagado inline (reutiliza el patrón de M2.3)
- Serilog con EnrichDiagnosticContext: CorrelationId + ClientIp
- AppHost: añadir gateway-api con referencias a los 4 servicios
  Puerto 8080 — único punto de entrada externo
- .http unificado: @api = https://localhost:8080 para todo
- El cliente ya no necesita conocer 4 puertos distintos"

git push origin m4.4
```

---

## Checklist final

- [ ] `dotnet build OrderFlow.sln` → sin errores
- [ ] 5 servicios en dashboard de Aspire (gateway en :8080)
- [ ] `GET https://localhost:8080/api/products` → 200 OK (YARP enruta)
- [ ] `POST https://localhost:8080/api/orders` → 201 + Saga inicia
- [ ] 11 POSTs seguidos → respuesta 11 devuelve 429 + Retry-After
- [ ] Logs de gateway-api y orders-api con mismo CorrelationId en Aspire
- [ ] `GET https://localhost:8080/health` → 200 Healthy
- [ ] Commit en rama `m4.4` y push completados

---

## El Módulo 4 completo

```
M4.1 — HTTP Síncrono
  Orders → Products: HttpClientFactory + Polly + CorrelationId

M4.2 — Mensajería Asíncrona
  RabbitMQ + MassTransit + Notifications.API + OrderFlow.Contracts

M4.3 — Saga Pattern
  Payments.API + OrderSaga + compensación automática

M4.4 — API Gateway ← este módulo
  YARP · Rate Limiting · Transforms · Puerto único :8080
```

## Preview JWT en M5.1

El hueco ya está preparado en Program.cs. En M5.1 se añadirá antes de `app.UseRateLimiter()`:

```csharp
// M5.1 — añadir aquí
builder.Services
    .AddAuthentication(JwtBearerDefaults.AuthenticationScheme)
    .AddJwtBearer(options =>
    {
        options.Authority = "https://auth.techshop.es"; // Keycloak
        options.Audience  = "orderflow-gateway";
    });

builder.Services.AddAuthorization();

// En el pipeline — antes del rate limiter:
app.UseAuthentication();
app.UseAuthorization();
```

Gracias a `RequestHeadersCopy: true`, el header `Authorization: Bearer eyJ...` se propaga automáticamente a todos los servicios internos. Los servicios extraen los claims sin revalidar el token.

## Lo que viene en M5.1

`docs/m5.1.md` → JWT + OAuth 2.0 con Keycloak. El gateway valida el token una vez. Los servicios internos confían en el gateway.
