# M2.2 — Inyección de Dependencias y Configuración Avanzada

## Branch

```bash
git checkout m2.1
git checkout -b m2.2
```

## Objetivo

Orders.API con DI bien configurada (lifetimes correctos, ValidateOnBuild), patrón Options con validación en startup, configuración por entorno, User Secrets para credenciales locales, y Health Checks para SQL Server y RabbitMQ visibles en el dashboard de Aspire.

## Qué se añade en este módulo

```
src/Orders.API/
├── Orders.API.csproj                         ← añadir paquetes HC + User Secrets
├── Program.cs                                ← DI validation + Options + HC + Key Vault
├── appsettings.json                          ← añadir sección "Orders"
├── appsettings.Development.json              ← overrides de desarrollo
├── appsettings.Production.json               ← overrides de producción (nuevo)
└── Application/
    └── Settings/
        ├── OrdersSettings.cs                 ← clase de configuración tipada (nuevo)
        └── OrdersSettingsValidator.cs        ← validador custom (nuevo)
```

---

## Paso 1 — Instalar paquetes NuGet

```bash
# Health checks para SQL Server y RabbitMQ
dotnet add src/Orders.API/Orders.API.csproj package AspNetCore.HealthChecks.SqlServer
dotnet add src/Orders.API/Orders.API.csproj package AspNetCore.HealthChecks.Rabbitmq

# Azure Key Vault (solo se usa en producción, pero la referencia se añade ahora)
dotnet add src/Orders.API/Orders.API.csproj package Azure.Extensions.AspNetCore.Configuration.Secrets
dotnet add src/Orders.API/Orders.API.csproj package Azure.Identity
```

---

## Paso 2 — Orders.API.csproj (fichero completo)

Reemplazar el contenido completo de `src/Orders.API/Orders.API.csproj`:

```xml
<Project Sdk="Microsoft.NET.Sdk.Web">

  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
    <RootNamespace>Orders.API</RootNamespace>
    <AssemblyName>Orders.API</AssemblyName>
    <UserSecretsId>orderflow-orders-api</UserSecretsId>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="AspNetCore.HealthChecks.SqlServer" Version="9.*" />
    <PackageReference Include="AspNetCore.HealthChecks.Rabbitmq" Version="9.*" />
    <PackageReference Include="Azure.Extensions.AspNetCore.Configuration.Secrets" Version="1.*" />
    <PackageReference Include="Azure.Identity" Version="1.*" />
    <PackageReference Include="Microsoft.AspNetCore.OpenApi" Version="10.*" />
  </ItemGroup>

  <ItemGroup>
    <ProjectReference Include="..\..\infrastructure\OrderFlow.ServiceDefaults\OrderFlow.ServiceDefaults.csproj" />
  </ItemGroup>

</Project>
```

---

## Paso 3 — Crear la estructura de carpetas

```bash
mkdir -p src/Orders.API/Application/Settings
```

---

## Paso 4 — OrdersSettings.cs (fichero completo)

Crear `src/Orders.API/Application/Settings/OrdersSettings.cs`:

```csharp
using System.ComponentModel.DataAnnotations;

namespace Orders.API.Application.Settings;

/// <summary>
/// Configuración tipada de Orders.API.
/// Se registra con el patrón Options y se valida en startup.
/// Sección en appsettings.json: "Orders"
/// </summary>
public class OrdersSettings
{
    public const string SectionName = "Orders";

    [Range(1, 100, ErrorMessage = "MaxLinesPerOrder debe estar entre 1 y 100")]
    public int MaxLinesPerOrder { get; set; } = 50;

    [Range(0.01, double.MaxValue,
        ErrorMessage = "MinOrderAmount debe ser mayor que 0")]
    public decimal MinOrderAmount { get; set; } = 10.00m;

    public bool AllowBackorders { get; set; } = false;

    [Range(1, 3650,
        ErrorMessage = "RetentionDays debe estar entre 1 y 3650 días")]
    public int RetentionDays { get; set; } = 365;

    [Required]
    [StringLength(3, MinimumLength = 3,
        ErrorMessage = "Currency debe ser un código ISO de 3 letras")]
    public string Currency { get; set; } = "EUR";
}
```

---

## Paso 5 — OrdersSettingsValidator.cs (fichero completo)

Crear `src/Orders.API/Application/Settings/OrdersSettingsValidator.cs`:

```csharp
using Microsoft.Extensions.Options;

namespace Orders.API.Application.Settings;

/// <summary>
/// Validador custom de OrdersSettings.
/// Verifica reglas de negocio que no caben en DataAnnotations simples.
/// Se ejecuta junto con ValidateDataAnnotations() en startup.
/// </summary>
public class OrdersSettingsValidator : IValidateOptions<OrdersSettings>
{
    public ValidateOptionsResult Validate(string? name, OrdersSettings options)
    {
        var errors = new List<string>();

        // Regla de negocio: si no se permiten backorders,
        // el importe mínimo debe ser al menos 5€
        if (!options.AllowBackorders && options.MinOrderAmount < 5.00m)
        {
            errors.Add(
                "Cuando AllowBackorders es false, " +
                "MinOrderAmount debe ser al menos 5.00");
        }

        // Regla de compliance: retención mínima de 90 días
        if (options.RetentionDays < 90)
        {
            errors.Add(
                "RetentionDays debe ser al menos 90 días " +
                "por requisitos de compliance");
        }

        return errors.Count > 0
            ? ValidateOptionsResult.Fail(errors)
            : ValidateOptionsResult.Success;
    }
}
```

---

## Paso 6 — Program.cs de Orders.API (fichero completo)

Reemplazar el contenido completo de `src/Orders.API/Program.cs`:

```csharp
using Azure.Identity;
using Microsoft.AspNetCore.Diagnostics.HealthChecks;
using Microsoft.Extensions.Diagnostics.HealthChecks;
using Microsoft.Extensions.Options;
using Orders.API.Application.Settings;
using RabbitMQ.Client;

var builder = WebApplication.CreateBuilder(args);

// ─── Aspire ServiceDefaults ───────────────────────────────────────────────────
builder.AddServiceDefaults();

// ─── Validación del DI container en desarrollo ───────────────────────────────
builder.Host.UseDefaultServiceProvider(options =>
{
    options.ValidateScopes = builder.Environment.IsDevelopment();
    options.ValidateOnBuild = builder.Environment.IsDevelopment();
});

// ─── Patrón Options con validación en startup ─────────────────────────────────
builder.Services
    .AddOptions<OrdersSettings>()
    .BindConfiguration(OrdersSettings.SectionName)
    .ValidateDataAnnotations()
    .ValidateOnStart();

builder.Services
    .AddSingleton<IValidateOptions<OrdersSettings>, OrdersSettingsValidator>();

// ─── RabbitMQ connection (singleton) para Health Check ───────────────────────
// AddRabbitMQ() ya no acepta connection string — requiere IConnection registrado en DI
builder.Services.AddSingleton<IConnection>(_ =>
{
    var factory = new ConnectionFactory
    {
        Uri = new Uri(builder.Configuration.GetConnectionString("messaging")!)
    };
    return factory.CreateConnectionAsync().GetAwaiter().GetResult();
});

// ─── Health Checks ─────────────────────────────────────────────────────────────
builder.Services.AddHealthChecks()
    // SQL Server — Unhealthy si no responde (sin BD no podemos procesar pedidos)
    .AddSqlServer(
        connectionString: builder.Configuration.GetConnectionString("sqlserver")!,
        healthQuery: "SELECT 1",
        name: "orders-db",
        failureStatus: HealthStatus.Unhealthy,
        tags: ["ready", "db"])
    // RabbitMQ — Degraded si no responde (podemos leer pero no publicar eventos)
    // Usa el IConnection singleton registrado arriba
    .AddRabbitMQ(
        name: "rabbitmq",
        failureStatus: HealthStatus.Degraded,
        tags: ["ready", "messaging"]);

// ─── Servicios ────────────────────────────────────────────────────────────────
builder.Services.AddControllers();
builder.Services.AddOpenApi();

// ─── Azure Key Vault en producción (Managed Identity, sin credenciales en código) ─
if (!builder.Environment.IsDevelopment())
{
    var keyVaultUri = builder.Configuration["KeyVaultUri"];
    if (!string.IsNullOrEmpty(keyVaultUri))
    {
        builder.Configuration.AddAzureKeyVault(
            new Uri(keyVaultUri),
            new DefaultAzureCredential());
    }
}

var app = builder.Build();

// ─── Pipeline ─────────────────────────────────────────────────────────────────
if (app.Environment.IsDevelopment())
    app.MapOpenApi();

app.UseHttpsRedirection();
app.UseAuthorization();
app.MapControllers();

// Health check endpoints con granularidad por tags
app.MapHealthChecks("/health/ready", new HealthCheckOptions
{
    Predicate = check => check.Tags.Contains("ready")
});

app.MapDefaultEndpoints(); // /health y /alive — de ServiceDefaults

app.Run();
```

---

## Paso 7 — appsettings.json de Orders.API (fichero completo)

Reemplazar `src/Orders.API/appsettings.json`:

```json
{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",
  "Orders": {
    "MaxLinesPerOrder": 50,
    "MinOrderAmount": 10.00,
    "AllowBackorders": false,
    "RetentionDays": 365,
    "Currency": "EUR"
  }
}
```

---

## Paso 8 — appsettings.Development.json de Orders.API (fichero completo)

Reemplazar `src/Orders.API/appsettings.Development.json`:

```json
{
  "Logging": {
    "LogLevel": {
      "Default": "Debug",
      "Microsoft.AspNetCore": "Information",
      "Microsoft.EntityFrameworkCore.Database.Command": "Information"
    }
  },
  "Orders": {
    "MaxLinesPerOrder": 50,
    "MinOrderAmount": 0.01,
    "AllowBackorders": true,
    "RetentionDays": 90,
    "Currency": "EUR"
  }
}
```

> **Diferencias respecto a producción:** `MinOrderAmount: 0.01` permite probar pedidos pequeños, `AllowBackorders: true` facilita pruebas, `RetentionDays: 90` es el mínimo de compliance, logs en `Debug` para ver más detalle.

---

## Paso 9 — appsettings.Production.json de Orders.API (fichero nuevo)

Crear `src/Orders.API/appsettings.Production.json`:

```json
{
  "Logging": {
    "LogLevel": {
      "Default": "Warning",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "Orders": {
    "MaxLinesPerOrder": 50,
    "MinOrderAmount": 10.00,
    "AllowBackorders": false,
    "RetentionDays": 365,
    "Currency": "EUR"
  }
}
```

> **Solo Warning+** en producción: menos ruido, menos coste en Application Insights.
> **Connection strings NO van aquí** — van en User Secrets (desarrollo) o Azure Key Vault (producción).

---

## Paso 10 — Inicializar User Secrets

```bash
dotnet user-secrets init --project src/Orders.API

# Añadir las connection strings locales (fuera del repo)
dotnet user-secrets set "ConnectionStrings:OrdersDb" \
  "Server=(localdb)\\MSSQLLocalDB;Database=OrdersDb;Trusted_Connection=true;TrustServerCertificate=true;" \
  --project src/Orders.API

dotnet user-secrets set "ConnectionStrings:messaging" \
  "amqp://guest:guest@localhost:5672" \
  --project src/Orders.API
```

> Los User Secrets se guardan en `%APPDATA%\Microsoft\UserSecrets\orderflow-orders-api\secrets.json`.
> Nunca se commitean al repositorio. En Visual Studio: click derecho en el proyecto → "Manage User Secrets".

---

## Paso 11 — Verificar que OrdersController puede usar IOptions

Actualizar `src/Orders.API/Controllers/OrdersController.cs` para demostrar que IOptions funciona:

```csharp
using Microsoft.AspNetCore.Mvc;
using Microsoft.Extensions.Options;
using Orders.API.Application.Settings;

namespace Orders.API.Controllers;

[ApiController]
[Route("api/[controller]")]
public class OrdersController : ControllerBase
{
    private readonly ILogger<OrdersController> _logger;
    private readonly OrdersSettings _settings;

    public OrdersController(
        ILogger<OrdersController> logger,
        IOptions<OrdersSettings> settings)
    {
        _logger = logger;
        _settings = settings.Value;
    }

    [HttpGet]
    public IActionResult GetAll()
    {
        _logger.LogInformation("Getting all orders");
        return Ok(new List<object>());
    }

    [HttpGet("{id:guid}")]
    public IActionResult GetById(Guid id)
    {
        _logger.LogInformation("Getting order {OrderId}", id);
        return NotFound();
    }

    [HttpPost]
    public IActionResult Create([FromBody] object request)
    {
        _logger.LogInformation(
            "Creating new order. MaxLines: {MaxLines}, Currency: {Currency}",
            _settings.MaxLinesPerOrder,
            _settings.Currency);
        return Created($"/api/orders/{Guid.NewGuid()}", null);
    }
}
```

---

## Paso 12 — Verificar que compila

```bash
dotnet build OrderFlow.slnx
```

Sin errores. Si hay warnings de nullable, resolverlos.

---

## Paso 13 — Verificar ValidateOnStart

Para comprobar que la validación en startup funciona, modificar temporalmente `appsettings.Development.json` poniendo un valor inválido:

```json
"Orders": {
  "MaxLinesPerOrder": -5
}
```

Al ejecutar `dotnet run --project infrastructure/OrderFlow.AppHost` debe fallar inmediatamente con:

```
System.OptionsValidationException: DataAnnotation validation failed
for 'OrdersSettings' members: 'MaxLinesPerOrder' with the error:
'MaxLinesPerOrder debe estar entre 1 y 100'.
```

Restaurar el valor correcto (`50`) antes de continuar.

---

## Paso 14 — Arrancar con Aspire y verificar Health Checks

```bash
dotnet run --project infrastructure/OrderFlow.AppHost
```

En el Dashboard de Aspire verificar:

| Comprobación | Resultado esperado |
|---|---|
| `orders-api` estado | **Running** ✅ |
| `GET /health` | `200 OK` con estado `Healthy` o `Degraded` |
| `GET /health/ready` | `200 OK` solo con checks marcados `ready` |
| `GET /alive` | `200 OK` |
| `GET /openapi/v1.json` | spec OpenAPI válida |

> Si RabbitMQ no está arrancado en local, `/health` devolverá `Degraded` (no `Unhealthy`) — correcto, ese es el comportamiento esperado.

Ejemplo de respuesta de `/health` con todo sano:

```json
{
  "status": "Healthy",
  "totalDuration": "00:00:00.0234567",
  "entries": {
    "orders-db": { "status": "Healthy", "duration": "00:00:00.0123" },
    "rabbitmq":  { "status": "Healthy", "duration": "00:00:00.0089" }
  }
}
```

---

## Paso 15 — Estructura final verificada

```
src/Orders.API/
├── Orders.API.csproj                    ✅ HC packages + UserSecretsId
├── Program.cs                           ✅ ValidateOnBuild + Options + HC + KeyVault
├── Controllers/
│   └── OrdersController.cs             ✅ inyecta IOptions<OrdersSettings>
├── Properties/
│   └── launchSettings.json             ✅ sin cambios desde M2.1
├── Application/
│   └── Settings/
│       ├── OrdersSettings.cs           ✅ DataAnnotations + defaults
│       └── OrdersSettingsValidator.cs  ✅ reglas de negocio cross-field
├── appsettings.json                    ✅ con sección "Orders"
├── appsettings.Development.json        ✅ overrides de desarrollo
├── appsettings.Production.json         ✅ overrides de producción (nuevo)
└── Orders.API.http                     ✅ sin cambios
```

---

## Paso 16 — Commit y push

```bash
git add .
git commit -m "m2.2: DI avanzada, Options pattern y Health Checks

- OrdersSettings con DataAnnotations y valores por defecto
- OrdersSettingsValidator con reglas de negocio cross-field
- ValidateOnBuild + ValidateOnStart: fallo rápido en startup
- appsettings por entorno: Development (Debug, permisivo) + Production (Warning, estricto)
- User Secrets inicializados (connection strings fuera del repo)
- Health checks: SQL Server (Unhealthy) + RabbitMQ (Degraded)
- Endpoint /health/ready con tags para readiness probe
- Azure Key Vault en producción con DefaultAzureCredential
- OrdersController inyecta IOptions<OrdersSettings>"

git push origin m2.2
```

---

## Checklist final

- [ ] `dotnet build OrderFlow.slnx` → sin errores ni warnings
- [ ] Forzar valor inválido en appsettings y verificar que el arranque falla con mensaje claro
- [ ] `dotnet run --project infrastructure/OrderFlow.AppHost` → Dashboard arranca
- [ ] `orders-api` aparece como **Running** en el Dashboard
- [ ] `GET /health` → `200 OK` con JSON de estados
- [ ] `GET /health/ready` → `200 OK` con checks tagged `ready`
- [ ] `GET /alive` → `200 OK`
- [ ] `dotnet user-secrets list --project src/Orders.API` → muestra las connection strings
- [ ] Ningún secreto en appsettings.json ni en el repo
- [ ] Commit en rama `m2.2` y push completados

Si algún punto falla, resolverlo antes de avanzar. M2.3 añade Serilog y OpenTelemetry encima de esta base.

---

## Lo que viene en M2.3

`docs/m2.3.md` → Logging y observabilidad:
- Serilog con structured logging y sinks configurables
- Middleware de Correlation IDs para rastrear peticiones entre servicios
- OpenTelemetry: traces y métricas custom
- Todo visible en el dashboard de Aspire
