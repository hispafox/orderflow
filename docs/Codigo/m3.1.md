# M3.1 — Diseño de un Microservicio: del Dominio al Código

## Branch

```bash
git checkout m2.4
git checkout -b m3.1
```

## Objetivo

Implementar el modelo de dominio completo de Orders.API con DDD táctico, C# 14 y Clean Architecture. Al terminar, el dominio encapsula todas las reglas de negocio de TechShop, no conoce EF Core ni ninguna infraestructura, y es totalmente testeable sin mocks.

## Qué se construye en este módulo

```
src/Orders.API/
├── Orders.API.csproj                                    ← añadir EF Core SqlServer + Design
├── Program.cs                                           ← añadir AddOrdersInfrastructure
├── Domain/
│   ├── Guards.cs                                        ← helper de validaciones (nuevo)
│   ├── Exceptions/
│   │   └── DomainException.cs                          ← excepción base del dominio (nuevo)
│   ├── ValueObjects/
│   │   ├── Money.cs                                     ← importe con moneda (nuevo)
│   │   ├── Address.cs                                   ← dirección de envío (nuevo)
│   │   └── OrderStatus.cs                               ← estado con transiciones (nuevo)
│   ├── Entities/
│   │   ├── OrderLine.cs                                 ← entity hija, solo accesible desde Order (nuevo)
│   │   └── Order.cs                                     ← Aggregate Root (nuevo)
│   ├── Events/
│   │   ├── IDomainEvent.cs                              ← interface base (nuevo)
│   │   ├── OrderCreatedEvent.cs                         ← (nuevo)
│   │   ├── OrderConfirmedEvent.cs                       ← (nuevo)
│   │   ├── OrderCancelledEvent.cs                       ← (nuevo)
│   │   ├── OrderShippedEvent.cs                         ← (nuevo)
│   │   └── OrderDeliveredEvent.cs                       ← (nuevo)
│   └── Interfaces/
│       └── IOrderRepository.cs                          ← interfaz en el dominio (nuevo)
└── Infrastructure/
    ├── Persistence/
    │   ├── OrderDbContext.cs                             ← EF Core DbContext (nuevo)
    │   ├── SqlOrderRepository.cs                        ← implementa IOrderRepository (nuevo)
    │   └── Migrations/                                  ← generado por EF Core
    └── ServiceCollectionExtensions.cs                   ← registro en DI (nuevo)

tests/Orders.API.Tests/
├── Shared/Builders/OrderBuilder.cs                      ← actualizar con nueva firma
└── Unit/Domain/                                         ← actualizar tests con dominio real
```

---

## Paso 1 — Instalar paquetes NuGet

```bash
# EF Core para SQL Server
dotnet add src/Orders.API/Orders.API.csproj package Microsoft.EntityFrameworkCore.SqlServer
dotnet add src/Orders.API/Orders.API.csproj package Microsoft.EntityFrameworkCore.Design
```

---

## Paso 2 — Orders.API.csproj (fichero completo)

Reemplazar el contenido de `src/Orders.API/Orders.API.csproj`:

```xml
<Project Sdk="Microsoft.NET.Sdk.Web">

  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
    <RootNamespace>Orders.API</RootNamespace>
    <AssemblyName>Orders.API</AssemblyName>
    <UserSecretsId>orderflow-orders-api</UserSecretsId>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="AspNetCore.HealthChecks.SqlServer" Version="9.*" />
    <PackageReference Include="AspNetCore.HealthChecks.RabbitMQ" Version="9.*" />
    <PackageReference Include="Azure.Extensions.AspNetCore.Configuration.Secrets" Version="1.*" />
    <PackageReference Include="Azure.Identity" Version="1.*" />
    <PackageReference Include="Microsoft.EntityFrameworkCore.SqlServer" Version="10.*" />
    <PackageReference Include="Microsoft.EntityFrameworkCore.Design" Version="10.*">
      <PrivateAssets>all</PrivateAssets>
      <IncludeAssets>runtime; build; native; contentfiles; analyzers; buildtransitive</IncludeAssets>
    </PackageReference>
    <PackageReference Include="Serilog.AspNetCore" Version="9.*" />
    <PackageReference Include="Serilog.Sinks.Console" Version="6.*" />
    <PackageReference Include="Serilog.Enrichers.Environment" Version="3.*" />
    <PackageReference Include="Serilog.Enrichers.Thread" Version="4.*" />
    <PackageReference Include="Serilog.Enrichers.Process" Version="3.*" />
  </ItemGroup>

  <ItemGroup>
    <ProjectReference Include="..\..\infrastructure\OrderFlow.ServiceDefaults\OrderFlow.ServiceDefaults.csproj" />
  </ItemGroup>

</Project>
```

---

## Paso 3 — Crear estructura de carpetas del dominio

```bash
mkdir -p src/Orders.API/Domain/Exceptions
mkdir -p src/Orders.API/Domain/ValueObjects
mkdir -p src/Orders.API/Domain/Entities
mkdir -p src/Orders.API/Domain/Events
mkdir -p src/Orders.API/Domain/Interfaces
mkdir -p src/Orders.API/Infrastructure/Persistence
```

---

## Paso 4 — DomainException.cs (fichero completo)

Crear `src/Orders.API/Domain/Exceptions/DomainException.cs`:

```csharp
namespace Orders.API.Domain.Exceptions;

/// <summary>
/// Excepción lanzada cuando se viola una regla de negocio del dominio.
/// No debe confundirse con errores de infraestructura o de validación de entrada.
/// Los handlers de la capa de aplicación la capturan y la convierten en 422 Unprocessable Entity.
/// </summary>
public class DomainException : Exception
{
    public DomainException(string message) : base(message) { }

    public DomainException(string message, Exception innerException)
        : base(message, innerException) { }
}
```

---

## Paso 5 — Guards.cs (fichero completo)

Crear `src/Orders.API/Domain/Guards.cs`:

```csharp
using Orders.API.Domain.Exceptions;

namespace Orders.API.Domain;

/// <summary>
/// Helper estático con guard clauses para validar parámetros en el dominio.
/// Cada método retorna el valor validado para permitir asignación directa.
/// </summary>
public static class Guard
{
    public static string NotNullOrEmpty(string value, string paramName)
    {
        if (string.IsNullOrWhiteSpace(value))
            throw new DomainException($"{paramName} cannot be null or empty");
        return value;
    }

    public static T NotNull<T>(T? value, string paramName) where T : class
    {
        if (value is null)
            throw new DomainException($"{paramName} cannot be null");
        return value;
    }

    public static int Positive(int value, string paramName)
    {
        if (value <= 0)
            throw new DomainException(
                $"{paramName} must be positive. Got: {value}");
        return value;
    }

    public static decimal NonNegative(decimal value, string paramName)
    {
        if (value < 0)
            throw new DomainException(
                $"{paramName} cannot be negative. Got: {value}");
        return value;
    }
}
```

---

## Paso 6 — Money.cs (fichero completo)

Crear `src/Orders.API/Domain/ValueObjects/Money.cs`:

```csharp
using Orders.API.Domain.Exceptions;

namespace Orders.API.Domain.ValueObjects;

/// <summary>
/// Value Object que representa un importe con su moneda.
/// Inmutable por diseño: sealed record.
/// Igualdad por valor automática gracias al record.
/// </summary>
public sealed record Money
{
    public decimal Amount { get; }
    public string Currency { get; }

    public Money(decimal amount, string currency)
    {
        if (amount < 0)
            throw new DomainException(
                $"Money amount cannot be negative. Got: {amount}");

        if (string.IsNullOrWhiteSpace(currency) || currency.Length != 3)
            throw new DomainException(
                $"Currency must be a 3-letter ISO code. Got: '{currency}'");

        Amount   = amount;
        Currency = currency.ToUpperInvariant();
    }

    public Money Add(Money other)
    {
        EnsureSameCurrency(other);
        return new Money(Amount + other.Amount, Currency);
    }

    public Money Subtract(Money other)
    {
        EnsureSameCurrency(other);
        var result = Amount - other.Amount;
        if (result < 0)
            throw new DomainException("Money cannot be negative after subtraction");
        return new Money(result, Currency);
    }

    public Money Multiply(int quantity)
    {
        if (quantity <= 0)
            throw new DomainException("Quantity must be positive");
        return new Money(Amount * quantity, Currency);
    }

    private void EnsureSameCurrency(Money other)
    {
        if (Currency != other.Currency)
            throw new DomainException(
                $"Cannot operate on different currencies: {Currency} and {other.Currency}");
    }

    public static Money Zero(string currency) => new(0, currency);

    public override string ToString() => $"{Amount:F2} {Currency}";
}
```

---

## Paso 7 — Address.cs (fichero completo)

Crear `src/Orders.API/Domain/ValueObjects/Address.cs`:

```csharp
using Orders.API.Domain.Exceptions;

namespace Orders.API.Domain.ValueObjects;

/// <summary>
/// Value Object que representa una dirección de envío.
/// Validación de código de país ISO 3166-1 alpha-2 (2 letras).
/// </summary>
public sealed record Address
{
    public string Street  { get; }
    public string City    { get; }
    public string ZipCode { get; }
    public string Country { get; }
    public string? State  { get; }

    public Address(
        string street,
        string city,
        string zipCode,
        string country,
        string? state = null)
    {
        Street  = Guard.NotNullOrEmpty(street,  nameof(street));
        City    = Guard.NotNullOrEmpty(city,    nameof(city));
        ZipCode = Guard.NotNullOrEmpty(zipCode, nameof(zipCode));
        Country = ValidateCountryCode(country);
        State   = state;
    }

    private static string ValidateCountryCode(string country)
    {
        if (string.IsNullOrWhiteSpace(country) || country.Length != 2)
            throw new DomainException(
                $"Country must be a 2-letter ISO code. Got: '{country}'");
        return country.ToUpperInvariant();
    }

    public override string ToString() => $"{Street}, {City} {ZipCode}, {Country}";
}
```

---

## Paso 8 — OrderStatus.cs (fichero completo)

Crear `src/Orders.API/Domain/ValueObjects/OrderStatus.cs`:

```csharp
using Orders.API.Domain.Exceptions;

namespace Orders.API.Domain.ValueObjects;

/// <summary>
/// Value Object que representa el estado de un pedido.
/// No es un enum simple: encapsula las reglas de transición de estado.
/// Constructor privado: solo se usan las instancias estáticas predefinidas.
/// </summary>
public sealed record OrderStatus
{
    public static readonly OrderStatus Pending   = new("Pending");
    public static readonly OrderStatus Confirmed = new("Confirmed");
    public static readonly OrderStatus Shipped   = new("Shipped");
    public static readonly OrderStatus Delivered = new("Delivered");
    public static readonly OrderStatus Cancelled = new("Cancelled");

    public string Value { get; }

    private OrderStatus(string value) => Value = value;

    // Reglas de transición expresadas como dato, no como código
    private static readonly Dictionary<OrderStatus, OrderStatus[]> ValidTransitions = new()
    {
        [Pending]   = [Confirmed, Cancelled],
        [Confirmed] = [Shipped,   Cancelled],
        [Shipped]   = [Delivered],
        [Delivered] = [],
        [Cancelled] = []
    };

    public bool CanTransitionTo(OrderStatus next)
        => ValidTransitions[this].Contains(next);

    public OrderStatus TransitionTo(OrderStatus next)
    {
        if (!CanTransitionTo(next))
            throw new DomainException(
                $"Cannot transition from '{Value}' to '{next.Value}'");
        return next;
    }

    /// <summary>Para EF Core: reconstruir desde la BD.</summary>
    public static OrderStatus FromString(string value) => value switch
    {
        "Pending"   => Pending,
        "Confirmed" => Confirmed,
        "Shipped"   => Shipped,
        "Delivered" => Delivered,
        "Cancelled" => Cancelled,
        _ => throw new DomainException($"Unknown OrderStatus: '{value}'")
    };

    public override string ToString() => Value;
}
```

---

## Paso 9 — IDomainEvent.cs y eventos (ficheros completos)

Crear `src/Orders.API/Domain/Events/IDomainEvent.cs`:

```csharp
namespace Orders.API.Domain.Events;

/// <summary>
/// Contrato base para los domain events.
/// Los domain events son hechos que ocurrieron en el dominio — inmutables.
/// </summary>
public interface IDomainEvent
{
    Guid EventId { get; }
    DateTime OccurredAt { get; }
}
```

Crear `src/Orders.API/Domain/Events/OrderCreatedEvent.cs`:

```csharp
using Orders.API.Domain.ValueObjects;

namespace Orders.API.Domain.Events;

public sealed record OrderCreatedEvent(
    Guid OrderId,
    Guid CustomerId,
    Money Total) : IDomainEvent
{
    public Guid EventId { get; } = Guid.NewGuid();
    public DateTime OccurredAt { get; } = DateTime.UtcNow;
}
```

Crear `src/Orders.API/Domain/Events/OrderConfirmedEvent.cs`:

```csharp
using Orders.API.Domain.ValueObjects;

namespace Orders.API.Domain.Events;

public sealed record OrderConfirmedEvent(
    Guid OrderId,
    Guid CustomerId,
    Money Total) : IDomainEvent
{
    public Guid EventId { get; } = Guid.NewGuid();
    public DateTime OccurredAt { get; } = DateTime.UtcNow;
}
```

Crear `src/Orders.API/Domain/Events/OrderCancelledEvent.cs`:

```csharp
namespace Orders.API.Domain.Events;

public sealed record OrderCancelledEvent(
    Guid OrderId,
    Guid CustomerId,
    string Reason) : IDomainEvent
{
    public Guid EventId { get; } = Guid.NewGuid();
    public DateTime OccurredAt { get; } = DateTime.UtcNow;
}
```

Crear `src/Orders.API/Domain/Events/OrderShippedEvent.cs`:

```csharp
namespace Orders.API.Domain.Events;

public sealed record OrderShippedEvent(Guid OrderId) : IDomainEvent
{
    public Guid EventId { get; } = Guid.NewGuid();
    public DateTime OccurredAt { get; } = DateTime.UtcNow;
}
```

Crear `src/Orders.API/Domain/Events/OrderDeliveredEvent.cs`:

```csharp
namespace Orders.API.Domain.Events;

public sealed record OrderDeliveredEvent(Guid OrderId) : IDomainEvent
{
    public Guid EventId { get; } = Guid.NewGuid();
    public DateTime OccurredAt { get; } = DateTime.UtcNow;
}
```

---

## Paso 10 — OrderLine.cs (fichero completo)

Crear `src/Orders.API/Domain/Entities/OrderLine.cs`:

```csharp
using Orders.API.Domain.Exceptions;
using Orders.API.Domain.ValueObjects;

namespace Orders.API.Domain.Entities;

/// <summary>
/// Entity dentro del aggregate Order.
/// Constructor privado: solo Order puede crear OrderLines via el factory method interno.
/// Todos los setters son privados: solo el Aggregate Root puede modificar sus entidades hijas.
/// </summary>
public class OrderLine
{
    public Guid   Id          { get; private set; }
    public Guid   ProductId   { get; private set; }
    public string ProductName { get; private set; } = null!;
    public int    Quantity    { get; private set; }
    public Money  UnitPrice   { get; private set; } = null!;

    /// <summary>Total de la línea — siempre consistente con cantidad y precio.</summary>
    public Money LineTotal => UnitPrice.Multiply(Quantity);

    /// <summary>Para EF Core: reconstruir desde la BD.</summary>
    private OrderLine() { }

    /// <summary>
    /// Factory method interno: solo Order puede crear OrderLines.
    /// internal garantiza que nada fuera del assembly pueda llamarlo directamente.
    /// </summary>
    internal static OrderLine Create(
        Guid   productId,
        string productName,
        int    quantity,
        Money  unitPrice)
    {
        if (quantity <= 0 || quantity > 1000)
            throw new DomainException(
                $"Quantity must be between 1 and 1000. Got: {quantity}");

        return new OrderLine
        {
            Id          = Guid.NewGuid(),
            ProductId   = productId,
            ProductName = Guard.NotNullOrEmpty(productName, nameof(productName)),
            Quantity    = quantity,
            UnitPrice   = Guard.NotNull(unitPrice, nameof(unitPrice))
        };
    }

    /// <summary>Solo el Aggregate Root puede actualizar cantidad.</summary>
    internal void UpdateQuantity(int newQuantity)
    {
        if (newQuantity <= 0)
            throw new DomainException("Quantity must be positive");
        Quantity = newQuantity;
    }
}
```

---

## Paso 11 — Order.cs (fichero completo — Aggregate Root)

Crear `src/Orders.API/Domain/Entities/Order.cs`:

```csharp
using Orders.API.Domain.Events;
using Orders.API.Domain.Exceptions;
using Orders.API.Domain.ValueObjects;

namespace Orders.API.Domain.Entities;

/// <summary>
/// Aggregate Root del bounded context de pedidos.
/// Guardián de todas las invariantes de negocio.
/// Único punto de entrada al aggregate Order+OrderLines.
/// El dominio no conoce EF Core, SQL Server ni ninguna infraestructura.
/// </summary>
public class Order
{
    // ─── Propiedades ─────────────────────────────────────────────────────────
    public Guid          Id              { get; private set; }
    public Guid          CustomerId      { get; private set; }
    public OrderStatus   Status          { get; private set; } = null!;
    public Address       ShippingAddress { get; private set; } = null!;
    public DateTime      CreatedAt       { get; private set; }
    public DateTime?     ConfirmedAt     { get; private set; }
    public DateTime?     CancelledAt     { get; private set; }

    // C# 14 field-backed property con validación en el setter privado
    public string? CancellationReason
    {
        get;
        private set
        {
            if (value is not null && value.Length > 500)
                throw new DomainException(
                    "Cancellation reason cannot exceed 500 characters");
            field = value;
        }
    }

    // Las líneas: lista interna, solo lectura hacia afuera
    private readonly List<OrderLine> _lines = [];
    public IReadOnlyList<OrderLine> Lines => _lines.AsReadOnly();

    /// <summary>
    /// Total calculado siempre desde las líneas — nunca almacenado en BD.
    /// Garantiza consistencia absoluta entre líneas y total.
    /// </summary>
    public Money Total => Lines.Any()
        ? Lines.Skip(1).Aggregate(
            Lines[0].LineTotal,
            (acc, line) => acc.Add(line.LineTotal))
        : Money.Zero("EUR");

    // Domain Events registrados durante la operación
    private readonly List<IDomainEvent> _domainEvents = [];
    public IReadOnlyList<IDomainEvent> DomainEvents => _domainEvents.AsReadOnly();

    /// <summary>Constructor privado para EF Core.</summary>
    private Order() { }

    // ─── Factory Method ───────────────────────────────────────────────────────

    /// <summary>
    /// Único punto de entrada para crear un pedido.
    /// Aplica todas las invariantes de creación y registra OrderCreatedEvent.
    /// </summary>
    public static Order Create(
        Guid customerId,
        IEnumerable<(Guid ProductId, string ProductName, int Quantity, Money UnitPrice)> items,
        Address shippingAddress)
    {
        var itemList = items.ToList();

        if (!itemList.Any())
            throw new DomainException("Order must have at least one item");

        if (itemList.Count > 50)
            throw new DomainException("Order cannot have more than 50 lines");

        if (itemList.Any(i => i.UnitPrice.Currency != "EUR"))
            throw new DomainException("TechShop only accepts EUR prices");

        var order = new Order
        {
            Id              = Guid.NewGuid(),
            CustomerId      = customerId,
            Status          = OrderStatus.Pending,
            ShippingAddress = Guard.NotNull(shippingAddress, nameof(shippingAddress)),
            CreatedAt       = DateTime.UtcNow
        };

        foreach (var item in itemList)
        {
            order._lines.Add(OrderLine.Create(
                item.ProductId,
                item.ProductName,
                item.Quantity,
                item.UnitPrice));
        }

        // Verificar importe mínimo después de calcular el total
        if (order.Total.Amount < 10.00m)
            throw new DomainException("Minimum order amount is 10.00 EUR");

        order._domainEvents.Add(new OrderCreatedEvent(
            order.Id,
            order.CustomerId,
            order.Total));

        return order;
    }

    // ─── Métodos de negocio ───────────────────────────────────────────────────

    /// <summary>Confirmar el pedido. Pending → Confirmed.</summary>
    public void Confirm()
    {
        Status      = Status.TransitionTo(OrderStatus.Confirmed);
        ConfirmedAt = DateTime.UtcNow;
        _domainEvents.Add(new OrderConfirmedEvent(Id, CustomerId, Total));
    }

    /// <summary>Cancelar el pedido. Pending/Confirmed → Cancelled.</summary>
    public void Cancel(string reason)
    {
        Guard.NotNullOrEmpty(reason, nameof(reason));
        Status             = Status.TransitionTo(OrderStatus.Cancelled);
        CancelledAt        = DateTime.UtcNow;
        CancellationReason = reason;
        _domainEvents.Add(new OrderCancelledEvent(Id, CustomerId, reason));
    }

    /// <summary>Marcar como enviado. Confirmed → Shipped.</summary>
    public void Ship()
    {
        Status = Status.TransitionTo(OrderStatus.Shipped);
        _domainEvents.Add(new OrderShippedEvent(Id));
    }

    /// <summary>Marcar como entregado. Shipped → Delivered.</summary>
    public void Deliver()
    {
        Status = Status.TransitionTo(OrderStatus.Delivered);
        _domainEvents.Add(new OrderDeliveredEvent(Id));
    }

    /// <summary>
    /// Limpiar los domain events después de procesarlos.
    /// El handler los publica vía MediatR y luego llama a este método.
    /// </summary>
    public void ClearDomainEvents() => _domainEvents.Clear();
}
```

---

## Paso 12 — IOrderRepository.cs (fichero completo)

Crear `src/Orders.API/Domain/Interfaces/IOrderRepository.cs`:

```csharp
using Orders.API.Domain.Entities;

namespace Orders.API.Domain.Interfaces;

/// <summary>
/// Puerto de salida para la persistencia de Orders.
/// El dominio define la interfaz — la infraestructura la implementa.
/// Sin IQueryable, sin Expression, sin DbSet: el dominio no conoce EF Core.
/// </summary>
public interface IOrderRepository
{
    /// <summary>Obtiene un pedido por su Id. Null si no existe.</summary>
    Task<Order?> GetByIdAsync(Guid id, CancellationToken ct = default);

    /// <summary>Lista pedidos de un cliente, ordenados por fecha descendente.</summary>
    Task<IReadOnlyList<Order>> GetByCustomerIdAsync(
        Guid customerId, CancellationToken ct = default);

    /// <summary>Lista pedidos pendientes de procesamiento.</summary>
    Task<IReadOnlyList<Order>> GetPendingAsync(CancellationToken ct = default);

    /// <summary>Persiste el aggregate. Detecta si es insert o update.</summary>
    Task SaveAsync(Order order, CancellationToken ct = default);
}
```

---

## Paso 13 — OrderDbContext.cs (fichero completo)

Crear `src/Orders.API/Infrastructure/Persistence/OrderDbContext.cs`:

```csharp
using Microsoft.EntityFrameworkCore;
using Orders.API.Domain.Entities;
using Orders.API.Domain.ValueObjects;

namespace Orders.API.Infrastructure.Persistence;

/// <summary>
/// DbContext de Orders.API. Configura el mapeo entre el modelo de dominio y SQL Server.
/// - Todas las propiedades privadas se acceden via reflexión de EF Core
/// - Value Objects (Address) mapeados como owned entities (columnas en tabla principal)
/// - OrderStatus mapeado con conversión string ↔ record
/// - Total NO se almacena — se calcula siempre desde las líneas
/// </summary>
public class OrderDbContext : DbContext
{
    public OrderDbContext(DbContextOptions<OrderDbContext> options) : base(options) { }

    public DbSet<Order> Orders => Set<Order>();

    protected override void OnModelCreating(ModelBuilder modelBuilder)
    {
        modelBuilder.Entity<Order>(entity =>
        {
            entity.ToTable("Orders", schema: "orders");
            entity.HasKey(o => o.Id);

            entity.Property(o => o.CustomerId).IsRequired();
            entity.Property(o => o.CreatedAt).IsRequired();
            entity.Property(o => o.ConfirmedAt);
            entity.Property(o => o.CancelledAt);
            entity.Property(o => o.CancellationReason).HasMaxLength(500);

            // OrderStatus: conversión string ↔ sealed record
            entity.Property(o => o.Status)
                .HasConversion(
                    s => s.Value,                        // Guardar: OrderStatus → string
                    v => OrderStatus.FromString(v))      // Leer: string → OrderStatus
                .HasColumnName("Status")
                .HasMaxLength(20)
                .IsRequired();

            // Address como owned entity: columnas en la tabla Orders con prefijo Shipping
            entity.OwnsOne(o => o.ShippingAddress, addr =>
            {
                addr.Property(a => a.Street) .HasColumnName("ShippingStreet") .HasMaxLength(200).IsRequired();
                addr.Property(a => a.City)   .HasColumnName("ShippingCity")   .HasMaxLength(100).IsRequired();
                addr.Property(a => a.ZipCode).HasColumnName("ShippingZipCode").HasMaxLength(20) .IsRequired();
                addr.Property(a => a.Country).HasColumnName("ShippingCountry").HasMaxLength(2)  .IsRequired();
                addr.Property(a => a.State)  .HasColumnName("ShippingState")  .HasMaxLength(100);
            });

            // Las líneas: acceso al campo privado _lines
            entity.HasMany<OrderLine>("_lines")
                .WithOne()
                .HasForeignKey("OrderId")
                .OnDelete(DeleteBehavior.Cascade);
        });

        modelBuilder.Entity<OrderLine>(entity =>
        {
            entity.ToTable("OrderLines", schema: "orders");
            entity.HasKey(l => l.Id);
            entity.Property(l => l.ProductId).IsRequired();
            entity.Property(l => l.ProductName).HasMaxLength(200).IsRequired();
            entity.Property(l => l.Quantity).IsRequired();

            // Money como owned entity en OrderLine
            entity.OwnsOne(l => l.UnitPrice, money =>
            {
                money.Property(m => m.Amount)  .HasColumnName("UnitPriceAmount")  .HasPrecision(18, 2).IsRequired();
                money.Property(m => m.Currency).HasColumnName("UnitPriceCurrency").HasMaxLength(3)     .IsRequired();
            });
        });
    }
}
```

---

## Paso 14 — SqlOrderRepository.cs (fichero completo)

Crear `src/Orders.API/Infrastructure/Persistence/SqlOrderRepository.cs`:

```csharp
using Microsoft.EntityFrameworkCore;
using Orders.API.Domain.Entities;
using Orders.API.Domain.Interfaces;

namespace Orders.API.Infrastructure.Persistence;

/// <summary>
/// Implementación de IOrderRepository con EF Core y SQL Server.
/// Vive en la capa de infraestructura — el dominio no la conoce.
/// </summary>
public class SqlOrderRepository : IOrderRepository
{
    private readonly OrderDbContext _dbContext;

    public SqlOrderRepository(OrderDbContext dbContext)
        => _dbContext = dbContext;

    public async Task<Order?> GetByIdAsync(Guid id, CancellationToken ct = default)
        => await _dbContext.Orders
            .Include("_lines")           // Accede al campo privado del aggregate
            .FirstOrDefaultAsync(o => o.Id == id, ct);

    public async Task<IReadOnlyList<Order>> GetByCustomerIdAsync(
        Guid customerId, CancellationToken ct = default)
        => await _dbContext.Orders
            .Include("_lines")
            .Where(o => o.CustomerId == customerId)
            .OrderByDescending(o => o.CreatedAt)
            .ToListAsync(ct);

    public async Task<IReadOnlyList<Order>> GetPendingAsync(CancellationToken ct = default)
        => await _dbContext.Orders
            .Include("_lines")
            .Where(o => o.Status == Domain.ValueObjects.OrderStatus.Pending)
            .OrderBy(o => o.CreatedAt)
            .ToListAsync(ct);

    public async Task SaveAsync(Order order, CancellationToken ct = default)
    {
        var entry = _dbContext.Entry(order);
        if (entry.State == EntityState.Detached)
            _dbContext.Orders.Add(order);   // Insert
        // Si está tracked: EF Core detecta los cambios automáticamente (Update)
        await _dbContext.SaveChangesAsync(ct);
    }
}
```

---

## Paso 15 — ServiceCollectionExtensions.cs (fichero completo)

Crear `src/Orders.API/Infrastructure/ServiceCollectionExtensions.cs`:

```csharp
using Microsoft.EntityFrameworkCore;
using Microsoft.Extensions.Configuration;
using Microsoft.Extensions.DependencyInjection;
using Orders.API.Domain.Interfaces;
using Orders.API.Infrastructure.Persistence;

namespace Orders.API.Infrastructure;

public static class ServiceCollectionExtensions
{
    /// <summary>
    /// Registra todos los servicios de infraestructura de Orders.API.
    /// Se llama desde Program.cs para mantenerlo limpio.
    /// </summary>
    public static IServiceCollection AddOrdersInfrastructure(
        this IServiceCollection services,
        IConfiguration configuration)
    {
        // DbContext con schema "orders" y tabla de migraciones propia
        services.AddDbContext<OrderDbContext>(options =>
            options.UseSqlServer(
                configuration.GetConnectionString("OrdersDb"),
                sql => sql.MigrationsHistoryTable(
                    "__EFMigrationsHistory", "orders")));

        // Interfaz del dominio → implementación de infraestructura
        services.AddScoped<IOrderRepository, SqlOrderRepository>();

        return services;
    }
}
```

---

## Paso 16 — Actualizar Program.cs

Añadir `AddOrdersInfrastructure` en `src/Orders.API/Program.cs`. Localizar el bloque de servicios y añadir antes de `AddControllers()`:

```csharp
// ─── Infraestructura ──────────────────────────────────────────────────────────
builder.Services.AddOrdersInfrastructure(builder.Configuration);
```

El bloque de servicios de Program.cs queda:

```csharp
// ─── Infraestructura ──────────────────────────────────────────────────────────
builder.Services.AddOrdersInfrastructure(builder.Configuration);

// ─── Servicios ────────────────────────────────────────────────────────────────
builder.Services.AddControllers();
builder.Services.AddOpenApi();
```

---

## Paso 17 — Crear la primera migration

```bash
# Desde la raíz del repo
dotnet ef migrations add InitialOrderSchema \
    --project src/Orders.API \
    --startup-project src/Orders.API \
    --context OrderDbContext \
    --output-dir Infrastructure/Persistence/Migrations

# Aplicar a LocalDB
dotnet ef database update \
    --project src/Orders.API \
    --startup-project src/Orders.API \
    --context OrderDbContext
```

La migration crea el schema SQL completo:

```sql
CREATE SCHEMA [orders];

CREATE TABLE [orders].[Orders] (
    [Id]                  uniqueidentifier NOT NULL,
    [CustomerId]          uniqueidentifier NOT NULL,
    [Status]              nvarchar(20)     NOT NULL  DEFAULT 'Pending',
    [ShippingStreet]      nvarchar(200)    NOT NULL,
    [ShippingCity]        nvarchar(100)    NOT NULL,
    [ShippingZipCode]     nvarchar(20)     NOT NULL,
    [ShippingCountry]     nvarchar(2)      NOT NULL,
    [ShippingState]       nvarchar(100)    NULL,
    [CreatedAt]           datetime2        NOT NULL,
    [ConfirmedAt]         datetime2        NULL,
    [CancelledAt]         datetime2        NULL,
    [CancellationReason]  nvarchar(500)    NULL,
    CONSTRAINT [PK_Orders] PRIMARY KEY ([Id])
);

CREATE TABLE [orders].[OrderLines] (
    [Id]                  uniqueidentifier NOT NULL,
    [OrderId]             uniqueidentifier NOT NULL,
    [ProductId]           uniqueidentifier NOT NULL,
    [ProductName]         nvarchar(200)    NOT NULL,
    [Quantity]            int              NOT NULL,
    [UnitPriceAmount]     decimal(18,2)    NOT NULL,
    [UnitPriceCurrency]   nvarchar(3)      NOT NULL,
    CONSTRAINT [PK_OrderLines] PRIMARY KEY ([Id]),
    CONSTRAINT [FK_OrderLines_Orders_OrderId]
        FOREIGN KEY ([OrderId]) REFERENCES [orders].[Orders] ([Id]) ON DELETE CASCADE
);
```

> **Nota:** El `Total` NO se almacena en la BD. Se calcula siempre en memoria desde las líneas. Para queries de listado eficientes, usaremos Dapper con un read model en M6.3.

---

## Paso 18 — Actualizar OrderBuilder en tests

Actualizar `tests/Orders.API.Tests/Shared/Builders/OrderBuilder.cs` con la nueva firma de `Order.Create`:

```csharp
using Orders.API.Domain.Entities;
using Orders.API.Domain.ValueObjects;

namespace Orders.API.Tests.Shared.Builders;

public class OrderBuilder
{
    private Guid _customerId = Guid.NewGuid();
    private readonly List<(Guid ProductId, string ProductName, int Quantity, Money UnitPrice)> _items = [];
    private Address _address = new("Calle Mayor 1", "Madrid", "28001", "ES");

    public OrderBuilder WithCustomer(Guid customerId)
    {
        _customerId = customerId;
        return this;
    }

    public OrderBuilder WithAddress(string street, string city, string zip, string country)
    {
        _address = new Address(street, city, zip, country);
        return this;
    }

    public OrderBuilder WithLine(
        string productName = "Test Product",
        int    quantity    = 1,
        decimal price      = 10.00m,
        string currency    = "EUR")
    {
        _items.Add((Guid.NewGuid(), productName, quantity, new Money(price, currency)));
        return this;
    }

    public OrderBuilder WithManyLines(int count)
    {
        for (int i = 0; i < count; i++)
            WithLine($"Product {i + 1}");
        return this;
    }

    public Order Build()
    {
        if (_items.Count == 0) WithLine();
        return Order.Create(_customerId, _items, _address);
    }

    public Order BuildConfirmed()
    {
        var order = Build();
        order.Confirm();
        return order;
    }

    public Order BuildCancelled(string reason = "Test cancellation")
    {
        var order = Build();
        order.Cancel(reason);
        return order;
    }
}
```

---

## Paso 19 — Actualizar tests del dominio

Actualizar `tests/Orders.API.Tests/Unit/Domain/OrderTests.cs` con el dominio real:

```csharp
using FluentAssertions;
using Orders.API.Domain.Events;
using Orders.API.Domain.Exceptions;
using Orders.API.Domain.ValueObjects;
using Orders.API.Tests.Shared.Builders;
using Xunit;

namespace Orders.API.Tests.Unit.Domain;

[Trait("Category", "Unit")]
public class OrderTests
{
    [Fact]
    public void Create_ValidOrder_ShouldHavePendingStatusAndRegisterEvent()
    {
        var order = new OrderBuilder()
            .WithLine("MacBook Pro", quantity: 1, price: 1999.99m)
            .Build();

        order.Status.Should().Be(OrderStatus.Pending);
        order.CreatedAt.Should().BeCloseTo(DateTime.UtcNow, TimeSpan.FromSeconds(5));
        order.DomainEvents.Should().ContainSingle()
            .Which.Should().BeOfType<OrderCreatedEvent>();
    }

    [Fact]
    public void Create_EmptyItems_ShouldThrowDomainException()
    {
        var address = new Address("Calle Mayor 1", "Madrid", "28001", "ES");
        var act = () => Order.Create(Guid.NewGuid(), [], address);

        act.Should().Throw<DomainException>()
            .WithMessage("*at least one item*");
    }

    [Fact]
    public void Create_MoreThan50Lines_ShouldThrowDomainException()
    {
        var order = new OrderBuilder().WithManyLines(51);
        var act = () => order.Build();

        act.Should().Throw<DomainException>()
            .WithMessage("*50 lines*");
    }

    [Fact]
    public void Confirm_PendingOrder_ShouldChangeStatusAndRegisterEvent()
    {
        var order = new OrderBuilder().Build();

        order.Confirm();

        order.Status.Should().Be(OrderStatus.Confirmed);
        order.ConfirmedAt.Should().NotBeNull();
        order.DomainEvents.Should().HaveCount(2); // Created + Confirmed
        order.DomainEvents.Last().Should().BeOfType<OrderConfirmedEvent>();
    }

    [Fact]
    public void Cancel_PendingOrder_ShouldWorkWithReason()
    {
        var order = new OrderBuilder().Build();

        order.Cancel("Customer changed their mind");

        order.Status.Should().Be(OrderStatus.Cancelled);
        order.CancellationReason.Should().Be("Customer changed their mind");
        order.CancelledAt.Should().NotBeNull();
    }

    [Fact]
    public void Cancel_DeliveredOrder_ShouldThrowDomainException()
    {
        var order = new OrderBuilder().BuildConfirmed();
        order.Ship();
        order.Deliver();

        var act = () => order.Cancel("Too late");

        act.Should().Throw<DomainException>()
            .WithMessage("*Cannot transition from 'Delivered'*");
    }

    [Fact]
    public void Total_ShouldBeCalculatedFromLines()
    {
        var order = new OrderBuilder()
            .WithLine("MacBook Pro", quantity: 1, price: 1999.99m)
            .WithLine("Magic Mouse", quantity: 2, price: 79.99m)
            .Build();

        order.Total.Amount.Should().Be(1999.99m + 79.99m * 2);
        order.Total.Currency.Should().Be("EUR");
    }

    [Fact]
    public void ClearDomainEvents_ShouldRemoveAllEvents()
    {
        var order = new OrderBuilder().Build();

        order.ClearDomainEvents();

        order.DomainEvents.Should().BeEmpty();
    }
}
```

---

## Paso 20 — Verificar que compila y los tests pasan

```bash
dotnet build OrderFlow.sln
```

```bash
dotnet test --filter "Category=Unit" --verbosity normal
```

Todos los unit tests deben pasar.

---

## Paso 21 — Estructura final verificada

```
src/Orders.API/Domain/
├── Guards.cs                      ✅
├── Exceptions/DomainException.cs  ✅
├── ValueObjects/
│   ├── Money.cs                   ✅ sealed record, validación, operaciones
│   ├── Address.cs                 ✅ sealed record, ISO country code
│   └── OrderStatus.cs             ✅ sealed record, transiciones como dato
├── Entities/
│   ├── OrderLine.cs               ✅ internal constructor, LineTotal calculado
│   └── Order.cs                   ✅ Aggregate Root, factory method, 4 métodos de negocio
├── Events/
│   ├── IDomainEvent.cs            ✅
│   ├── OrderCreatedEvent.cs       ✅
│   ├── OrderConfirmedEvent.cs     ✅
│   ├── OrderCancelledEvent.cs     ✅
│   ├── OrderShippedEvent.cs       ✅
│   └── OrderDeliveredEvent.cs     ✅
└── Interfaces/IOrderRepository.cs ✅

src/Orders.API/Infrastructure/
├── Persistence/
│   ├── OrderDbContext.cs          ✅ owned entities, conversión OrderStatus
│   ├── SqlOrderRepository.cs      ✅ Include("_lines") para campo privado
│   └── Migrations/                ✅ InitialOrderSchema generado
└── ServiceCollectionExtensions.cs ✅ AddOrdersInfrastructure
```

---

## Paso 22 — Commit y push

```bash
git add .
git commit -m "m3.1: Dominio completo de Orders con DDD táctico y Clean Architecture

- DomainException como excepción base para violaciones de reglas de negocio
- Guard helper con validaciones reutilizables
- Money: sealed record con Amount/Currency, operaciones aritméticas, igualdad por valor
- Address: sealed record con validación ISO country code
- OrderStatus: sealed record con transiciones de estado como dato (no como switch)
- OrderLine: entity con constructor internal, LineTotal calculado
- Order: Aggregate Root con factory Create, métodos Confirm/Cancel/Ship/Deliver
  - Invariantes: mínimo 1 línea, máximo 50, solo EUR, mínimo 10€
  - C# 14: field-backed property CancellationReason, collection expressions []
  - Domain Events: OrderCreated/Confirmed/Cancelled/Shipped/Delivered
- IDomainEvent + 5 eventos como sealed records
- IOrderRepository: interfaz limpia en dominio (sin IQueryable, sin EF Core)
- SqlOrderRepository: implementación con EF Core e Include de campo privado _lines
- OrderDbContext: owned entities para Address y Money, conversión para OrderStatus
- ServiceCollectionExtensions: AddOrdersInfrastructure()
- Migration InitialOrderSchema: schema orders, tablas Orders y OrderLines
- Total NO almacenado en BD — calculado siempre desde las líneas
- OrderBuilder actualizado con nueva firma de Order.Create
- Unit tests actualizados con el dominio real"

git push origin m3.1
```

---

## Checklist final

- [ ] `dotnet build OrderFlow.sln` → sin errores
- [ ] `dotnet test --filter "Category=Unit"` → todos pasan
- [ ] `dotnet ef database update` → migration aplicada a LocalDB
- [ ] `sqllocaldb info MSSQLLocalDB` → instancia Running
- [ ] Schema `orders` creado con tablas `Orders` y `OrderLines`
- [ ] `dotnet run --project infrastructure/OrderFlow.AppHost` → Dashboard arranca
- [ ] Commit en rama `m3.1` y push completados

---

## Lo que viene en M3.2

`docs/m3.2.md` → Controllers: exponer el dominio como API REST:
- `OrdersController` con todos los endpoints
- Routing avanzado, model binding, `ActionResult<T>`
- Exception middleware que convierte `DomainException` → 422
- `Problem Details` RFC 7807
- Los Controllers son delgados — solo traducen HTTP a Commands de MediatR
