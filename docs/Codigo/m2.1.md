# M2.1 — El ecosistema .NET 10 para microservicios

## Branch

```bash
git checkout main
git checkout -b m2.1
```

## Objetivo

Orders.API arrancando con 3 endpoints skeleton (GET, GET/{id}, POST), ServiceDefaults integrado, Aspire orquestando, OpenAPI 3.1 generado automáticamente. El primer código del curso — de la pizarra al compilador.

## Qué se construye en este módulo

```
src/Orders.API/
├── Orders.API.csproj                ← solo ref a ServiceDefaults, sin paquetes extra
├── Program.cs                       ← AddServiceDefaults + OpenAPI + Controllers
├── Controllers/
│   └── OrdersController.cs          ← GET, GET/{id}, POST skeleton con object request
├── Properties/
│   └── launchSettings.json          ← perfiles https + Orders.API (Aspire)
├── appsettings.json                 ← configuración mínima
└── Orders.API.http                  ← fichero .http para pruebas manuales

infrastructure/OrderFlow.AppHost/
├── OrderFlow.AppHost.csproj
├── Program.cs                       ← AddConnectionString + AddDatabase + AddProject
└── appsettings.json                 ← connection strings LocalDB + RabbitMQ
```

> **Nota:** En M2.1 solo existe Orders.API. Los demás servicios se añaden cuando los necesitamos:
> `Products.API` → M4.1 · `Notifications.API` → M4.2 · `Payments.API` → M4.3 · `Gateway.API` → M4.4 · `OrderFlow.Contracts` → M4.2

---

## Paso 1 — Crear el proyecto Orders.API

```bash
dotnet new webapi -n Orders.API -o src/Orders.API \
  --framework net10.0 \
  --use-controllers

dotnet sln add src/Orders.API/Orders.API.csproj

# Añadir referencia a ServiceDefaults
dotnet add src/Orders.API/Orders.API.csproj \
  reference infrastructure/OrderFlow.ServiceDefaults/OrderFlow.ServiceDefaults.csproj

# Borrar el controller de ejemplo del template
rm src/Orders.API/Controllers/WeatherForecastController.cs
rm src/Orders.API/WeatherForecast.cs
```

---

## Paso 2 — Orders.API.csproj (fichero completo)

Reemplazar el contenido completo de `src/Orders.API/Orders.API.csproj`:

```xml
<Project Sdk="Microsoft.NET.Sdk.Web">

  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
    <RootNamespace>Orders.API</RootNamespace>
    <AssemblyName>Orders.API</AssemblyName>
  </PropertyGroup>

  <ItemGroup>
    <!-- ServiceDefaults: OpenTelemetry + health checks + service discovery -->
    <ProjectReference Include="..\..\infrastructure\OrderFlow.ServiceDefaults\OrderFlow.ServiceDefaults.csproj" />
  </ItemGroup>

</Project>
```

> **Nota:** OpenAPI viene incluido con el SDK de .NET 10 — no necesita paquete NuGet adicional.
> Los demás paquetes se añaden cuando los necesitamos:
> M2.3 → `Serilog.AspNetCore` · M3.2 → `FluentValidation.AspNetCore` · M3.4 → `MediatR`
> M3.5 → `Polly` · M3.6 → `EF Core + Dapper` · M4.2 → `MassTransit.RabbitMQ` · M5.1 → `JwtBearer`

---

## Paso 3 — Program.cs de Orders.API (fichero completo)

Reemplazar el contenido completo de `src/Orders.API/Program.cs`:

```csharp
var builder = WebApplication.CreateBuilder(args);

// Una sola línea: OpenTelemetry + health checks + service discovery
builder.AddServiceDefaults();

builder.Services.AddControllers();
builder.Services.AddOpenApi();

var app = builder.Build();

if (app.Environment.IsDevelopment())
{
    app.MapOpenApi(); // expone /openapi/v1.json
}

app.UseHttpsRedirection();
app.UseAuthorization();
app.MapControllers();

// Expone /health y /alive — registrados por ServiceDefaults
app.MapDefaultEndpoints();

app.Run();
```

---

## Paso 4 — OrdersController.cs (fichero completo)

Crear `src/Orders.API/Controllers/OrdersController.cs`:

```csharp
using Microsoft.AspNetCore.Mvc;

namespace Orders.API.Controllers;

[ApiController]
[Route("api/[controller]")]
public class OrdersController : ControllerBase
{
    private readonly ILogger<OrdersController> _logger;

    public OrdersController(ILogger<OrdersController> logger)
    {
        _logger = logger;
    }

    [HttpGet]
    public IActionResult GetAll()
    {
        _logger.LogInformation("Getting all orders");
        // Por ahora devolvemos una lista vacía
        return Ok(new List<object>());
    }

    [HttpGet("{id:guid}")]
    public IActionResult GetById(Guid id)
    {
        _logger.LogInformation("Getting order {OrderId}", id);
        // Por ahora devolvemos 404 siempre
        return NotFound();
    }

    [HttpPost]
    public IActionResult Create([FromBody] object request)
    {
        _logger.LogInformation("Creating new order");
        // Por ahora simulamos la creación
        return Created($"/api/orders/{Guid.NewGuid()}", null);
    }
}
```

> **Nota:** `object request` es intencional en M2.1 — es un skeleton puro.
> Los DTOs tipados y la validación llegan en M3.2. En M3.4 se reemplaza por Commands de CQRS.

---

## Paso 5 — appsettings.json de Orders.API (fichero completo)

Reemplazar `src/Orders.API/appsettings.json`:

```json
{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*"
}
```

> Las connection strings **NO van aquí**. Aspire las inyecta como variables de entorno en desarrollo. Azure Key Vault en producción.

---

## Paso 6 — launchSettings.json de Orders.API (fichero completo)

Reemplazar `src/Orders.API/Properties/launchSettings.json`:

```json
{
  "$schema": "https://json.schemastore.org/launchsettings.json",
  "profiles": {
    "https": {
      "commandName": "Project",
      "dotnetRunMessages": true,
      "launchBrowser": false,
      "applicationUrl": "https://localhost:7153;http://localhost:5153",
      "environmentVariables": {
        "ASPNETCORE_ENVIRONMENT": "Development"
      }
    },
    "Orders.API (Aspire)": {
      "commandName": "Project",
      "dotnetRunMessages": true,
      "launchBrowser": false,
      "applicationUrl": "https://localhost:7153;http://localhost:5153",
      "environmentVariables": {
        "ASPNETCORE_ENVIRONMENT": "Development",
        "ASPIRE_DASHBOARD_OTLP_ENDPOINT_URL": "http://localhost:18889"
      }
    }
  }
}
```

> **Dos perfiles:**
> - `https` — arrancar el servicio directamente sin Aspire
> - `Orders.API (Aspire)` — Aspire usa este perfil al lanzar el servicio; incluye la URL OTLP para enviar trazas al Dashboard

---

## Paso 7 — Fichero .http para pruebas manuales (fichero completo)

Crear `src/Orders.API/Orders.API.http`:

```http
@baseUrl = https://localhost:7153

### GET todos los pedidos
GET {{baseUrl}}/api/orders
Accept: application/json

###

### GET pedido por ID (devuelve 404 en skeleton)
GET {{baseUrl}}/api/orders/3fa85f64-5717-4562-b3fc-2c963f66afa6
Accept: application/json

###

### POST crear pedido
POST {{baseUrl}}/api/orders
Content-Type: application/json

{
  "customerId": "3fa85f64-5717-4562-b3fc-2c963f66afa6",
  "items": [
    {
      "productId": "a4b5c6d7-e8f9-0123-4567-89abcdef0123",
      "quantity": 2
    }
  ]
}

###

### Health check (registrado por ServiceDefaults)
GET {{baseUrl}}/health

###

### OpenAPI spec (solo en Development)
GET {{baseUrl}}/openapi/v1.json
```

---

## Paso 8 — AppHost Program.cs (fichero completo)

Reemplazar el contenido completo de `infrastructure/OrderFlow.AppHost/Program.cs`:

```csharp
var builder = DistributedApplication.CreateBuilder(args);

// SQL Server local (LocalDB) — sin Docker
var sqlServer = builder.AddConnectionString("sqlserver");

// RabbitMQ local — sin Docker
var messaging = builder.AddConnectionString("messaging");

// Base de datos de Orders
var ordersDb = sqlServer.AddDatabase("OrdersDb");

// Servicio Orders.API
builder.AddProject<Projects.Orders_API>("orders-api")
    .WithReference(ordersDb)
    .WithReference(messaging);

builder.Build().Run();
```

> En módulos siguientes se añadirán los demás servicios y sus bases de datos conforme se vayan creando.

---

## Paso 9 — AppHost .csproj (fichero completo)

Reemplazar el contenido completo de `infrastructure/OrderFlow.AppHost/OrderFlow.AppHost.csproj`:

```xml
<Project Sdk="Microsoft.NET.Sdk">

  <Sdk Name="Aspire.AppHost.Sdk" Version="9.1.*" />

  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <TargetFramework>net10.0</TargetFramework>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
    <IsAspireHost>true</IsAspireHost>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="Aspire.Hosting.AppHost" Version="9.1.*" />
  </ItemGroup>

  <ItemGroup>
    <ProjectReference Include="..\..\src\Orders.API\Orders.API.csproj" />
  </ItemGroup>

</Project>
```

---

## Paso 10 — AppHost appsettings.json (fichero completo)

Crear/reemplazar `infrastructure/OrderFlow.AppHost/appsettings.json`:

```json
{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Aspire.Hosting": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "ConnectionStrings": {
    "sqlserver": "Server=(localdb)\\MSSQLLocalDB;Trusted_Connection=true;TrustServerCertificate=true;",
    "messaging": "amqp://guest:guest@localhost:5672"
  }
}
```

---

## Paso 11 — Referencia de AppHost a Orders.API

```bash
dotnet add infrastructure/OrderFlow.AppHost/OrderFlow.AppHost.csproj \
  reference src/Orders.API/Orders.API.csproj
```

---

## Paso 12 — Verificar que compila

```bash
dotnet build OrderFlow.sln
```

Sin errores. Sin warnings de nullable. Si hay warnings, resolverlos antes de continuar.

---

## Paso 13 — Arrancar con Aspire y verificar

```bash
dotnet run --project infrastructure/OrderFlow.AppHost
```

En el Dashboard de Aspire verificar que `orders-api` aparece como **Running** ✅

Usar el fichero `Orders.API.http` para verificar los endpoints:

| Endpoint | Resultado esperado |
|---|---|
| `GET /api/orders` | `200 OK` con `[]` |
| `GET /api/orders/{guid}` | `404 Not Found` |
| `POST /api/orders` | `201 Created` con Location header |
| `GET /health` | `200 OK` |
| `GET /openapi/v1.json` | Spec OpenAPI 3.1 válida |

---

## Paso 14 — Estructura final verificada

```
orderflow/
├── CLAUDE.md
├── OrderFlow.sln
├── infrastructure/
│   ├── OrderFlow.AppHost/
│   │   ├── OrderFlow.AppHost.csproj    ✅ ref a Orders.API
│   │   ├── Program.cs                  ✅ AddConnectionString + AddDatabase + AddProject
│   │   └── appsettings.json            ✅ LocalDB + RabbitMQ
│   └── OrderFlow.ServiceDefaults/
│       ├── OrderFlow.ServiceDefaults.csproj  ✅ sin modificar
│       └── Extensions.cs                    ✅ sin modificar
└── src/
    └── Orders.API/
        ├── Orders.API.csproj           ✅ solo ref a ServiceDefaults
        ├── Program.cs                  ✅ AddServiceDefaults + OpenAPI + Controllers
        ├── Controllers/
        │   └── OrdersController.cs     ✅ GET, GET/{id}, POST skeleton
        ├── Properties/
        │   └── launchSettings.json     ✅ 2 perfiles: https + Orders.API (Aspire)
        ├── appsettings.json            ✅ configuración mínima
        └── Orders.API.http             ✅ 5 requests de prueba
```

---

## Paso 15 — Commit y push

```bash
git add .
git commit -m "m2.1: Orders.API skeleton con Aspire

- Program.cs con AddServiceDefaults(), OpenAPI 3.1, Controllers
- OrdersController GET /api/orders, GET /api/orders/{id}, POST /api/orders (skeleton)
- AppHost con AddConnectionString (LocalDB + RabbitMQ, sin Docker)
- AppHost appsettings.json con connection strings locales
- launchSettings.json con perfiles https y Orders.API (Aspire)
- Fichero .http con 5 requests de prueba manual
- /health y /alive via ServiceDefaults.MapDefaultEndpoints()"

git push origin m2.1
```

---

## Checklist final

- [ ] `dotnet build OrderFlow.sln` → sin errores ni warnings
- [ ] `dotnet run --project infrastructure/OrderFlow.AppHost` → Dashboard arranca
- [ ] `orders-api` aparece como **Running** en el Dashboard
- [ ] `GET /api/orders` → `200 OK` con `[]`
- [ ] `GET /api/orders/{guid}` → `404 Not Found`
- [ ] `POST /api/orders` → `201 Created`
- [ ] `GET /health` → `200 OK`
- [ ] `GET /openapi/v1.json` → spec OpenAPI válida
- [ ] Commit en rama `m2.1` y push completados

Si algún punto falla, resolverlo antes de avanzar. M2.2 construye directamente encima de este código.

---

## Lo que viene en M2.2

`docs/m2.2.md` → Inyección de Dependencias y configuración avanzada:
- Keyed Services para múltiples implementaciones del mismo interface
- Patrón Options completo: `IOptions`, `IOptionsSnapshot`, `IOptionsMonitor`
- Validación de configuración con DataAnnotations en startup
- Gestión de secretos con User Secrets
- Health Checks custom para SQL Server LocalDB
