# M2.3 — Logging, Observabilidad y Diagnóstico

## Branch

```bash
git checkout m2.2
git checkout -b m2.3
```

## Objetivo

Orders.API deja de ser una caja negra. Al terminar este módulo cada petición queda registrada con Serilog structured logging, correlacionada entre servicios mediante Correlation IDs, y visible en el dashboard de Aspire con trazas distribuidas y métricas de negocio personalizadas.

## Qué se añade en este módulo

```
src/Orders.API/
├── Orders.API.csproj                              ← añadir paquetes Serilog
├── Program.cs                                     ← bootstrap logger + Serilog + OTel completo
├── appsettings.json                               ← añadir sección "Serilog"
├── appsettings.Development.json                   ← Serilog Debug + EF queries
├── appsettings.Production.json                    ← Serilog Warning + App Insights sink
├── API/
│   └── Middleware/
│       └── CorrelationIdMiddleware.cs             ← genera/propaga X-Correlation-Id (nuevo)
└── Infrastructure/
    └── Telemetry/
        ├── OrdersTelemetry.cs                     ← ActivitySource para spans manuales (nuevo)
        ├── OrdersMetrics.cs                       ← Counters y Histograms de negocio (nuevo)
        └── OpenTelemetryEnricher.cs               ← TraceId+SpanId en logs Serilog (nuevo)
```

---

## Paso 1 — Instalar paquetes NuGet

```bash
dotnet add src/Orders.API/Orders.API.csproj package Serilog.AspNetCore
dotnet add src/Orders.API/Orders.API.csproj package Serilog.Sinks.Console
dotnet add src/Orders.API/Orders.API.csproj package Serilog.Enrichers.Environment
dotnet add src/Orders.API/Orders.API.csproj package Serilog.Enrichers.Thread
dotnet add src/Orders.API/Orders.API.csproj package Serilog.Enrichers.Process
```

> En M7.2 se añadirá `Serilog.Sinks.ApplicationInsights` para producción.

---

## Paso 2 — Orders.API.csproj (fichero completo)

Reemplazar el contenido completo de `src/Orders.API/Orders.API.csproj`:

```xml
<Project Sdk="Microsoft.NET.Sdk.Web">

  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
    <RootNamespace>Orders.API</RootNamespace>
    <AssemblyName>Orders.API</AssemblyName>
    <UserSecretsId>orderflow-orders-api</UserSecretsId>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="AspNetCore.HealthChecks.SqlServer" Version="9.*" />
    <PackageReference Include="AspNetCore.HealthChecks.RabbitMQ" Version="9.*" />
    <PackageReference Include="Azure.Extensions.AspNetCore.Configuration.Secrets" Version="1.*" />
    <PackageReference Include="Azure.Identity" Version="1.*" />
    <PackageReference Include="Serilog.AspNetCore" Version="9.*" />
    <PackageReference Include="Serilog.Sinks.Console" Version="6.*" />
    <PackageReference Include="Serilog.Enrichers.Environment" Version="3.*" />
    <PackageReference Include="Serilog.Enrichers.Thread" Version="4.*" />
    <PackageReference Include="Serilog.Enrichers.Process" Version="3.*" />
  </ItemGroup>

  <ItemGroup>
    <ProjectReference Include="..\..\infrastructure\OrderFlow.ServiceDefaults\OrderFlow.ServiceDefaults.csproj" />
  </ItemGroup>

</Project>
```

---

## Paso 3 — Crear estructura de carpetas

```bash
mkdir -p src/Orders.API/API/Middleware
mkdir -p src/Orders.API/Infrastructure/Telemetry
```

---

## Paso 4 — OpenTelemetryEnricher.cs (fichero completo)

Crear `src/Orders.API/Infrastructure/Telemetry/OpenTelemetryEnricher.cs`:

```csharp
using System.Diagnostics;
using Serilog.Core;
using Serilog.Events;

namespace Orders.API.Infrastructure.Telemetry;

/// <summary>
/// Enriquece cada log de Serilog con el TraceId y SpanId del Activity actual de OTel.
/// Permite correlacionar logs con trazas en Application Insights y en el dashboard de Aspire.
/// </summary>
public class OpenTelemetryEnricher : ILogEventEnricher
{
    public void Enrich(LogEvent logEvent, ILogEventPropertyFactory propertyFactory)
    {
        var activity = Activity.Current;
        if (activity is null) return;

        logEvent.AddPropertyIfAbsent(
            propertyFactory.CreateProperty("TraceId", activity.TraceId.ToString()));

        logEvent.AddPropertyIfAbsent(
            propertyFactory.CreateProperty("SpanId", activity.SpanId.ToString()));
    }
}
```

---

## Paso 5 — OrdersTelemetry.cs (fichero completo)

Crear `src/Orders.API/Infrastructure/Telemetry/OrdersTelemetry.cs`:

```csharp
using System.Diagnostics;

namespace Orders.API.Infrastructure.Telemetry;

/// <summary>
/// Fuente de actividades de OpenTelemetry para Orders.API.
/// Permite crear spans manuales en la lógica de negocio.
/// </summary>
public static class OrdersTelemetry
{
    public static readonly ActivitySource ActivitySource =
        new ActivitySource("Orders.API", "1.0.0");
}
```

---

## Paso 6 — OrdersMetrics.cs (fichero completo)

Crear `src/Orders.API/Infrastructure/Telemetry/OrdersMetrics.cs`:

```csharp
using System.Diagnostics.Metrics;

namespace Orders.API.Infrastructure.Telemetry;

/// <summary>
/// Métricas de negocio de Orders.API.
/// Se registra como Singleton. Visible en la pestaña Metrics del dashboard de Aspire.
/// En producción exporta a Application Insights vía el Azure Monitor Exporter.
/// </summary>
public class OrdersMetrics
{
    private readonly Counter<long> _ordersCreated;
    private readonly Counter<long> _ordersFailed;
    private readonly Histogram<double> _processingTime;

    public OrdersMetrics(IMeterFactory meterFactory)
    {
        var meter = meterFactory.Create("Orders.API");

        _ordersCreated = meter.CreateCounter<long>(
            "orders.created.total",
            description: "Total de pedidos creados correctamente");

        _ordersFailed = meter.CreateCounter<long>(
            "orders.failed.total",
            description: "Total de pedidos que fallaron durante su creación");

        _processingTime = meter.CreateHistogram<double>(
            "orders.processing.duration.ms",
            unit: "ms",
            description: "Duración del procesamiento de un pedido en milisegundos");
    }

    public void RecordOrderCreated(string currency, string customerType) =>
        _ordersCreated.Add(1, new TagList
        {
            { "currency", currency },
            { "customer_type", customerType }
        });

    public void RecordOrderFailed(string reason) =>
        _ordersFailed.Add(1, new TagList
        {
            { "reason", reason }
        });

    public void RecordProcessingTime(double milliseconds) =>
        _processingTime.Record(milliseconds);
}
```

---

## Paso 7 — CorrelationIdMiddleware.cs (fichero completo)

Crear `src/Orders.API/API/Middleware/CorrelationIdMiddleware.cs`:

```csharp
using System.Diagnostics;
using Serilog.Context;

namespace Orders.API.API.Middleware;

/// <summary>
/// Middleware que genera o propaga un Correlation ID único por petición.
/// - Si la petición trae X-Correlation-Id: lo reutiliza
/// - Si no: usa el TraceId de OTel o genera un nuevo GUID
/// El CorrelationId se añade al LogContext de Serilog para que aparezca
/// en TODOS los logs del request automáticamente.
/// </summary>
public class CorrelationIdMiddleware
{
    private const string CorrelationIdHeader = "X-Correlation-Id";
    private readonly RequestDelegate _next;

    public CorrelationIdMiddleware(RequestDelegate next) => _next = next;

    public async Task InvokeAsync(HttpContext context)
    {
        // Usar el CorrelationId que viene del upstream (YARP u otro servicio),
        // o el TraceId del Activity actual de OTel,
        // o generar uno nuevo si no hay ninguno
        var correlationId = context.Request.Headers[CorrelationIdHeader]
                                .FirstOrDefault()
                            ?? Activity.Current?.TraceId.ToString()
                            ?? Guid.NewGuid().ToString("N");

        // Devolver el CorrelationId al cliente en el response
        context.Response.Headers[CorrelationIdHeader] = correlationId;

        // Añadir al LogContext: aparece en TODOS los logs del request via Serilog
        using (LogContext.PushProperty("CorrelationId", correlationId))
        {
            // Disponible en HttpContext para propagarlo en llamadas salientes
            context.Items["CorrelationId"] = correlationId;
            await _next(context);
        }
    }
}
```

---

## Paso 8 — Program.cs de Orders.API (fichero completo)

Reemplazar el contenido completo de `src/Orders.API/Program.cs`:

```csharp
using Azure.Identity;
using Microsoft.AspNetCore.Diagnostics.HealthChecks;
using Microsoft.Extensions.Diagnostics.HealthChecks;
using Microsoft.Extensions.Options;
using Orders.API.API.Middleware;
using Orders.API.Application.Settings;
using Orders.API.Infrastructure.Telemetry;
using Serilog;
using Serilog.Events;

// ─── Bootstrap logger ──────────────────────────────────────────────────────────
// Activo desde el primer instante para capturar errores de startup
Log.Logger = new LoggerConfiguration()
    .WriteTo.Console()
    .CreateBootstrapLogger();

try
{
    var builder = WebApplication.CreateBuilder(args);

    // ─── Aspire ServiceDefaults ───────────────────────────────────────────────
    builder.AddServiceDefaults();

    // ─── Serilog ─────────────────────────────────────────────────────────────
    // Reemplaza el logger de .NET con Serilog como backend
    builder.Host.UseSerilog((ctx, services, cfg) => cfg
        .ReadFrom.Configuration(ctx.Configuration)   // Lee de appsettings.json
        .ReadFrom.Services(services)                 // Permite inyectar sinks
        .Enrich.FromLogContext()                     // LogContext.PushProperty
        .Enrich.WithMachineName()                    // Nombre del servidor
        .Enrich.WithEnvironmentName()                // Development/Production
        .Enrich.With<OpenTelemetryEnricher>());      // TraceId y SpanId de OTel

    // ─── OpenTelemetry (extendido sobre ServiceDefaults) ─────────────────────
    builder.Services.AddOpenTelemetry()
        .WithTracing(tracing => tracing
            .AddAspNetCoreInstrumentation()             // Spans por request HTTP
            .AddHttpClientInstrumentation()             // Spans por llamadas salientes
            .AddEntityFrameworkCoreInstrumentation()    // Spans por queries EF Core
            .AddSource("Orders.API")                    // Spans manuales de OrdersTelemetry
            .AddOtlpExporter())                         // → Dashboard de Aspire
        .WithMetrics(metrics => metrics
            .AddAspNetCoreInstrumentation()             // http.server.request.duration
            .AddHttpClientInstrumentation()
            .AddRuntimeInstrumentation()                // CPU, GC, threads
            .AddMeter("Orders.API")                     // Métricas de OrdersMetrics
            .AddOtlpExporter());

    // Métricas de negocio
    builder.Services.AddSingleton<OrdersMetrics>();

    // ─── Validación del DI container en desarrollo ────────────────────────────
    builder.Host.UseDefaultServiceProvider(options =>
    {
        options.ValidateScopes = builder.Environment.IsDevelopment();
        options.ValidateOnBuild = builder.Environment.IsDevelopment();
    });

    // ─── Patrón Options con validación en startup ─────────────────────────────
    builder.Services
        .AddOptions<OrdersSettings>()
        .BindConfiguration(OrdersSettings.SectionName)
        .ValidateDataAnnotations()
        .ValidateOnStart();

    builder.Services
        .AddSingleton<IValidateOptions<OrdersSettings>, OrdersSettingsValidator>();

    // ─── Health Checks ────────────────────────────────────────────────────────
    builder.Services.AddHealthChecks()
        .AddSqlServer(
            connectionString: builder.Configuration.GetConnectionString("OrdersDb")!,
            healthQuery: "SELECT 1",
            name: "orders-db",
            failureStatus: HealthStatus.Unhealthy,
            tags: ["ready", "db"])
        .AddRabbitMQ(
            rabbitConnectionString: builder.Configuration.GetConnectionString("messaging")!,
            name: "rabbitmq",
            failureStatus: HealthStatus.Degraded,
            tags: ["ready", "messaging"]);

    // ─── Servicios ────────────────────────────────────────────────────────────
    builder.Services.AddControllers();
    builder.Services.AddOpenApi();

    // ─── Azure Key Vault en producción ────────────────────────────────────────
    if (!builder.Environment.IsDevelopment())
    {
        var keyVaultUri = builder.Configuration["KeyVaultUri"];
        if (!string.IsNullOrEmpty(keyVaultUri))
        {
            builder.Configuration.AddAzureKeyVault(
                new Uri(keyVaultUri),
                new DefaultAzureCredential());
        }
    }

    var app = builder.Build();

    // ─── Pipeline ─────────────────────────────────────────────────────────────
    if (app.Environment.IsDevelopment())
        app.MapOpenApi();

    app.UseHttpsRedirection();

    // Correlation IDs — antes de otros middlewares para que los logs los incluyan
    app.UseMiddleware<CorrelationIdMiddleware>();

    // Log automático de cada request HTTP (excluye health checks)
    app.UseSerilogRequestLogging(options =>
    {
        options.MessageTemplate =
            "HTTP {RequestMethod} {RequestPath} responded {StatusCode} in {Elapsed:0.0000} ms";

        options.EnrichDiagnosticContext = (diagnosticContext, httpContext) =>
        {
            diagnosticContext.Set("RequestHost", httpContext.Request.Host.Value);
            diagnosticContext.Set("RequestScheme", httpContext.Request.Scheme);
            diagnosticContext.Set("UserAgent",
                httpContext.Request.Headers["User-Agent"].FirstOrDefault());
        };

        // Health checks en Verbose — no aparecen con MinimumLevel=Information en producción
        options.GetLevel = (ctx, elapsed, ex) =>
        {
            if (ex is not null) return LogEventLevel.Error;
            if (ctx.Response.StatusCode >= 500) return LogEventLevel.Error;
            if (ctx.Response.StatusCode >= 400) return LogEventLevel.Warning;
            if (ctx.Request.Path.StartsWithSegments("/health") ||
                ctx.Request.Path.StartsWithSegments("/alive"))
                return LogEventLevel.Verbose;
            return LogEventLevel.Information;
        };
    });

    app.UseAuthorization();
    app.MapControllers();

    app.MapHealthChecks("/health/ready", new HealthCheckOptions
    {
        Predicate = check => check.Tags.Contains("ready")
    });

    app.MapDefaultEndpoints(); // /health y /alive

    app.Run();
}
catch (Exception ex)
{
    Log.Fatal(ex, "Application startup failed");
}
finally
{
    // OBLIGATORIO: vacía el buffer de Serilog antes de que el proceso termine
    Log.CloseAndFlush();
}
```

---

## Paso 9 — appsettings.json de Orders.API (fichero completo)

Reemplazar `src/Orders.API/appsettings.json`:

```json
{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",
  "Orders": {
    "MaxLinesPerOrder": 50,
    "MinOrderAmount": 10.00,
    "AllowBackorders": false,
    "RetentionDays": 365,
    "Currency": "EUR"
  },
  "Serilog": {
    "MinimumLevel": {
      "Default": "Information",
      "Override": {
        "Microsoft": "Warning",
        "Microsoft.AspNetCore": "Warning",
        "System": "Warning"
      }
    },
    "Enrich": [ "FromLogContext", "WithMachineName", "WithEnvironmentName" ],
    "WriteTo": [
      {
        "Name": "Console",
        "Args": {
          "outputTemplate": "[{Timestamp:HH:mm:ss} {Level:u3}] {SourceContext} {Message:lj} {Properties:j}{NewLine}{Exception}"
        }
      }
    ]
  }
}
```

---

## Paso 10 — appsettings.Development.json de Orders.API (fichero completo)

Reemplazar `src/Orders.API/appsettings.Development.json`:

```json
{
  "Logging": {
    "LogLevel": {
      "Default": "Debug",
      "Microsoft.AspNetCore": "Information",
      "Microsoft.EntityFrameworkCore.Database.Command": "Information"
    }
  },
  "Orders": {
    "MaxLinesPerOrder": 50,
    "MinOrderAmount": 0.01,
    "AllowBackorders": true,
    "RetentionDays": 90,
    "Currency": "EUR"
  },
  "Serilog": {
    "MinimumLevel": {
      "Default": "Debug",
      "Override": {
        "Microsoft.EntityFrameworkCore.Database.Command": "Information",
        "Orders.API": "Debug"
      }
    }
  }
}
```

---

## Paso 11 — appsettings.Production.json de Orders.API (fichero completo)

Reemplazar `src/Orders.API/appsettings.Production.json`:

```json
{
  "Logging": {
    "LogLevel": {
      "Default": "Warning",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "Orders": {
    "MaxLinesPerOrder": 50,
    "MinOrderAmount": 10.00,
    "AllowBackorders": false,
    "RetentionDays": 365,
    "Currency": "EUR"
  },
  "Serilog": {
    "MinimumLevel": {
      "Default": "Warning",
      "Override": {
        "Orders.API": "Information"
      }
    },
    "WriteTo": [
      {
        "Name": "Console"
      }
    ]
  }
}
```

> **Nota:** En M7.2 se añadirá el `ApplicationInsights` sink en `appsettings.Production.json` con la connection string de Key Vault. Por ahora solo Console para que el proceso no falle sin App Insights configurado.

---

## Paso 12 — Verificar que compila

```bash
dotnet build OrderFlow.sln
```

Sin errores ni warnings.

---

## Paso 13 — Arrancar con Aspire y verificar observabilidad

```bash
dotnet run --project infrastructure/OrderFlow.AppHost
```

Hacer una petición con el fichero `.http`:

```http
POST https://localhost:7153/api/orders
Content-Type: application/json

{
  "customerId": "3fa85f64-5717-4562-b3fc-2c963f66afa6",
  "items": []
}
```

**En el dashboard de Aspire verificar:**

| Pestaña | Qué verificar |
|---|---|
| **Logs** | Log de request con `CorrelationId` y `TraceId` visibles |
| **Traces** | Traza del `POST /api/orders` con span visible |
| **Metrics** | Contador `http.server.request.duration` registrado |

Log esperado en consola:

```
[10:23:45 INF] Orders.API.Controllers.OrdersController
  Creating new order. MaxLines: 50, Currency: EUR
  {"CorrelationId": "3fa85f6457174562", "TraceId": "4bf92f3577b34da6..."}

[10:23:45 INF] HTTP POST /api/orders responded 201 in 45.3 ms
  {"RequestHost": "localhost:7153", "CorrelationId": "3fa85f6457174562"}
```

---

## Paso 14 — Estructura final verificada

```
src/Orders.API/
├── Orders.API.csproj                         ✅ paquetes Serilog añadidos
├── Program.cs                                ✅ bootstrap logger + Serilog + OTel + middleware
├── Controllers/
│   └── OrdersController.cs                  ✅ sin cambios (ILogger<T> funciona con Serilog)
├── API/
│   └── Middleware/
│       └── CorrelationIdMiddleware.cs        ✅ genera/propaga X-Correlation-Id
├── Application/
│   └── Settings/
│       ├── OrdersSettings.cs                ✅ sin cambios desde M2.2
│       └── OrdersSettingsValidator.cs       ✅ sin cambios desde M2.2
├── Infrastructure/
│   └── Telemetry/
│       ├── OpenTelemetryEnricher.cs         ✅ TraceId + SpanId en logs Serilog
│       ├── OrdersTelemetry.cs               ✅ ActivitySource para spans manuales
│       └── OrdersMetrics.cs                 ✅ Counters + Histogram de negocio
├── appsettings.json                         ✅ sección "Serilog" añadida
├── appsettings.Development.json             ✅ Debug + EF queries
└── appsettings.Production.json              ✅ Warning + Orders.API en Information
```

---

## Paso 15 — Commit y push

```bash
git add .
git commit -m "m2.3: Serilog, Correlation IDs y OpenTelemetry

- Serilog con bootstrap logger, enrichers y configuración por entorno
- UseSerilogRequestLogging() con filtro de health checks (Verbose)
- Log.CloseAndFlush() en finally para no perder logs al parar el servicio
- CorrelationIdMiddleware: genera/propaga X-Correlation-Id, añade al LogContext
- OpenTelemetryEnricher: TraceId y SpanId de OTel en cada log de Serilog
- OrdersTelemetry: ActivitySource 'Orders.API' para spans manuales
- OrdersMetrics: counters orders.created.total y orders.failed.total,
  histogram orders.processing.duration.ms
- OTel extendido: AddEntityFrameworkCoreInstrumentation, AddRuntimeInstrumentation
- appsettings por entorno: Debug en Development, Warning en Production"

git push origin m2.3
```

---

## Checklist final

- [ ] `dotnet build OrderFlow.sln` → sin errores ni warnings
- [ ] `dotnet run --project infrastructure/OrderFlow.AppHost` → Dashboard arranca
- [ ] Hacer un `POST /api/orders` con el fichero .http
- [ ] Log del request visible en pestaña **Logs** del Dashboard de Aspire con `CorrelationId`
- [ ] Traza del `POST /api/orders` visible en pestaña **Traces** con span
- [ ] Métricas `http.server.request.duration` en pestaña **Metrics**
- [ ] Header `X-Correlation-Id` presente en la respuesta HTTP
- [ ] `GET /health` NO genera log de request (Verbose filtrado)
- [ ] `GET /alive` NO genera log de request (Verbose filtrado)
- [ ] Commit en rama `m2.3` y push completados

Si algún punto falla, resolverlo antes de avanzar. M2.4 añade los tests que verifican todo lo construido en M2.1-M2.3.

---

## Lo que viene en M2.4

`docs/m2.4.md` → Testing:
- La pirámide de testing en microservicios
- Unit tests con xUnit y NSubstitute
- Integration tests con WebApplicationFactory y LocalDB
- Contract testing con Pact — verificar que el contrato entre Orders y Products no se rompe
