# M3.6 — Práctica Guiada: Orders.API Completo

---

## Microservicios con .NET 10

Módulo 3 — Desarrollo de Microservicios
Submódulo 3.6 — Práctica Guiada: Orders.API Completo

Duración estimada: 60 minutos
Curso de 25 horas · Formación técnica

---

## Esta sesión es diferente

M3.1 a M3.5 fueron sesiones de concepto + código. M3.6 es un laboratorio: construimos Orders.API de principio a fin, integrando todo lo aprendido.

**No hay conceptos nuevos.** Solo integración, verificación y consolidación.

Al terminar esta sesión tendrás:
- Orders.API funcional con dominio, Controllers, CQRS, EF Core + LocalDB, Serilog, Polly y health checks
- Todos los tests pasando
- Ambos servicios (Orders + Products) arrancando con F5 en Aspire
- Llamadas HTTP reales entre Orders y Products
- Trazas distribuidas visibles en el dashboard de Aspire

---

## El punto de partida

Antes de empezar el lab, verifica que tienes instalado (recordatorio de Requisitos del curso):

```
✅ Visual Studio 2022 v17.12+ con workloads:
   - ASP.NET and web development
   - Data storage and processing
   - .NET Aspire SDK

✅ dotnet --version  →  10.0.x
✅ sqllocaldb info   →  al menos MSSQLLocalDB
✅ git --version     →  2.x
```

Si algo falla, revisa los requisitos de software de M0 (Prerequisites).

**Estructura de la solución al inicio:**
```
OrderFlow.sln
├── src/Orders.API/        ← construido en M3.1-M3.5 (puede estar incompleto)
├── src/Products.API/      ← construido en M3.3
├── infrastructure/
│   ├── OrderFlow.AppHost/
│   └── OrderFlow.ServiceDefaults/
└── tests/Orders.API.Tests/
```

---

## Checkpoint 0: La estructura de la solución

Antes del código, la estructura. Verifica que tienes estas carpetas en Orders.API:

```
Orders.API/
├── Domain/
│   ├── Entities/
│   │   ├── Order.cs
│   │   └── OrderLine.cs
│   ├── ValueObjects/
│   │   ├── Money.cs
│   │   ├── Address.cs
│   │   └── OrderStatus.cs
│   ├── Events/
│   │   ├── IDomainEvent.cs
│   │   ├── OrderCreatedEvent.cs
│   │   └── OrderConfirmedEvent.cs
│   ├── Exceptions/
│   │   └── DomainException.cs
│   ├── Interfaces/
│   │   └── IOrderRepository.cs
│   └── Guards.cs
├── Application/
│   ├── Commands/
│   ├── Queries/
│   ├── Behaviors/
│   ├── DTOs/
│   └── Exceptions/
├── Infrastructure/
│   ├── Persistence/
│   └── Http/
├── API/
│   ├── Controllers/
│   ├── DTOs/
│   ├── Mappings/
│   └── Middleware/
└── Program.cs
```

Si te falta alguna carpeta, créala ahora.

---

## Paso 1: El dominio (M3.1)

**Objetivo:** Orders.API tiene el dominio completo y compilando.

Verifica que `Order.cs` tiene:

```csharp
// Checklist rápido — el aggregate completo
public class Order
{
    // ✅ Propiedades con private setter
    public Guid Id { get; private set; }
    public Guid CustomerId { get; private set; }
    public OrderStatus Status { get; private set; }
    public Address ShippingAddress { get; private set; } = null!;
    public DateTime CreatedAt { get; private set; }
    public DateTime? ConfirmedAt { get; private set; }
    public DateTime? CancelledAt { get; private set; }
    public string? CancellationReason { get; private set; }

    // ✅ Colecciones privadas con readonly access
    private readonly List<OrderLine> _lines = [];
    public IReadOnlyList<OrderLine> Lines => _lines.AsReadOnly();
    private readonly List<IDomainEvent> _domainEvents = [];
    public IReadOnlyList<IDomainEvent> DomainEvents => _domainEvents.AsReadOnly();

    // ✅ Total calculado, nunca almacenado
    public Money Total => Lines.Any()
        ? Lines.Skip(1).Aggregate(Lines[0].LineTotal,
            (acc, l) => acc.Add(l.LineTotal))
        : Money.Zero("EUR");

    // ✅ Constructor privado para EF Core
    private Order() { }

    // ✅ Factory method
    public static Order Create(...) { }

    // ✅ Métodos de negocio
    public void Confirm() { }
    public void Cancel(string reason) { }
    public void ClearDomainEvents() => _domainEvents.Clear();
}
```

**Test rápido:** `dotnet build Orders.API` sin errores.

---

## Paso 2: La persistencia (EF Core + LocalDB)

**Objetivo:** OrderDbContext configurado y migration aplicada a LocalDB.

Verifica el `OrderDbContext.cs`:

```csharp
public class OrderDbContext : DbContext
{
    public OrderDbContext(DbContextOptions<OrderDbContext> options)
        : base(options) { }

    public DbSet<Order> Orders { get; set; }

    protected override void OnModelCreating(ModelBuilder modelBuilder)
    {
        modelBuilder.HasDefaultSchema("orders");

        modelBuilder.Entity<Order>(entity =>
        {
            entity.ToTable("Orders");
            entity.HasKey(o => o.Id);

            // ✅ OrderStatus como string
            entity.Property(o => o.Status)
                .HasConversion(s => s.Value, v => OrderStatus.FromString(v))
                .HasColumnName("Status")
                .HasMaxLength(20);

            // ✅ Address como owned entity
            entity.OwnsOne(o => o.ShippingAddress, addr =>
            {
                addr.Property(a => a.Street).HasColumnName("ShippingStreet").HasMaxLength(200);
                addr.Property(a => a.City).HasColumnName("ShippingCity").HasMaxLength(100);
                addr.Property(a => a.ZipCode).HasColumnName("ShippingZipCode").HasMaxLength(20);
                addr.Property(a => a.Country).HasColumnName("ShippingCountry").HasMaxLength(2);
            });

            // ✅ OrderLines como entidades hijas
            entity.HasMany("_lines").WithOne()
                .HasForeignKey("OrderId")
                .OnDelete(DeleteBehavior.Cascade);
        });

        modelBuilder.Entity<OrderLine>(entity =>
        {
            entity.ToTable("OrderLines");
            entity.HasKey(l => l.Id);
            entity.OwnsOne(l => l.UnitPrice, money =>
            {
                money.Property(m => m.Amount).HasColumnName("UnitPriceAmount").HasPrecision(18, 2);
                money.Property(m => m.Currency).HasColumnName("UnitPriceCurrency").HasMaxLength(3);
            });
        });
    }
}
```

---

## Checkpoint 1: Crear y aplicar la migration

```bash
# Desde la carpeta de la solución
dotnet ef migrations add InitialOrderSchema \
    --project src/Orders.API \
    --startup-project src/Orders.API \
    --context OrderDbContext

dotnet ef database update \
    --project src/Orders.API \
    --startup-project src/Orders.API \
    --context OrderDbContext
```

**Verificar con Azure Data Studio o SSMS:**
```sql
-- Conectar a: (localdb)\MSSQLLocalDB
-- Debe existir:
SELECT * FROM orders.Orders;       -- Tabla vacía
SELECT * FROM orders.OrderLines;   -- Tabla vacía
```

Si la migration falla, el error más común es la connection string. Verifica en `appsettings.Development.json`:

```json
{
  "ConnectionStrings": {
    "OrdersDb": "Server=(localdb)\\MSSQLLocalDB;Database=OrdersDb;Trusted_Connection=true;TrustServerCertificate=true"
  }
}
```

---

## Paso 3: Los Commands y Queries (MediatR)

**Objetivo:** CQRS con MediatR funcionando.

Verifica que tienes estos ficheros en Application:

```
Application/
├── Commands/
│   ├── CreateOrderCommand.cs          ✅ IRequest<OrderDto>, ITransactional
│   ├── CreateOrderHandler.cs          ✅ IRequestHandler
│   ├── CreateOrderCommandValidator.cs ✅ AbstractValidator<CreateOrderCommand>
│   ├── ConfirmOrderCommand.cs         ✅ IRequest, ITransactional
│   ├── ConfirmOrderHandler.cs
│   ├── CancelOrderCommand.cs          ✅ IRequest, ITransactional
│   └── CancelOrderHandler.cs
│
├── Queries/
│   ├── GetOrderByIdQuery.cs           ✅ IRequest<OrderDto?>
│   ├── GetOrderByIdHandler.cs
│   ├── ListOrdersQuery.cs             ✅ IRequest<PagedResult<OrderSummaryDto>>
│   └── ListOrdersHandler.cs
│
└── Behaviors/
    ├── ITransactional.cs              ✅ Marker interface
    ├── LoggingBehavior.cs             ✅ IPipelineBehavior<,>
    ├── ValidationBehavior.cs          ✅ IPipelineBehavior<,>
    └── TransactionBehavior.cs         ✅ IPipelineBehavior<TRequest: ITransactional,>
```

---

## Paso 4: El Controller (M3.2)

**Objetivo:** 5 endpoints REST funcionando.

Verifica `OrdersController.cs`:

```csharp
[ApiController]
[Route("api/[controller]")]
public class OrdersController : ControllerBase
{
    // ✅ Solo IMediator como dependencia
    private readonly IMediator _mediator;
    private readonly ILogger<OrdersController> _logger;

    // ✅ GET /api/orders?status=pending&page=1&pageSize=20
    [HttpGet]
    public async Task<ActionResult<PagedResult<OrderSummaryDto>>> GetAll(...)

    // ✅ GET /api/orders/{id}
    [HttpGet("{id:guid}")]
    public async Task<ActionResult<OrderDto>> GetById(Guid id, ...)

    // ✅ POST /api/orders
    [HttpPost]
    public async Task<ActionResult<OrderDto>> Create(...)
    // Retorna: 201 + Location header

    // ✅ POST /api/orders/{id}/confirm
    [HttpPost("{id:guid}/confirm")]
    public async Task<IActionResult> Confirm(Guid id, ...)
    // Retorna: 204 No Content

    // ✅ POST /api/orders/{id}/cancel
    [HttpPost("{id:guid}/cancel")]
    public async Task<IActionResult> Cancel(Guid id, [FromBody] CancelOrderRequest, ...)
    // Retorna: 204 No Content
}
```

---

## Paso 5: El middleware de excepciones

**Objetivo:** DomainException → 422, OrderNotFoundException → 404.

```csharp
// API/Middleware/DomainExceptionMiddleware.cs

public class DomainExceptionMiddleware
{
    private readonly RequestDelegate _next;
    private readonly ILogger<DomainExceptionMiddleware> _logger;

    public async Task InvokeAsync(HttpContext context)
    {
        try
        {
            await _next(context);
        }
        catch (DomainException ex)
        {
            // ✅ Reglas de negocio → 422
            _logger.LogWarning(ex, "Domain violation: {Message}", ex.Message);
            context.Response.StatusCode = 422;
            await context.Response.WriteAsJsonAsync(new ProblemDetails
            {
                Status = 422, Title = "Business rule violation", Detail = ex.Message
            });
        }
        catch (OrderNotFoundException ex)
        {
            // ✅ Pedido no existe → 404
            context.Response.StatusCode = 404;
            await context.Response.WriteAsJsonAsync(new ProblemDetails
            {
                Status = 404, Title = "Order not found", Detail = ex.Message
            });
        }
        catch (ValidationException ex)
        {
            // ✅ FluentValidation → 422 con detalles de campo
            context.Response.StatusCode = 422;
            var errors = ex.Errors
                .GroupBy(e => e.PropertyName)
                .ToDictionary(g => g.Key, g => g.Select(e => e.ErrorMessage).ToArray());
            await context.Response.WriteAsJsonAsync(new ValidationProblemDetails(errors)
            {
                Status = 422, Title = "Validation failed"
            });
        }
    }
}
```

---

## Paso 6: El Program.cs completo

**Objetivo:** Todo el Program.cs integrado y sin errores de compilación.

```csharp
// Orders.API/Program.cs

Log.Logger = new LoggerConfiguration().WriteTo.Console().CreateBootstrapLogger();

try
{
    var builder = WebApplication.CreateBuilder(args);

    // 1. ServiceDefaults (Aspire + OTel + Health checks base)
    builder.AddServiceDefaults();

    // 2. Logging (Serilog)
    builder.Host.UseSerilog((ctx, svc, cfg) => cfg
        .ReadFrom.Configuration(ctx.Configuration)
        .ReadFrom.Services(svc)
        .Enrich.FromLogContext()
        .Enrich.WithMachineName()
        .Enrich.WithProperty("Service", "orders-api"));

    // 3. Options con validación
    builder.Services.AddOptions<OrdersSettings>()
        .BindConfiguration("Orders")
        .ValidateDataAnnotations()
        .ValidateOnStart();

    // 4. EF Core + SQL Server
    builder.Services.AddDbContext<OrderDbContext>(options =>
        options.UseSqlServer(
            builder.Configuration.GetConnectionString("OrdersDb")));

    // 5. Repositorios
    builder.Services.AddScoped<IOrderRepository, SqlOrderRepository>();

    // 6. MediatR + Behaviors
    builder.Services.AddMediatR(cfg =>
    {
        cfg.RegisterServicesFromAssembly(typeof(Program).Assembly);
        cfg.AddBehavior(typeof(IPipelineBehavior<,>), typeof(LoggingBehavior<,>));
        cfg.AddBehavior(typeof(IPipelineBehavior<,>), typeof(ValidationBehavior<,>));
        cfg.AddBehavior(typeof(IPipelineBehavior<,>), typeof(TransactionBehavior<,>));
    });
    builder.Services.AddValidatorsFromAssembly(typeof(Program).Assembly);

    // 7. HttpClient para Products con Polly
    builder.Services.AddHttpClient<ProductsClient>(c =>
        c.BaseAddress = new Uri("http://products-api"))
        .AddResilienceHandler("products-pipeline", pipeline =>
        {
            pipeline
                .AddRetry(new HttpRetryStrategyOptions { MaxRetryAttempts = 3 })
                .AddCircuitBreaker(new HttpCircuitBreakerStrategyOptions
                    { FailureRatio = 0.5, BreakDuration = TimeSpan.FromSeconds(30) })
                .AddTimeout(TimeSpan.FromSeconds(5));
        });

    // 8. Health Checks adicionales
    builder.Services.AddHealthChecks()
        .AddSqlServer(builder.Configuration.GetConnectionString("OrdersDb")!, name: "orders-db");

    // 9. Controllers + OpenAPI
    builder.Services.AddControllers();
    builder.Services.AddOpenApi();
    builder.Services.AddMemoryCache();

    var app = builder.Build();

    // 10. Middleware pipeline (orden importa)
    app.UseMiddleware<DomainExceptionMiddleware>();
    app.UseMiddleware<CorrelationIdMiddleware>();
    app.UseSerilogRequestLogging(o =>
        o.GetLevel = (ctx, _, _) =>
            ctx.Request.Path.StartsWithSegments("/health")
                ? LogEventLevel.Verbose
                : LogEventLevel.Information);

    if (app.Environment.IsDevelopment())
        app.MapOpenApi();

    app.UseHttpsRedirection();
    app.UseAuthorization();
    app.MapControllers();
    app.MapDefaultEndpoints();

    app.Run();
}
catch (Exception ex) { Log.Fatal(ex, "Orders.API startup failed"); }
finally { Log.CloseAndFlush(); }
```

---

## Checkpoint 2: F5 y el servicio arranca

```bash
# Verificar que el servicio arranca
dotnet run --project src/Orders.API

# Debes ver en los logs:
# [INF] Now listening on: https://localhost:7153
# [INF] Application started. Press Ctrl+C to shut down.
```

**Prueba básica con .http:**

```http
### Health check
GET https://localhost:7153/health
Accept: application/json
# Debe devolver: {"status":"Healthy","entries":{"orders-db":{"status":"Healthy"}}}

### OpenAPI spec
GET https://localhost:7153/openapi/v1.json
# Debe devolver el JSON de la spec OpenAPI

### GET vacío — sin pedidos todavía
GET https://localhost:7153/api/orders
# Debe devolver: {"items":[],"totalCount":0,"page":1,"pageSize":20}
```

Si algo falla en este checkpoint, no continuar. Resolver primero.

---

## Paso 7: Arrancar Aspire con ambos servicios

**Objetivo:** Orders.API y Products.API arrancando juntos con F5.

Verifica el AppHost:

```csharp
// OrderFlow.AppHost/Program.cs

var builder = DistributedApplication.CreateBuilder(args);

var sqlServer = builder.AddConnectionString("sqlserver");
var messaging = builder.AddConnectionString("messaging");

// Bases de datos
var ordersDb = sqlServer.AddDatabase("OrdersDb");
var productsDb = sqlServer.AddDatabase("ProductsDb");

// Products.API (primero porque Orders depende de él)
var products = builder.AddProject<Projects.Products_API>("products-api")
    .WithReference(productsDb)
    .WithReference(messaging);

// Orders.API con referencia a Products
var orders = builder.AddProject<Projects.Orders_API>("orders-api")
    .WithReference(ordersDb)
    .WithReference(messaging)
    .WithReference(products); // ← Service Discovery: orders llama a products

builder.Build().Run();
```

**F5 en el AppHost** → debe abrir el dashboard de Aspire con:
- `orders-api` → Running ✅
- `products-api` → Running ✅

---

## Checkpoint 3: Crear el primer pedido real

Con ambos servicios corriendo, el flujo completo:

```http
### 1. Primero crear un producto en Products.API
POST https://localhost:7200/api/products
Content-Type: application/json

{
    "name": "MacBook Pro 16\" M4 Max",
    "description": "El portátil más potente de Apple",
    "price": 3999.99,
    "currency": "EUR",
    "initialStock": 25,
    "categoryId": "c1b2a3d4-e5f6-0123-4567-89abcdef0123"
}
# Guarda el productId de la respuesta

### 2. Crear un pedido en Orders.API usando ese productId
POST https://localhost:7153/api/orders
Content-Type: application/json

{
    "customerId": "3fa85f64-5717-4562-b3fc-2c963f66afa6",
    "items": [
        {
            "productId": "{{productId}}",
            "productName": "MacBook Pro 16\" M4 Max",
            "quantity": 1,
            "unitPrice": 3999.99,
            "currency": "EUR"
        }
    ],
    "shippingAddress": {
        "street": "Gran Vía 28",
        "city": "Madrid",
        "zipCode": "28013",
        "country": "ES"
    }
}
# Debe devolver 201 + Location header
# Guarda el orderId de la respuesta
```

---

## Checkpoint 4: Ver la traza distribuida en Aspire

Después de crear el pedido, abre el dashboard de Aspire (normalmente en https://localhost:15888):

**Pestaña Traces:**

Busca el trace del POST /api/orders. Debes ver algo como:

```
POST /api/orders (orders-api)               145ms total
  ├── LoggingBehavior                          2ms
  ├── ValidationBehavior                       1ms
  ├── TransactionBehavior                      1ms
  ├── CreateOrderHandler                     138ms
  │   ├── HTTP GET /api/products/{id}          45ms  ← llamada a products-api
  │   │   └── (products-api) GET /api/products/{id}
  │   │       └── SQL SELECT Products...       12ms
  │   └── SQL INSERT Orders                    18ms
  │       └── SQL INSERT OrderLines             8ms
  └── (TransactionBehavior) COMMIT             2ms
```

Esta traza distribuida es el valor de OpenTelemetry: puedes ver exactamente qué pasó, dónde y cuánto tardó, cruzando los dos servicios.

---

## Checkpoint 5: Confirmar y cancelar pedidos

```http
### 3. Confirmar el pedido
POST https://localhost:7153/api/orders/{{orderId}}/confirm
# Debe devolver 204 No Content

### 4. Ver el pedido confirmado
GET https://localhost:7153/api/orders/{{orderId}}
# status debe ser "Confirmed"
# confirmAt debe tener timestamp

### 5. Intentar cancelar un pedido confirmado y luego enviado
POST https://localhost:7153/api/orders/{{orderId}}/cancel
Content-Type: application/json

{ "reason": "Customer changed mind" }

# Si el estado es Confirmed: debe devolver 204 (cancelación válida)
# Si el estado es Shipped: debe devolver 422 con:
# { "title": "Business rule violation",
#   "detail": "Cannot transition from 'Confirmed' to 'Cancelled'" }
```

---

## Checkpoint 6: Validaciones funcionando

Verifica que el ValidationBehavior rechaza requests inválidos:

```http
### Request sin items — debe devolver 422
POST https://localhost:7153/api/orders
Content-Type: application/json

{
    "customerId": "00000000-0000-0000-0000-000000000000",
    "items": [],
    "shippingAddress": {
        "street": "",
        "city": "Madrid",
        "zipCode": "28013",
        "country": "INVALID"
    }
}
# Respuesta esperada: 422 con errors por campo:
# {
#   "title": "Validation failed",
#   "status": 422,
#   "errors": {
#     "CustomerId": ["CustomerId is required"],
#     "Items": ["Order must have at least one item"],
#     "ShippingAddress.Street": ["..."],
#     "ShippingAddress.Country": ["Country must be 2-letter ISO code"]
#   }
# }
```

---

## Checkpoint 7: Los tests pasan

```bash
# Ejecutar todos los tests
dotnet test tests/Orders.API.Tests

# Debe mostrar algo como:
# Test run for Orders.API.Tests
# ...
# Passed!  - Failed: 0, Passed: 18, Skipped: 0, Total: 18

# Si algún test falla, verlo en detalle:
dotnet test tests/Orders.API.Tests --logger "console;verbosity=detailed"
```

Los tests que deben pasar:

**Unit tests del dominio:**
- `Order_Create_WithValidItems_ShouldHavePendingStatus`
- `Order_Confirm_PendingOrder_ShouldChangeStatus`
- `Order_Cancel_ConfirmedOrder_ShouldWork`
- `Order_Cancel_DeliveredOrder_ShouldThrow`
- `Money_Add_SameCurrency_ShouldReturnSum`
- `Money_Add_DifferentCurrencies_ShouldThrow`
- `OrderStatus_TransitionTo_InvalidTransition_ShouldThrow`

**Unit tests de Application:**
- `CreateOrderHandler_ValidCommand_ShouldCreateOrder`
- `CreateOrderHandler_EmptyItems_ShouldThrowDomainException`
- `ValidationBehavior_InvalidCommand_ShouldThrowValidationException`
- `ValidationBehavior_ValidCommand_ShouldCallNext`

**Integration tests:**
- `GetOrders_ReturnsOkWithEmptyList`
- `CreateOrder_ValidRequest_Returns201WithLocation`
- `CreateOrder_InvalidRequest_Returns422`
- `ConfirmOrder_PendingOrder_Returns204`
- `GetOrder_NonExisting_Returns404`
- `Health_ReturnsHealthy`

---

## Verificación: el estado de la BD tras los tests

Después de los tests de integración, la BD de test debe estar limpia. Verifica el test cleanup:

```csharp
// En la OrdersApiFactory
protected override void ConfigureWebHost(IWebHostBuilder builder)
{
    builder.ConfigureTestServices(services =>
    {
        // Usar InMemory para tests rápidos
        services.AddDbContext<OrderDbContext>(options =>
            options.UseInMemoryDatabase($"TestOrdersDb_{Guid.NewGuid()}"));

        // Fake publisher — sin RabbitMQ en tests
        services.AddSingleton<IEventPublisher, FakeEventPublisher>();
    });
    builder.UseEnvironment("Testing");
}
```

Cada test run tiene su propia BD InMemory — no hay contaminación entre tests.

---

## Verificación: logs estructurados en Aspire

En la pestaña **Structured** del dashboard de Aspire, filtra por `Service = orders-api`.

Debes ver logs como:

```
[INF] Handling CreateOrderCommand: { CustomerId: "3fa85f64...", Items: [{...}] }
[INF] HTTP GET http://products-api/api/products/a4b5c6d7... → 200 OK (45ms)
[INF] Order 7b3a2c1d... created. Total: 3999.99 EUR
[INF] Handled CreateOrderCommand in 148ms
[INF] HTTP 201 POST /api/orders → 201 Created (150ms) [3fa85f64...]
```

Fíjate en:
- El `CorrelationId` aparece en todos los logs del mismo request
- El LoggingBehavior loggea el inicio y fin de cada Command con duración
- Los logs de Serilog son estructurados (propiedades separadas, no texto plano)

---

## Verificación: métricas en el dashboard

En la pestaña **Metrics** del dashboard de Aspire:

**http.server.request.duration:** histograma de duración de las peticiones HTTP recibidas por Orders.API
- Filtra por `route = /api/orders` para ver solo los POSTs

**http.client.request.duration:** histograma de duración de peticiones HTTP enviadas por Orders.API a Products.API
- Filtra por `server.address = products-api`

**dotnet.process.cpu.time:** uso de CPU del proceso Orders.API

**dotnet.gc.collections:** número de colecciones del Garbage Collector

Estas métricas son las que, en producción, alimentarán los dashboards de Azure Monitor y las alertas de Application Insights.

---

## Simulación de fallo de resiliencia

Para verificar que Polly funciona, detén Products.API manualmente y observa el comportamiento:

1. En el dashboard de Aspire → click en `products-api` → Stop
2. Intenta crear un pedido en Orders.API
3. Observa los logs de Serilog en la pestaña Structured:

```
[WRN] Retry 1/3 for Products.API. Cause: Connection refused. Next: 500ms
[WRN] Retry 2/3 for Products.API. Cause: Connection refused. Next: 1000ms
[WRN] Retry 3/3 for Products.API. Cause: Connection refused. Next: 2000ms
[WRN] Using cached data — Products.API unavailable (o null si sin caché)
[WRN] HTTP 422 POST /api/orders → 422 Unprocessable Entity
```

4. Reinicia Products.API desde Aspire
5. Crea otro pedido — debe funcionar normalmente

Este es el comportamiento correcto: Orders.API es resiliente a fallos temporales de Products.

---

## El fichero .http definitivo de Orders.API

```http
@ordersUrl = https://localhost:7153
@productsUrl = https://localhost:7200
@customerId = 3fa85f64-5717-4562-b3fc-2c963f66afa6
@orderId = REPLACE_WITH_ACTUAL_ORDER_ID
@productId = REPLACE_WITH_ACTUAL_PRODUCT_ID

### [SETUP] Crear producto de prueba en Products.API
POST {{productsUrl}}/api/products
Content-Type: application/json

{
    "name": "MacBook Pro 16\" M4 Max",
    "price": 3999.99,
    "currency": "EUR",
    "initialStock": 25,
    "categoryId": "c1b2a3d4-e5f6-0123-4567-89abcdef0123"
}

###

### [1] Crear pedido
POST {{ordersUrl}}/api/orders
Content-Type: application/json

{
    "customerId": "{{customerId}}",
    "items": [
        {
            "productId": "{{productId}}",
            "productName": "MacBook Pro 16\" M4 Max",
            "quantity": 1,
            "unitPrice": 3999.99,
            "currency": "EUR"
        }
    ],
    "shippingAddress": {
        "street": "Gran Vía 28",
        "city": "Madrid",
        "zipCode": "28013",
        "country": "ES"
    }
}

###

### [2] Ver el pedido creado
GET {{ordersUrl}}/api/orders/{{orderId}}

###

### [3] Listar todos los pedidos
GET {{ordersUrl}}/api/orders

###

### [4] Listar pedidos pendientes
GET {{ordersUrl}}/api/orders?status=Pending

###

### [5] Confirmar pedido
POST {{ordersUrl}}/api/orders/{{orderId}}/confirm

###

### [6] Cancelar pedido
POST {{ordersUrl}}/api/orders/{{orderId}}/cancel
Content-Type: application/json

{ "reason": "Customer changed mind" }

###

### [ERROR] Body inválido → 422
POST {{ordersUrl}}/api/orders
Content-Type: application/json

{ "customerId": "00000000-0000-0000-0000-000000000000", "items": [] }

###

### [ERROR] Pedido inexistente → 404
GET {{ordersUrl}}/api/orders/99999999-9999-9999-9999-999999999999
```

---

## Diagnóstico: problemas comunes y soluciones

**Error: "No connection could be made to SQL Server LocalDB"**
```bash
# Iniciar la instancia manualmente
sqllocaldb start MSSQLLocalDB
sqllocaldb info MSSQLLocalDB  # Debe mostrar State: Running
```

**Error: "The entity type 'OrderLine' requires a primary key"**
```csharp
// En el DbContext — añadir HasKey
modelBuilder.Entity<OrderLine>(entity =>
    entity.HasKey(l => l.Id)); // ← asegúrate de tenerlo
```

**Error: "Could not resolve service for type 'IOrderRepository'"**
```csharp
// En Program.cs — verificar que está registrado
builder.Services.AddScoped<IOrderRepository, SqlOrderRepository>();
```

**Error: Aspire no arranca Products.API**
```
# En el AppHost, verificar que el nombre del proyecto coincide exactamente
builder.AddProject<Projects.Products_API>("products-api")
//                 ↑ Namespace exacto del proyecto
```

**Error: ValidationException no convierte a 422**
```csharp
// Verificar que el middleware está ANTES de app.MapControllers()
app.UseMiddleware<DomainExceptionMiddleware>(); // ← primero
app.MapControllers();                           // ← después
```

---

## Lo que NO está en M3.6 todavía

Para tenerlo claro — estas funcionalidades se añaden en módulos posteriores:

**Módulo 4 — Comunicación:**
- ❌ Mensajería asíncrona con RabbitMQ (cuando el pedido se crea, no hay eventos reales todavía)
- ❌ Notifications.API (M4.2)
- ❌ Payments.API (M4.3)
- ❌ Saga de pedidos completa (M4.3)
- ❌ YARP API Gateway (M4.4)

**Módulo 5 — Seguridad:**
- ❌ JWT / OAuth 2.0 (M5.1)
- ❌ Keycloak (M5.1)

**Módulo 6 — Datos:**
- ❌ Outbox Pattern (M6.2)
- ❌ Read model con Dapper (M6.3)

**Módulo 7 — Despliegue:**
- ❌ Azure App Services (M7.2)
- ❌ GitHub Actions CI/CD (M7.3)
- ❌ Application Insights en producción (M7.4)

Lo que tenemos es sólido, completo y listo para el siguiente paso.

---

## Paso adicional: el SqlOrderRepository completo

**Objetivo:** Verificar que el repositorio implementa la interfaz del dominio correctamente.

```csharp
// Infrastructure/Persistence/SqlOrderRepository.cs

public class SqlOrderRepository : IOrderRepository
{
    private readonly OrderDbContext _dbContext;

    public SqlOrderRepository(OrderDbContext dbContext)
        => _dbContext = dbContext;

    public async Task<Order?> GetByIdAsync(Guid id, CancellationToken ct = default)
        => await _dbContext.Orders
            .Include("_lines") // Campo privado — acceso por nombre
            .FirstOrDefaultAsync(o => o.Id == id, ct);

    public async Task<IReadOnlyList<Order>> GetByCustomerIdAsync(
        Guid customerId, CancellationToken ct = default)
        => await _dbContext.Orders
            .Include("_lines")
            .Where(o => o.CustomerId == customerId)
            .OrderByDescending(o => o.CreatedAt)
            .ToListAsync(ct);

    public async Task<(IReadOnlyList<Order> Orders, int Total)> ListAsync(
        string? status,
        Guid? customerId,
        int page,
        int pageSize,
        CancellationToken ct = default)
    {
        var query = _dbContext.Orders.Include("_lines").AsQueryable();

        if (!string.IsNullOrEmpty(status))
            query = query.Where(o => o.Status.Value == status);

        if (customerId.HasValue)
            query = query.Where(o => o.CustomerId == customerId);

        var total = await query.CountAsync(ct);
        var orders = await query
            .OrderByDescending(o => o.CreatedAt)
            .Skip((page - 1) * pageSize)
            .Take(pageSize)
            .ToListAsync(ct);

        return (orders, total);
    }

    public async Task SaveAsync(Order order, CancellationToken ct = default)
    {
        var entry = _dbContext.Entry(order);
        if (entry.State == EntityState.Detached)
            _dbContext.Orders.Add(order);
        await _dbContext.SaveChangesAsync(ct);
    }
}
```

---

## Paso adicional: las extensiones de mapeo

**Objetivo:** Mapeo limpio de entidades de dominio a DTOs.

```csharp
// API/Mappings/OrderMappings.cs

public static class OrderMappings
{
    public static OrderDto ToDto(this Order order) => new()
    {
        Id = order.Id,
        CustomerId = order.CustomerId,
        Status = order.Status.Value,
        Total = order.Total.Amount,
        Currency = order.Total.Currency,
        CreatedAt = order.CreatedAt,
        ConfirmedAt = order.ConfirmedAt,
        CancelledAt = order.CancelledAt,
        CancellationReason = order.CancellationReason,
        Lines = order.Lines.Select(l => l.ToLineDto()).ToList(),
        ShippingAddress = order.ShippingAddress.ToAddressDto()
    };

    public static OrderSummaryDto ToSummaryDto(this Order order) => new()
    {
        Id = order.Id,
        CustomerId = order.CustomerId,
        Status = order.Status.Value,
        Total = order.Total.Amount,
        Currency = order.Total.Currency,
        LinesCount = order.Lines.Count,
        CreatedAt = order.CreatedAt
    };

    public static OrderLineDto ToLineDto(this OrderLine line) => new()
    {
        Id = line.Id,
        ProductId = line.ProductId,
        ProductName = line.ProductName,
        Quantity = line.Quantity,
        UnitPrice = line.UnitPrice.Amount,
        LineTotal = line.LineTotal.Amount
    };

    public static OrderAddressDto ToAddressDto(this Address address) => new()
    {
        Street = address.Street,
        City = address.City,
        ZipCode = address.ZipCode,
        Country = address.Country
    };

    public static CreateOrderCommand ToCommand(this CreateOrderRequest request)
        => new(
            request.CustomerId,
            request.Items.Select(i => new CreateOrderItemDto(
                i.ProductId, i.ProductName, i.Quantity, i.UnitPrice, i.Currency)).ToList(),
            new CreateOrderAddressDto(
                request.ShippingAddress.Street, request.ShippingAddress.City,
                request.ShippingAddress.ZipCode, request.ShippingAddress.Country));
}
```

---

## Paso adicional: appsettings completo

```json
// Orders.API/appsettings.json
{
  "Serilog": {
    "MinimumLevel": {
      "Default": "Information",
      "Override": {
        "Microsoft": "Warning",
        "Microsoft.EntityFrameworkCore.Database.Command": "Warning"
      }
    },
    "WriteTo": [{ "Name": "Console" }],
    "Enrich": ["FromLogContext", "WithMachineName", "WithEnvironmentName"]
  },
  "Orders": {
    "MaxLinesPerOrder": 50,
    "MinOrderAmount": 10.00,
    "Currency": "EUR"
  },
  "Resilience": {
    "ProductsApi": {
      "RetryAttempts": 3,
      "RetryDelayMs": 500,
      "CircuitBreakerFailureRatio": 0.5,
      "CircuitBreakerBreakDurationSec": 30,
      "TimeoutSec": 5
    }
  }
}

// Orders.API/appsettings.Development.json
{
  "Serilog": {
    "MinimumLevel": {
      "Default": "Debug",
      "Override": {
        "Microsoft.EntityFrameworkCore.Database.Command": "Information"
      }
    }
  },
  "Orders": {
    "MinOrderAmount": 0.01
  },
  "ConnectionStrings": {
    "OrdersDb": "Server=(localdb)\\MSSQLLocalDB;Database=OrdersDb;Trusted_Connection=true;TrustServerCertificate=true"
  }
}
```

---

## Paso adicional: el ConfirmOrderHandler completo

```csharp
// Application/Commands/ConfirmOrderHandler.cs

public class ConfirmOrderHandler : IRequestHandler<ConfirmOrderCommand>
{
    private readonly IOrderRepository _repository;
    private readonly ILogger<ConfirmOrderHandler> _logger;

    public ConfirmOrderHandler(
        IOrderRepository repository,
        ILogger<ConfirmOrderHandler> logger)
    {
        _repository = repository;
        _logger = logger;
    }

    public async Task Handle(ConfirmOrderCommand command, CancellationToken ct)
    {
        _logger.LogInformation("Confirming order {OrderId}", command.OrderId);

        var order = await _repository.GetByIdAsync(command.OrderId, ct)
            ?? throw new OrderNotFoundException(command.OrderId);

        // Confirmar — lanza DomainException si el estado no lo permite
        order.Confirm();

        await _repository.SaveAsync(order, ct);

        _logger.LogInformation(
            "Order {OrderId} confirmed at {ConfirmedAt}",
            order.Id, order.ConfirmedAt);
    }
}

// Application/Commands/CancelOrderHandler.cs
public class CancelOrderHandler : IRequestHandler<CancelOrderCommand>
{
    private readonly IOrderRepository _repository;

    public CancelOrderHandler(IOrderRepository repository)
        => _repository = repository;

    public async Task Handle(CancelOrderCommand command, CancellationToken ct)
    {
        var order = await _repository.GetByIdAsync(command.OrderId, ct)
            ?? throw new OrderNotFoundException(command.OrderId);

        order.Cancel(command.Reason);
        await _repository.SaveAsync(order, ct);
    }
}
```

---

## Laboratorio extendido: explorar el dashboard de Aspire

Dedica 5-10 minutos a explorar el dashboard mientras el sistema está corriendo:

**Resources tab:**
- Ver el estado de cada servicio: Running / Stopped / Error
- Ver las URLs de cada servicio (HTTP y HTTPS)
- Ver las variables de entorno que Aspire inyecta automáticamente
- Ver los endpoints expuestos por cada servicio

**Structured tab (logs):**
- Filtrar por `Service = orders-api`
- Buscar logs de un CorrelationId específico: copia el CorrelationId de la respuesta HTTP y búscalo
- Comparar los logs entre `orders-api` y `products-api` para el mismo request

**Traces tab:**
- Hacer click en una traza de crear pedido
- Explorar el timeline de spans
- Identificar dónde se pierde más tiempo: HTTP call vs SQL vs MediatR overhead

**Metrics tab:**
- Ver `http.server.request.duration` para Orders.API
- Filtrar por `route = POST /api/orders`
- Ver el histograma P50, P90, P99 de duración

---

## Laboratorio: simular carga con múltiples pedidos

Para ver las métricas con datos reales:

```http
### Crear 5 pedidos rápidos (ejecutar varias veces)
POST https://localhost:7153/api/orders
Content-Type: application/json

{
    "customerId": "3fa85f64-5717-4562-b3fc-2c963f66afa6",
    "items": [
        {
            "productId": "{{productId}}",
            "productName": "MacBook Pro",
            "quantity": 1,
            "unitPrice": 3999.99,
            "currency": "EUR"
        }
    ],
    "shippingAddress": {
        "street": "Gran Vía 28",
        "city": "Madrid",
        "zipCode": "28013",
        "country": "ES"
    }
}
```

Después de 5-10 pedidos:

**En Aspire Metrics:**
- `http.server.request.duration` mostrará múltiples puntos
- Podrás ver si hay variación en los tiempos de respuesta

**En Aspire Structured:**
- Múltiples logs de `CreateOrderCommand handled in Xms`
- Puedes identificar si alguno tarda más de lo normal

**En SQL Server (Azure Data Studio):**
```sql
SELECT Status, COUNT(*) as Quantity
FROM orders.Orders
GROUP BY Status
```

---

## Verificación: el flujo de datos completo

Trazabilidad completa de un pedido desde la petición HTTP hasta la base de datos:

```
[Browser/Postman]
    POST /api/orders { customerId, items, address }
        ↓ HTTP
[YARP Gateway] ← en M4.4, por ahora acceso directo
        ↓
[Orders.API - OrdersController]
    request.ToCommand() → CreateOrderCommand
        ↓ _mediator.Send()
[MediatR Pipeline]
    LoggingBehavior → start timer
    ValidationBehavior → FluentValidation OK
    TransactionBehavior → BEGIN TRANSACTION
        ↓
[CreateOrderHandler]
    ProductsClient.GetProductAsync() → HTTP GET products-api/...
        ↓ HTTP
    [Products.API - GetProductEndpoint]
        → SQL SELECT Products WHERE Id = ...
        ← 200 { name, price, stock }
    ↓
    Order.Create(customerId, items, address)
    → Order aggregate con domain events
    ↓
    _repository.SaveAsync(order)
    → SQL INSERT Orders
    → SQL INSERT OrderLines
    ↓
    mediator.Publish(OrderCreatedEvent) ← pendiente M4.2
    order.ClearDomainEvents()
    ← return OrderDto
        ↓
[TransactionBehavior] → COMMIT
[LoggingBehavior] → log 148ms
        ↓
[OrdersController]
    CreatedAtAction(GetById, {id}, orderDto)
        ↓ HTTP
[Browser/Postman]
    201 Created + Location: /api/orders/7b3a2c1d...
    { id, status: "Pending", total: 3999.99, ... }
```

---

## Laboratorio de resiliencia avanzado

Para los que quieran explorar más, un ejercicio opcional:

**Objetivo:** Observar el Circuit Breaker en acción.

1. Crea un endpoint falso en Products.API que siempre devuelve 503:
```csharp
// En Products.API, añadir temporalmente:
app.MapGet("/api/products/chaos", () => Results.StatusCode(503));
```

2. Configura un ProductsClient alternativo que llame a ese endpoint repetidamente.

3. En Aspire Structured logs, deberías ver:
```
[WRN] Retry 1/3 for Products.API. Cause: 503 ServiceUnavailable
[WRN] Retry 2/3 for Products.API. Cause: 503 ServiceUnavailable
[WRN] Retry 3/3 for Products.API. Cause: 503 ServiceUnavailable
[ERR] Circuit OPENED for Products.API! Break duration: 30s
[WRN] Circuit open — using cached data (o null si sin caché)
```

4. Espera 30 segundos → Circuit Breaker pasa a Half-Open
5. En el siguiente intento verás:
```
[INF] Circuit HALF-OPEN — testing Products.API...
[INF] Circuit CLOSED — Products.API is healthy again
```

---

## Preguntas de reflexión del Módulo 3

Antes de pasar al Módulo 4, reflexiona:

**Sobre el diseño:**
- ¿Dónde vive la regla "un pedido no puede cancelarse si ya está entregado"? ¿Por qué ahí y no en el Controller?
- Si quieres añadir una nueva operación de negocio (ej: "marcar como enviado"), ¿qué tienes que crear? ¿Qué NO tienes que tocar?

**Sobre la observabilidad:**
- Tienes un pedido con un bug en producción. El cliente dice "fallé al crear el pedido hace 2 horas". ¿Cómo lo encontrarías en los logs?
- ¿Qué información del CorrelationId necesitas para trazar el problema de principio a fin?

**Sobre la resiliencia:**
- Si el Circuit Breaker está abierto 30 segundos, ¿qué ocurre con los pedidos que llegan durante ese tiempo?
- ¿Cuándo sería mejor aumentar el BreakDuration a 60 segundos? ¿Y cuándo reducirlo a 10?

**Sobre CQRS:**
- ¿Por qué las Queries no tienen TransactionBehavior? ¿Qué pasaría si lo tuvieran?
- Si en el futuro quieres que el listado de pedidos sea ultra-rápido, ¿qué cambiarías en la ListOrdersQuery?

---

## Guía de debugging: Visual Studio + Aspire

Con Aspire, el debugging multi-servicio es más fácil que nunca:

**Breakpoint en Orders.API y Products.API simultáneamente:**
1. Pon un breakpoint en `CreateOrderHandler.Handle()`
2. Pon un breakpoint en `GetProductEndpoint.Handle()`
3. F5 desde el AppHost
4. Envía POST /api/orders desde el fichero .http
5. Visual Studio parará primero en `CreateOrderHandler`, luego en `GetProductEndpoint`

Sin Aspire, necesitarías dos instancias de VS o VS + VS Code corriendo simultáneamente. Con Aspire y el AppHost, el debugger salta automáticamente entre proyectos.

**Hot Reload con Aspire:**
- Modifica el código de Orders.API mientras está corriendo
- Ctrl+S → Aspire detecta el cambio y recarga el servicio
- Sin necesidad de detener/reiniciar manualmente
- El estado de la BD se mantiene entre recargas

---

## El patrón de proyecto reutilizable

La estructura que hemos construido en Orders.API es un patrón que se puede replicar en cualquier microservicio:

**Para Payments.API (M4.3):**
- Payment aggregate con PaymentLine, PaymentStatus
- Commands: ProcessPaymentCommand, RefundPaymentCommand
- Queries: GetPaymentByIdQuery, GetPaymentsByOrderQuery
- Polly para llamar a un provider de pagos externo

**Para Notifications.API (M4.2):**
- Notification entity (sin aggregate, dominio simple)
- Consumers de MassTransit en vez de Commands HTTP
- Sin Polly (reactivo, no llama a otros servicios)

El patrón es el mismo. Los detalles de negocio cambian. La estructura se mantiene.

---

## Integración continua: los tests en el pipeline

Preview de cómo los tests del Módulo 3 encajan en el pipeline de CI (lo configuraremos en M7.3):

```yaml
# .github/workflows/orders-api.yml (preview)

name: Orders.API CI

on:
  push:
    paths: ['src/Orders.API/**', 'tests/Orders.API.Tests/**']

jobs:
  test:
    runs-on: windows-latest  # LocalDB requiere Windows
    steps:
      - uses: actions/checkout@v4

      - name: Setup .NET 10
        uses: actions/setup-dotnet@v4
        with: { dotnet-version: '10.0.x' }

      - name: Unit Tests
        run: dotnet test tests/Orders.API.Tests --filter "Category=Unit"

      - name: Integration Tests
        run: dotnet test tests/Orders.API.Tests --filter "Category=Integration"
        # LocalDB disponible en windows-latest directamente

      - name: Coverage
        run: dotnet test tests/Orders.API.Tests --collect "XPlat Code Coverage"

      - name: Build Orders.API
        run: dotnet build src/Orders.API -c Release
```

Con este pipeline, cada push a `Orders.API` ejecuta los tests automáticamente. Si algo falla, el PR no puede mergearse.

---

## Referencia rápida: comandos útiles durante el lab

```bash
# Verificar la solution completa
dotnet build OrderFlow.sln

# Ejecutar solo los tests de Orders.API
dotnet test tests/Orders.API.Tests

# Ejecutar tests por categoría
dotnet test --filter "Category=Unit"
dotnet test --filter "Category=Integration"

# Crear una nueva migration
dotnet ef migrations add NombreDeLaMigration \
    --project src/Orders.API \
    --context OrderDbContext

# Aplicar migrations pendientes
dotnet ef database update \
    --project src/Orders.API \
    --context OrderDbContext

# Ver migrations aplicadas
dotnet ef migrations list \
    --project src/Orders.API

# Revertir la última migration
dotnet ef migrations remove \
    --project src/Orders.API

# Ver el SQL que genera EF Core (sin aplicar)
dotnet ef migrations script \
    --project src/Orders.API

# Inspeccionar la BD desde CLI
sqllocaldb info MSSQLLocalDB
sqllocaldb start MSSQLLocalDB

# Listar las tablas creadas
# (usar Azure Data Studio o SSMS para esto)
```

---

## Métricas de éxito del Módulo 3

¿Cómo saber si el módulo está bien hecho? Estos son los indicadores:

**Rapidez de desarrollo:**
- Añadir un nuevo endpoint de Orders (ej: GET /api/orders/pending) debería tomar < 30 minutos
- Añadir una nueva regla de validación debería tomar < 10 minutos

**Seguridad de cambios:**
- Cambiar la regla "máximo 50 líneas" a "máximo 100" debería requerir cambiar solo 1 línea de código
- Cambiar el formato de la respuesta del endpoint de listado no debería romper ningún test de dominio

**Observabilidad:**
- Dado un CorrelationId, encontrar todos los logs de ese request en < 2 minutos
- Dado un error en producción (simulado), identificar el handler que falló en < 5 minutos

**Resiliencia:**
- Con Products.API detenido, crear un pedido no debe devolve 500 — debe devolver 422 con mensaje claro
- Con Products.API reiniciado, el siguiente pedido debe funcionar sin reiniciar Orders.API

Si pasas estos cuatro criterios, el Módulo 3 está correctamente implementado.

---

## Qué hemos aprendido en el Módulo 3

El Módulo 3 ha sido el más largo del curso por una razón: es donde se construye la base técnica que todo lo demás aprovecha.

**Del Módulo 3 llevamos al resto del curso:**

**→ Módulo 4 (Comunicación):**
El dominio de Orders emitirá Domain Events que se convertirán en Integration Events y cruzarán a Products, Payments y Notifications vía MassTransit.

**→ Módulo 5 (Seguridad):**
El middleware pipeline que construimos (correlación, excepciones) recibirá también el middleware de JWT. Los Controllers recibirán `[Authorize]` sin cambios de arquitectura.

**→ Módulo 6 (Datos):**
El CQRS que construimos tendrá su ListOrdersHandler reemplazado con Dapper para el read model. El TransactionBehavior se extenderá con el Outbox Pattern.

**→ Módulo 7 (Despliegue):**
El AppHost de Aspire se transformará en un pipeline de GitHub Actions que despliega automáticamente a Azure App Services.

La arquitectura que construiste aquí no es un ejercicio — es el patrón que vas a usar en producción.

---

## Ejercicio opcional: añadir el endpoint GET /api/orders/my

Un ejercicio para consolidar lo aprendido: añade un endpoint que devuelva los pedidos del usuario autenticado.

**Lo que necesitas crear:**

```csharp
// 1. Nueva Query
public record GetMyOrdersQuery(Guid CustomerId, int Page, int PageSize)
    : IRequest<PagedResult<OrderSummaryDto>>;

// 2. Handler (muy parecido a ListOrdersHandler)
public class GetMyOrdersHandler : IRequestHandler<GetMyOrdersQuery, PagedResult<OrderSummaryDto>>
{
    // Implementación similar a ListOrdersHandler pero filtrando por CustomerId
}

// 3. Nuevo endpoint en el Controller
[HttpGet("my")]
public async Task<ActionResult<PagedResult<OrderSummaryDto>>> GetMyOrders(
    [FromQuery] int page = 1,
    [FromQuery] int pageSize = 20,
    CancellationToken ct = default)
{
    // En M5.1 usaremos User.GetUserId() del JWT
    // Por ahora, hardcodeado para el ejercicio
    var customerId = Guid.Parse("3fa85f64-5717-4562-b3fc-2c963f66afa6");
    var query = new GetMyOrdersQuery(customerId, page, pageSize);
    return Ok(await _mediator.Send(query, ct));
}
```

**Test:**
```http
GET https://localhost:7153/api/orders/my
# Debe devolver solo los pedidos del customerId hardcodeado
```

**Tiempo estimado:** 20-30 minutos.

Si lo completas, tienes claro el patrón CQRS. Si te trabas, revisa el `ListOrdersHandler` — es casi idéntico.

---

## Ejercicio opcional: añadir paginación mejorada con Link headers

Mejora la respuesta del GET /api/orders añadiendo headers de navegación al estilo GitHub API:

```csharp
// En el action GetAll:
var result = await _mediator.Send(new ListOrdersQuery(status, page, pageSize), ct);

// Añadir Link headers
if (result.HasNextPage)
{
    var nextUrl = Url.Action(nameof(GetAll),
        new { status, page = page + 1, pageSize });
    Response.Headers.Append("Link", $"<{nextUrl}>; rel=\"next\"");
}
if (result.HasPreviousPage)
{
    var prevUrl = Url.Action(nameof(GetAll),
        new { status, page = page - 1, pageSize });
    Response.Headers.Append("Link", $"<{prevUrl}>; rel=\"prev\"");
}

// Headers adicionales de paginación
Response.Headers.Append("X-Total-Count", result.TotalCount.ToString());
Response.Headers.Append("X-Total-Pages", result.TotalPages.ToString());

return Ok(result);
```

**Test:** Los headers deben aparecer en la respuesta de Postman o .http files.

---

## Ejercicio opcional: testear el middleware de excepciones

Un test de integración que verifica que el middleware convierte correctamente:

```csharp
[Fact]
public async Task DomainException_ConvertsTo422WithProblemDetails()
{
    // Arrange: crear un pedido y luego intentar una operación inválida
    var createResponse = await _client.PostAsJsonAsync("/api/orders", validRequest);
    var created = await createResponse.Content.ReadFromJsonAsync<OrderDto>();

    // Confirmar para poder intentar una transición inválida
    await _client.PostAsync($"/api/orders/{created!.Id}/confirm", null);

    // Act: intentar cancelar un pedido en estado que no lo permite
    // (simular estado Delivered para el test)
    // En un test real, usaríamos un seeder que ponga el pedido en Delivered

    // Assert el comportamiento del middleware:
    var cancelResponse = await _client.PostAsJsonAsync(
        $"/api/orders/{created.Id}/cancel",
        new { reason = "Test" });

    if (cancelResponse.StatusCode == HttpStatusCode.UnprocessableEntity)
    {
        var problem = await cancelResponse.Content
            .ReadFromJsonAsync<ProblemDetails>();
        problem!.Status.Should().Be(422);
        problem.Title.Should().Be("Business rule violation");
        problem.Detail.Should().Contain("Cannot transition");
    }
}
```

---

## Snapshot del código fuente al final del Módulo 3

Para referencia, el estado de la solution al terminar M3.6:

```
OrderFlow.sln
├── src/
│   ├── Orders.API/                     ← 35 ficheros .cs
│   │   ├── Domain/                     ← 13 ficheros (M3.1)
│   │   ├── Application/                ← 12 ficheros (M3.4)
│   │   ├── Infrastructure/             ← 5 ficheros (M3.2, M3.5)
│   │   └── API/                        ← 8 ficheros (M3.2)
│   │
│   └── Products.API/                   ← 18 ficheros .cs (M3.3)
│       ├── Features/
│       ├── Domain/
│       └── Infrastructure/
│
├── infrastructure/
│   ├── OrderFlow.AppHost/              ← 1 fichero Program.cs
│   └── OrderFlow.ServiceDefaults/      ← 1 fichero Extensions.cs
│
└── tests/
    └── Orders.API.Tests/               ← 12 ficheros de tests
        ├── Unit/Domain/
        ├── Unit/Application/
        ├── Integration/Endpoints/
        └── Shared/
```

Lines of code aproximadas:
- `Orders.API`: ~2.200 líneas
- `Products.API`: ~800 líneas
- `Orders.API.Tests`: ~600 líneas
- `AppHost + ServiceDefaults`: ~80 líneas

---

## Módulo 3 completado ✅

Felicidades. Has construido dos microservicios reales con la arquitectura que se usa en producción.

**Lo que has aprendido:**

📐 **Diseño de dominio:** Aggregates, Value Objects, Domain Events, invariantes, Ubiquitous Language

🔌 **APIs:** Controllers con 5 verbos REST + Minimal APIs con TypedResults y Endpoint Filters

📬 **CQRS:** Commands, Queries, Handlers, Pipeline Behaviors (Logging, Validation, Transaction)

🔒 **Resiliencia:** Polly v8 con Retry, Circuit Breaker, Timeout, Fallback

👁️ **Observabilidad:** Serilog, OpenTelemetry, Aspire dashboard con logs, trazas y métricas

🧪 **Testing:** Pirámide completa, unit tests de dominio, integration tests con WebApplicationFactory

El **Módulo 4** añade la última pieza: la comunicación asíncrona entre servicios con RabbitMQ, MassTransit y la Saga de pedidos. Es donde el sistema se vuelve verdaderamente distribuido.

---

## Cierre del Módulo 3

El Módulo 3 está completo. Hemos construido dos microservicios reales:

**Orders.API** — El servicio más complejo del curso:
- Dominio rico con reglas de negocio encapsuladas
- API REST con Controllers, 5 endpoints
- CQRS con MediatR y pipeline de Behaviors
- Persistencia con EF Core y SQL Server LocalDB
- Resiliencia con Polly v8 para las llamadas a Products
- Observabilidad completa con Serilog + OpenTelemetry

**Products.API** — Servicio ligero y directo:
- Minimal APIs con Route Groups y TypedResults
- FluentValidation con Endpoint Filters
- Gestión de catálogo y stock
- Endpoint de reserva de stock para la futura Saga

**Ambos en Aspire** con F5, con trazas distribuidas, logs correlacionados y métricas en tiempo real.

En el **Módulo 4** conectamos estos servicios con mensajería asíncrona. El flujo de un pedido dejará de ser síncrono — cada servicio reaccionará a eventos, y la Saga coordinará el flujo completo: Orders → Products → Payments → Notifications.

---

## Checklist definitivo del Laboratorio M3.6

**✅ El servicio compila sin errores**
```bash
dotnet build src/Orders.API → Build succeeded. 0 Error(s)
```

**✅ El servicio arranca**
```bash
dotnet run --project src/Orders.API
# [INF] Now listening on: https://localhost:7153
```

**✅ Los endpoints responden**
- GET /health → 200 {"status":"Healthy"}
- GET /api/orders → 200 {"items":[],"totalCount":0}
- POST /api/orders → 201 + Location header
- POST /api/orders/{id}/confirm → 204
- POST /api/orders/{id}/cancel → 204 o 422

**✅ Los tests pasan**
```bash
dotnet test tests/Orders.API.Tests
# Passed! Failed: 0, Passed: N, Skipped: 0
```

**✅ Aspire arranca ambos servicios**
- F5 en AppHost → orders-api Running, products-api Running
- Dashboard abre en https://localhost:15888

**✅ La traza distribuida es visible**
- POST /api/orders genera traza que cruza orders-api y products-api
- Logs correlacionados con el mismo CorrelationId

**El Módulo 3 está completo. Bienvenido al Módulo 4.**

---

## Lo que viene en el Módulo 4

El Módulo 3 está completo. El Módulo 4 es el más emocionante: conectamos todos los servicios con mensajería asíncrona.

**M4.1 — Comunicación síncrona:** Creamos Products.API con HttpClientFactory y Refit. Tipos generados automáticamente desde el contrato OpenAPI.

**M4.2 — Mensajería asíncrona:** Añadimos RabbitMQ + MassTransit. Creamos Notifications.API. El primer evento real cruza servicios.

**M4.3 — Saga Pattern:** Creamos Payments.API. Implementamos la Saga completa: Orders → Products → Payments → Notifications.

**M4.4 — YARP Gateway:** Un punto de entrada único delante de los 4 servicios. Todo el tráfico entra por YARP.

Al terminar el Módulo 4, OrderFlow tendrá sus 4 microservicios comunicándose en tiempo real.
